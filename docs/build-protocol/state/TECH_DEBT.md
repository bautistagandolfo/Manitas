# TECH_DEBT

MEDIUM y LOW aceptados conscientemente en vez de corregidos, más los HIGH
que no bloquean automáticamente y fueron aprobados explícitamente por un
responsable. Referenciado desde `QUALITY_GATE.md` y desde la Fase 0.2.

| ID | Severidad | Descripción | Módulo/Fase de origen | Por qué se acepta | Aprobado por | Fecha |
|---|---|---|---|---|---|---|
| TD-1 | _ejemplo_ | ... | ... | ... | ... | ... |
| TD-2 | LOW | `coverageThreshold` de Jest (80% en `src/modules/**/*.service.ts`, BLUEPRINT §9.8) no está configurado en `backend/package.json`: con la carpeta `modules/` vacía, el glob no matchea ningún archivo y Jest falla (`Coverage data ... was not found`) en vez de solo no aplicar el umbral. | Fase 00 — Setup del proyecto | **Vencido**: la deferral apuntaba a la Fase 07, que ya pasó — `users.service.ts` y `auth.service.ts` existen desde entonces (ambos con cobertura real >85%, ver Fase 12). Falta decidir: configurar el umbral ahora, o re-aceptar la deferral con motivo y fecha nuevos. | agente | 2026-08-19 (vencido, ver nota Fase 12 — 2026-08-23) |
| TD-3 | MEDIUM | Sin timeout en las llamadas del frontend (`frontend/src/lib/http-client.ts`, `fetch` sin `AbortController`): si la red falla a mitad de un login u otra request, la UI queda colgada sin feedback ni forma de cancelar. | Fase 12 — Production readiness (`auth`) | No bloquea: uso real de bajo volumen (una sola tienda, pocos usuarios simultáneos), sin reportes del problema en producción todavía. | agente | 2026-08-23 |
| TD-4 | MEDIUM | `argon2.hash`/`argon2.verify` (`users.service.ts`, `auth.service.ts`) usan los parámetros por defecto de la librería (~64 MiB de memoria por operación). El hosting objetivo es gratuito (BLUEPRINT §9.10, Render free tier ronda 512MB de RAM) — una ráfaga real de logins concurrentes podría agotar la memoria de la instancia. | Fase 12 — Production readiness (`auth`) | No bloquea: la concurrencia esperada para una sola tienda es baja y el rate limit de login (20/min por IP, Fase 08) ya la acota. Revisar si aparecen problemas de memoria reales en producción, o al definir el plan de hosting final. | agente | 2026-08-23 |
| TD-5 | LOW | `GET /users` (`users.controller.ts`) no pagina — trae todos los usuarios en una sola respuesta. | Fase 12 — Production readiness (`auth`) | MVP de una sola tienda, cantidad de usuarios esperada en decenas como mucho — sin problema de performance real a esta escala. | agente | 2026-08-23 |
| TD-6 | LOW | `frontend/src/features/auth/AuthContext.tsx` exporta el componente `AuthProvider` y el hook `useAuth` desde el mismo archivo — ESLint marca `react-refresh/only-export-components` (degrada el fast refresh en dev, sin impacto en producción). | Fase 12 — Production readiness (`auth`) | Cosmético, sin impacto funcional ni de seguridad. | agente | 2026-08-23 |
| TD-7 | LOW | `package.json#prisma` (config de `db seed`) está deprecado — Prisma pide migrar a un archivo `prisma.config.ts` antes de la v7. | Fase 12 — Production readiness (`auth`) | El proyecto fija Prisma por debajo de la v7 a propósito (Prisma 7 es ESM-only). Se revisa cuando se decida actualizar. | agente | 2026-08-23 |
| TD-8 | LOW | En los 10 puntos de `frontend/src/features/catalog/**` que capturan `ApiError` (`ProductForm.tsx`, `VariantGridPage.tsx`, `NewVariantPage.tsx`, `ProductDetailPage.tsx`, `CatalogPage.tsx` y los 4 modals de stock/precio), el mensaje que se muestra al usuario es `err.message` tal cual lo manda el backend. Para cualquier regla que el formulario no valida en el cliente (ej. `nombre` > 200 caracteres, cap MaxLength del DTO), eso deja pasar el string crudo de class-validator en inglés (`"nombre must be shorter than or equal to 200 characters"`) en vez de un mensaje en español — confirmado en vivo en el formulario de alta de producto (Fase 08 QA, 2026-08-23). No es un bug funcional (el error se muestra, no se pierde el estado del formulario, no rompe nada) — es una inconsistencia de idioma/UX. | Fase 08 — QA adversarial (`products`) | No bloquea: no es pérdida de datos ni de funcionalidad, y una traducción genérica de errores de class-validator o una validación de máximos espejada en cada formulario es una mejora de UX transversal a varios módulos, no algo que este ticket deba resolver de forma aislada. | agente | 2026-08-23 |
| TD-9 | LOW | `npm audit` en `backend` subió de 3 a 12 advisories (5 low, 3 moderate, 4 high) tras agregar `@stryker-mutator/core`/`@stryker-mutator/jest-runner` como devDependencies (Fase 08, mutation testing obligatorio de BLUEPRINT §9.8). Los 9 nuevos vienen enteramente de la cadena de Stryker (`@inquirer/*`, `@babel/core`, `ajv`, `external-editor`, `tmp` — este último el único HIGH nuevo). Confirmado con `npm audit --omit=dev`: siguen siendo exactamente los mismos 3 high de siempre (cadena de `prisma` CLI, ya aceptada) — ninguno de los 9 nuevos toca el árbol de producción. | Fase 09 — Security audit (`products`) | No bloquea: Stryker es 100% devDependency, nunca se empaqueta (`nest build` solo incluye `src/`) ni corre como parte del server — solo se invoca a mano (`npx stryker run`) en desarrollo/CI. Revisar cuando Stryker actualice su cadena de `@inquirer/*`/`tmp`. | agente | 2026-08-23 |
