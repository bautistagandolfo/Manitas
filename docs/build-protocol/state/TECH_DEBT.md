# TECH_DEBT

MEDIUM y LOW aceptados conscientemente en vez de corregidos, más los HIGH
que no bloquean automáticamente y fueron aprobados explícitamente por un
responsable. Referenciado desde `QUALITY_GATE.md` y desde la Fase 0.2.

| ID | Severidad | Descripción | Módulo/Fase de origen | Por qué se acepta | Aprobado por | Fecha |
|---|---|---|---|---|---|---|
| TD-1 | _ejemplo_ | ... | ... | ... | ... | ... |
| TD-2 | LOW | `coverageThreshold` de Jest (80% en `src/modules/**/*.service.ts`, BLUEPRINT §9.8) no está configurado en `backend/package.json`: con la carpeta `modules/` vacía, el glob no matchea ningún archivo y Jest falla (`Coverage data ... was not found`) en vez de solo no aplicar el umbral. | Fase 00 — Setup del proyecto | **Vencido**: la deferral apuntaba a la Fase 07, que ya pasó — `users.service.ts` y `auth.service.ts` existen desde entonces (ambos con cobertura real >85%, ver Fase 12). Falta decidir: configurar el umbral ahora, o re-aceptar la deferral con motivo y fecha nuevos. | agente | 2026-08-19 (vencido, ver nota Fase 12 — 2026-08-23) |
| TD-3 | MEDIUM | Sin timeout en las llamadas del frontend (`frontend/src/lib/http-client.ts`, `fetch` sin `AbortController`): si la red falla a mitad de un login u otra request, la UI queda colgada sin feedback ni forma de cancelar. | Fase 12 — Production readiness (`auth`) | No bloquea: uso real de bajo volumen (una sola tienda, pocos usuarios simultáneos), sin reportes del problema en producción todavía. | agente | 2026-08-23 |
| TD-4 | MEDIUM | `argon2.hash`/`argon2.verify` (`users.service.ts`, `auth.service.ts`) usan los parámetros por defecto de la librería (~64 MiB de memoria por operación). El hosting objetivo es gratuito (BLUEPRINT §9.10, Render free tier ronda 512MB de RAM) — una ráfaga real de logins concurrentes podría agotar la memoria de la instancia. | Fase 12 — Production readiness (`auth`) | No bloquea: la concurrencia esperada para una sola tienda es baja y el rate limit de login (20/min por IP, Fase 08) ya la acota. Revisar si aparecen problemas de memoria reales en producción, o al definir el plan de hosting final. | agente | 2026-08-23 |
| TD-5 | LOW | `GET /users` (`users.controller.ts`) no pagina — trae todos los usuarios en una sola respuesta. | Fase 12 — Production readiness (`auth`) | MVP de una sola tienda, cantidad de usuarios esperada en decenas como mucho — sin problema de performance real a esta escala. | agente | 2026-08-23 |
| TD-6 | LOW | `frontend/src/features/auth/AuthContext.tsx` exporta el componente `AuthProvider` y el hook `useAuth` desde el mismo archivo — ESLint marca `react-refresh/only-export-components` (degrada el fast refresh en dev, sin impacto en producción). | Fase 12 — Production readiness (`auth`) | Cosmético, sin impacto funcional ni de seguridad. | agente | 2026-08-23 |
| TD-7 | LOW | `package.json#prisma` (config de `db seed`) está deprecado — Prisma pide migrar a un archivo `prisma.config.ts` antes de la v7. | Fase 12 — Production readiness (`auth`) | El proyecto fija Prisma por debajo de la v7 a propósito (Prisma 7 es ESM-only). Se revisa cuando se decida actualizar. | agente | 2026-08-23 |
| TD-8 | LOW | En los 10 puntos de `frontend/src/features/catalog/**` que capturan `ApiError` (`ProductForm.tsx`, `VariantGridPage.tsx`, `NewVariantPage.tsx`, `ProductDetailPage.tsx`, `CatalogPage.tsx` y los 4 modals de stock/precio), el mensaje que se muestra al usuario es `err.message` tal cual lo manda el backend. Para cualquier regla que el formulario no valida en el cliente (ej. `nombre` > 200 caracteres, cap MaxLength del DTO), eso deja pasar el string crudo de class-validator en inglés (`"nombre must be shorter than or equal to 200 characters"`) en vez de un mensaje en español — confirmado en vivo en el formulario de alta de producto (Fase 08 QA, 2026-08-23). No es un bug funcional (el error se muestra, no se pierde el estado del formulario, no rompe nada) — es una inconsistencia de idioma/UX. | Fase 08 — QA adversarial (`products`) | No bloquea: no es pérdida de datos ni de funcionalidad, y una traducción genérica de errores de class-validator o una validación de máximos espejada en cada formulario es una mejora de UX transversal a varios módulos, no algo que este ticket deba resolver de forma aislada. | agente | 2026-08-23 |
| TD-9 | LOW | `npm audit` en `backend` subió de 3 a 12 advisories (5 low, 3 moderate, 4 high) tras agregar `@stryker-mutator/core`/`@stryker-mutator/jest-runner` como devDependencies (Fase 08, mutation testing obligatorio de BLUEPRINT §9.8). Los 9 nuevos vienen enteramente de la cadena de Stryker (`@inquirer/*`, `@babel/core`, `ajv`, `external-editor`, `tmp` — este último el único HIGH nuevo). Confirmado con `npm audit --omit=dev`: siguen siendo exactamente los mismos 3 high de siempre (cadena de `prisma` CLI, ya aceptada) — ninguno de los 9 nuevos toca el árbol de producción. | Fase 09 — Security audit (`products`) | No bloquea: Stryker es 100% devDependency, nunca se empaqueta (`nest build` solo incluye `src/`) ni corre como parte del server — solo se invoca a mano (`npx stryker run`) en desarrollo/CI. Revisar cuando Stryker actualice su cadena de `@inquirer/*`/`tmp`. | agente | 2026-08-23 |
| TD-10 | LOW | `npm run build` del frontend (`tsc -b && vite build`) advierte que el bundle principal supera 500 kB minificado (611 kB, 187 kB gzip) en un solo chunk — sin `dynamic import()` ni code-splitting por ruta en ningún lugar de `frontend/src`. Notado por primera vez corriendo un build de producción real en la Fase 12 de `products` (las fases 12 anteriores, de `auth`, no dejaron constancia del tamaño del bundle) — coincide con que las pantallas de catálogo/grilla (T2.12) son una porción grande del código de UI agregado hasta ahora. | Fase 12 — Production readiness (`products`) | No bloquea: a la escala del MVP (una tienda, red de sucursal razonable, no un sitio público de alto tráfico) 187 kB gzip de JS es un costo de carga inicial aceptable. Vale la pena revisar `React.lazy`/code-splitting por ruta si el bundle sigue creciendo con `sales`/`cash-registers`/`returns`. | agente | 2026-08-23 |
| TD-11 | LOW | El header `Idempotency-Key` (`common/idempotency/idempotency.interceptor.ts`, T0.14) no tiene cota de longitud — cualquier string no vacío se acepta y se persiste tal cual en una columna `TEXT` sin límite (`cash_movements.idempotency_key`, y las demás tablas que la usan). El único techo real hoy es el tamaño máximo de cabecera HTTP de Node (16KB por defecto). | Fase 09 — Security audit (`cash-registers`) | No bloquea: el valor nunca se ejecuta ni se interpreta, solo se guarda y se compara por igualdad (`findUnique`) — el peor caso es una fila más pesada de lo normal, no una vulnerabilidad de ejecución. Es infraestructura compartida (`common/idempotency/`, usada también por `sales`/`returns`/`expenses` cuando existan) — un fix acá afecta a todos los módulos que la consuman, no es algo puntual de `cash-registers`. | agente | 2026-08-24 |
| TD-12 | LOW | `POST /cash-registers/movements/ingreso` y `/retiro` (`OWNER`-only, mueven efectivo) no tienen `@nestjs/throttler` — el único endpoint con rate limiting en todo el backend sigue siendo `/auth/login` (Fase 08 de `auth`). | Fase 09 — Security audit (`cash-registers`) | No bloquea: ya exige una sesión `OWNER` autenticada (no es un vector de fuerza bruta ni de enumeración), y `products`/`stock` — que también mutan dinero/stock siendo `OWNER`-only — tampoco lo tienen, así que no es una inconsistencia nueva de este módulo. Si se decide agregar rate limiting a rutas mutantes de dinero más allá del login, es una decisión transversal a varios módulos. | agente | 2026-08-24 |
| TD-13 | LOW | `payments.referencia` (`sale-payment.dto.ts`, columna `TEXT`) no tiene ningún formato/patrón validado más allá de `@MaxLength(500)` — podría contener un número de tarjeta completo si alguien lo pega ahí a mano, sin ningún aviso ni enmascarado. | Fase 09 — Security audit (`sales`) | No bloquea: la pantalla real (`CobroPage.tsx`) nunca envía este campo hoy — no hay ninguna forma de llegar a él desde el producto real, solo desde un cliente HTTP directo. Corregirlo implicaría inventar una regla de negocio sobre qué hace "válida" una referencia, que ni el blueprint ni la spec del módulo definen. Revisar si en el futuro se agrega un campo de referencia a la UI de cobro. | agente | 2026-08-25 |
| TD-14 | LOW | `POST /sales` (cualquier usuario autenticado, crea ventas) no tiene `@nestjs/throttler` — mismo patrón que TD-12, ahora reconfirmado también en el endpoint de mayor volumen del sistema (una venta por cliente en el mostrador). | Fase 09 — Security audit (`sales`) | No bloquea: mismo motivo que TD-12 — ya exige sesión autenticada, y el abuso real queda acotado por la disponibilidad de stock y el resto de las validaciones de negocio (no hay ganancia obvia para un atacante más allá de un DoS de bajo impacto que además requiere credenciales válidas). Decisión transversal a varios módulos, no específica de `sales`. | agente | 2026-08-25 |
| TD-15 | LOW | `POST /returns` (cualquier usuario autenticado, mueve dinero y stock) no tiene `@nestjs/throttler` — mismo patrón exacto que TD-12/TD-14, ahora reconfirmado también en `returns`. | Fase 09 — Security audit (`returns`) | No bloquea: mismo motivo que TD-12/TD-14 — ya exige sesión autenticada, y el abuso real queda acotado por el resto de las validaciones de negocio (tope por línea, plazo, sesión de caja). Decisión transversal a varios módulos, no específica de `returns` — si se decide agregar rate limiting a rutas mutantes de dinero, conviene resolverlo de una vez para `cash-registers`/`sales`/`returns` juntos. | agente | 2026-08-28 |
| TD-16 | LOW | `POST /expenses`, `POST`/`PATCH /expense-categories`, `PATCH /settings/:clave` (todas mutan dinero o configuración del sistema) no tienen `@nestjs/throttler` — mismo patrón exacto que TD-12/TD-14/TD-15, ahora reconfirmado también en `expenses`/`settings`. | Fase 09 — Security audit (`expenses`/`resultados`) | No bloquea: mismo motivo que TD-12/TD-14/TD-15 — todas exigen sesión autenticada (OWNER-only para `expenses`/`settings`), y el abuso real queda acotado por el resto de las validaciones de negocio. Decisión transversal a varios módulos — si se decide agregar rate limiting a rutas mutantes, conviene resolverlo de una vez para `cash-registers`/`sales`/`returns`/`expenses`/`settings` juntos, no módulo por módulo. | agente | 2026-08-29 |
