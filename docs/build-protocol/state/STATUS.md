# STATUS

Bitácora única de avance del protocolo. Toda fase la lee antes de empezar y
la actualiza al terminar. Si algo no está VERDE acá, no pasó — no importa
lo que diga la conversación.

Resultados posibles: `VERDE`, `BLOQUEADO`, `VERDE CON DEUDA` (pasó, pero
dejó MEDIUM/LOW documentados en `TECH_DEBT.md`).

| Fecha | Fase / Módulo | Resultado | Referencia | Responsable |
|---|---|---|---|---|
| _ejemplo_ 2026-08-16 | Fase 0.0 — Inventario de módulos | VERDE | `state/MODULES.md` | agente |
| 2026-08-20 | Fase 00 — Setup del proyecto | VERDE | commit pendiente (repo local sin commitear todavía — lo hace el usuario) | agente |
| 2026-08-23 | Fase 01 — Modelo de datos | VERDE | `backend/prisma/schema.prisma`, `backend/prisma/migrations/20260823002959_init/`, `backend/prisma/seed.ts`, `backend/test/integration/schema-constraints.integration.spec.ts` — commit pendiente (lo hace el usuario) | agente |
| 2026-08-23 | Fase 02 — Plan de construcción | VERDE | `state/ROADMAP.md` — revisado contra `MVP_SCOPE.md` y `BLUEPRINT.md`; T0.1–T0.5 y T0.8 pasan a VERDE (ya hechos en fases 00/01); se agregan T0.11–T0.14 (helpers es-AR, helpers de Decimal/redondeo, `settings`, interceptor de idempotencia) y T6.9 (pantalla de configuración), que faltaban en la versión original — commit pendiente (lo hace el usuario) | agente |
| 2026-08-23 | Fase 03 — Ambigüedades (Paso 1: compilar y recomendar) | VERDE | `state/AMBIGUITIES.md` — 10 ambigüedades de BLUEPRINT §11 compiladas con pregunta cerrada, recomendación y riesgo. 3 ya RESUELTAS (AMB-1, AMB-7 con caveat, AMB-8), 7 PENDIENTES de respuesta del PO (AMB-2 a AMB-6, AMB-9, AMB-10) — la Etapa 5 completa, T3.4, T4.1, T4.3 y T0.13 (valor real de `umbral_diferencia_caja`) quedan BLOQUEADOS hasta el Paso 2 — commit pendiente (lo hace el usuario) | agente |
| 2026-08-23 | Fase 03 — Ambigüedades (Paso 2: resolver) | VERDE | `state/AMBIGUITIES.md` — las 7 PENDIENTES quedan RESUELTAS: AMB-2 a AMB-5 y AMB-9 aprobadas según recomendación; AMB-6 (costeo por último costo) confirmada explícitamente aparte por ser ⚠️ ALTO RIESGO; AMB-10 (umbral de diferencia de caja) sin recomendación previa, definida por el PO en **$500** fijos. Las 10 ambigüedades de BLUEPRINT §11 quedan cerradas: ningún ticket del `ROADMAP.md` sigue bloqueado por ambigüedad — commit pendiente (lo hace el usuario) | agente |
| 2026-08-23 | Fase 05 — Quality Gate | VERDE | `QUALITY_GATE.md` — commit pendiente (lo hace el usuario) | agente |
| 2026-08-23 | Fase 06 — Spec del módulo `auth` | VERDE | `state/reports/modulo-auth-spec.md` — sin ambigüedades de negocio pendientes; corregido T1.2 (8h→12h, BLUEPRINT §9.6) y ampliado T1.1 en `ROADMAP.md`; hallazgo fuera de alcance reportado (falta ticket de importación CSV en Etapa 2, `DECISIONES_PENDIENTES.md` Bloque C2) — commit pendiente (lo hace el usuario) | agente |
| 2026-08-23 | T1.1 — Usuarios (`auth`) | VERDE | commit `3fc165d` en rama `feat/T1.1-usuarios` — 10 tests unitarios + 8 de integración en verde, lint y build en verde. De paso corrigió un bug real: `ValidationPipe`/`GlobalExceptionFilter` no corrían en tests de integración por estar registrados solo en `main.ts` (movidos a `APP_PIPE`/`APP_FILTER` en `AppModule`) | agente |
| 2026-08-23 | T1.2 — Login + JWT + logout (`auth`) | VERDE | commit `2e9f3b5` en rama `feat/T1.2-login` (ramificada de `feat/T1.1-usuarios`, todavía sin mergear a `main`) — 5 tests unitarios + 6 de integración en verde, lint y build en verde. Sin `GET /auth/me` ni guards: quedan para T1.3 | agente |
| 2026-08-23 | T1.3 — Guards + RolesGuard + `GET /auth/me` (`auth`) | VERDE | commit `fd69e5f` en rama `feat/T1.3-guards` (ramificada de `feat/T1.2-login`, todavía sin mergear a `main`) — 9 tests unitarios + 7 de integración nuevos, 28/28 tests de integración totales en verde, lint y build en verde. `/users` ahora exige `OWNER`; `/health`, login y logout quedan `@Public()` | agente |
| 2026-08-23 | T1.4 — Login + sesión + rutas protegidas (frontend, `auth`) | VERDE | commit `b0024ca` en rama `feat/T1.4-login-frontend` (ramificada de `feat/T1.3-guards`, todavía sin mergear a `main`) — lint y build en verde; probado a mano en navegador real (login incorrecto/correcto, persistencia tras F5, logout, bloqueo de ruta protegida). Sin tests automatizados de frontend (no hay Vitest instalado). **Etapa 1 (`auth`) completa: T1.1–T1.4 en VERDE.** Sigue el cierre de módulo: fases 07→08→09→10→11→12 | agente |
| 2026-08-23 | Módulo `auth` — Fase 07 (cierre de implementación) | VERDE | commit `ef8c3b9` en rama `feat/T1.4-login-frontend` — contrastado contra `state/reports/modulo-auth-spec.md`; sin funcionalidad faltante; 5 huecos de cobertura de tests corregidos (expiración JWT, carrera real de alta duplicada, invalidación de password vieja, logout→me 401, desactivar OWNER con otro activo a nivel integración); comentarios de coherencia en `SafeUser`/`SafeAuthUser`; sin TODOs ni código muerto. 31/31 integración, 24/24 unitarios, lint y build verde. Pendiente: rate limiting de login (recomendado en la spec, no bloquea — queda para fase 08 QA adversarial) | agente |
| 2026-08-22 | Módulo `auth` — Fase 08 (QA adversarial) | VERDE | `state/reports/modulo-auth-qa-2026-08-22.md`, commit pendiente en rama `feat/T1.4-login-frontend` — race condition real confirmada y corregida (última baja de OWNER, `count()` sin lock → `SELECT ... FOR UPDATE`, 29/30 carreras antes del fix, 0/30 después); contraseña sin `@MaxLength` (amplificaba costo de argon2, ruta pública sin sesión) corregida; sin rate limiting en `/auth/login` corregido (`@nestjs/throttler`, 20/min por IP, solo esa ruta); `X-Powered-By` expuesto corregido (middleware); mass-assignment ya bloqueado por el `ValidationPipe` global, ahora con test que lo confirma. Mutation testing (Stryker) omitido a propósito — BLUEPRINT §9.8 lo exige solo para `sales`/`stock`/`cash-registers`/`returns`/`resultados`, no `auth`. 25/25 unitarios, 39/39 integración, lint y build verde. Riesgo aceptado y documentado (no bug): JWT stateless no revoca sesión al instante si se desactiva/degrada a un usuario (BLUEPRINT §9.6, hasta 12h). **No se declara el módulo terminado — eso es la fase 12.** | agente |
| 2026-08-23 | Módulo `auth` — Fase 09 (Security audit) | **BLOQUEADO** | `state/reports/modulo-auth-secaudit-2026-08-23.md` — sin código modificado (regla de fase 09). 1 CRITICAL: sin protección CSRF real — la API acepta bodies `application/x-www-form-urlencoded` en rutas de mutación (Nest no restringe Content-Type), lo que evade CORS por completo vía `<form>` HTML nativo; permite crear un OWNER nuevo controlado por el atacante con solo que la víctima OWNER abra una página maliciosa (sesión real usada sin que el usuario haga nada), y login-CSRF sin sesión previa. 1 HIGH que afecta autenticación: el JWT de sesión queda en texto plano en cada línea de log (`pinoHttp` sin `redact`, header `cookie` completo) — quien tenga acceso a logs de producción puede secuestrar cualquier sesión activa, incluida OWNER, hasta por 12h. Ambos bloquean el Quality Gate por regla explícita. 3 MEDIUM/LOW no bloqueantes (sin `helmet`/headers de seguridad; `JWT_SECRET`/`COOKIE_SECRET` con mínimo de 16 chars, débil para HMAC, y `COOKIE_SECRET` no se usa en ningún lado; fix de `X-Powered-By` de fase 08 no cubre las respuestas OPTIONS de preflight; 3 advisories HIGH de npm audit en la cadena del CLI `prisma`, listado como dependencia de producción en vez de dev). Recorridas las 20 categorías del checklist, sin hallazgos nuevos en el resto. **No se declara el módulo seguro — sigue la fase 10 (remediación) antes de poder reintentar la fase 09.** | agente |
| 2026-08-23 | Módulo `auth` — Fase 10 (Security remediation) | VERDE | commit `047ea0a` en rama `feat/T1.4-login-frontend`, `state/reports/modulo-auth-secremediation-2026-08-23.md` — corregidos los 6 hallazgos de la fase 09: CSRF (middleware `json-only` global, rechaza con 415 cualquier `Content-Type` que no sea `application/json` en rutas de mutación); JWT en logs (`redact` en `pinoHttp` para `cookie` del request y `Set-Cookie` de la respuesta); sin headers de seguridad (`helmet()` global); `JWT_SECRET` a `.min(32)` y `COOKIE_SECRET` eliminado del schema (no se usaba); `X-Powered-By` en preflight OPTIONS — el fix de fase 08 y hasta `helmet()` como middleware resultaron insuficientes, se confirmó que Express lo reagrega al finalizar la respuesta sin importar el middleware; el fix real es `app.disable('x-powered-by')` vía `HttpAdapterHost` en `onModuleInit()`; `prisma` movido a `devDependencies` (con nota honesta: `npm audit --omit=dev` lo sigue marcando por un `peerDependency` de `@prisma/client`, no corregible desde este `package.json` — riesgo sigue LOW, no se ejecuta a partir de un request real). 29/29 unitarios (+4 nuevos), 43/43 integración (+4 nuevos), lint y build verde. Sin funcionalidad nueva, sin tocar nada fuera de los hallazgos reportados. Pendiente de acción manual del usuario (el agente no puede tocar `.env`/`.env.example`): actualizar `JWT_SECRET` local a ≥32 caracteres. **No se declara el módulo seguro — eso lo confirma la fase 11 (security re-audit).** | agente |
| 2026-08-23 | Módulo `auth` — Fase 11 (Security re-audit) | VERDE | `state/reports/modulo-auth-secreaudit-2026-08-23.md` — sin archivos modificados (regla de fase 11), solo verificación independiente. Los 6 hallazgos de la fase 09 confirmados corregidos con evidencia empírica, no solo lectura de código: CSRF (415 real contra `/auth/login` y `/users` con body urlencoded, login legítimo con JSON sigue funcionando); JWT en logs (43 tests de integración con logins reales, 0 apariciones de un JWT sin censurar, 102 `[REDACTED]`); headers de `helmet()` presentes; `JWT_SECRET` — la más fuerte de las verificaciones: el usuario regeneró el secreto real de `backend/.env` (32 bytes) y sacó `COOKIE_SECRET`, y el server + 43/43 tests de integración corrieron **sin ningún override de entorno** (a diferencia de fases 09/10, que necesitaron forzar un `JWT_SECRET` temporal por shell); `X-Powered-By` ausente del preflight OPTIONS real contra el server con CORS habilitado; `prisma` en `devDependencies` (limitación de `npm audit` por `peerDependency` de `@prisma/client` reconfirmada, no corregible, sigue LOW). Sin vulnerabilidades remanentes ni hallazgos nuevos — se revisaron específicamente los casos límite del middleware `json-only` (sin Content-Type, mayúsculas, charset) sin encontrar ninguna reapertura del CSRF. 29/29 unitarios, 43/43 integración, lint y build verde. **No bloqueado — pasa a la fase 12 (production readiness).** Nota fuera de alcance: `backend/.env.example` quedó con un cambio sin commitear (el usuario sacó `COOKIE_SECRET=` a mano) — no tocado en esta fase, queda a criterio del usuario. | agente |
| 2026-08-23 | Módulo `auth` — Fase 12 (Production readiness) | VERDE — **PRODUCTION READY: YES** | `state/reports/modulo-auth-prodready-2026-08-23.md` — sin código modificado (regla de fase 12). Sin CRITICAL ni HIGH. Security/Reliability/Performance/Code quality/Observability/Deployment revisados contra el checklist de la fase; hallazgos nuevos, todos MEDIUM/LOW y no bloqueantes, registrados en `state/TECH_DEBT.md`: TD-3 (MEDIUM, sin timeout/`AbortController` en `http-client.ts` del frontend — un corte de red deja la UI colgada sin feedback), TD-4 (MEDIUM, `argon2` con parámetros por defecto ~64MiB/operación, riesgo de RAM bajo ráfaga concurrente en el hosting gratuito objetivo — BLUEPRINT §9.10), TD-5 (LOW, `GET /users` sin paginación, aceptable a escala MVP), TD-6 (LOW, warning cosmético de ESLint en `AuthContext.tsx`), TD-7 (LOW, config de `prisma` en `package.json` deprecada, sin urgencia por el pin intencional debajo de v7). Se re-señaló TD-2 (fase 00, `coverageThreshold` de Jest) como vencido — su condición de deferral ("cuando exista el primer `*.service.ts`") ya se cumplió, falta una decisión. 29/29 unitarios, 43/43 integración, lint y build (backend y frontend) verde; cobertura real de servicios >85%; migraciones al día; CORS/cookies/rate-limit reconfirmados correctos. **Módulo `auth` aprobado — pasa a la fase 13 (Integration Audit).** | agente |
| 2026-08-23 | Módulo `products`/`variants` — Fase 06 (Spec del módulo) | **BLOQUEADO** | `state/reports/modulo-products-variants-spec.md` — spec completa (11 secciones) sobre BLUEPRINT §3.2/§3.3/§5.2/invariante 1/§7/§9.3/§9.4/§9.8/§12.2/§12.4, `MVP_SCOPE.md` (riesgo ALTO) y `DECISIONES_PENDIENTES.md` (A3-A5, C2). 2 ambigüedades nuevas agregadas a `state/AMBIGUITIES.md`: AMB-11 (⚠️ ALTO — ¿`SELLER` puede editar `precioVenta` manual, cargar costo en la grilla, o hacer ingreso de mercadería? recomendación: `OWNER`-only las tres) y AMB-12 (MEDIO — formato de columnas del CSV de importación, depende de B4 pendiente con la clienta; recomendación: plantilla propia, no bloquea). **Hallazgo técnico bloqueante, no de negocio:** T2.3/T2.5/T2.9/T2.10/T2.11/T2.12 dependían de T0.11 (helpers es-AR) y T0.12 (helpers de `Decimal`/redondeo, CLAUDE.md regla 5) sin que `ROADMAP.md` lo reflejara — ambos siguen `PENDIENTE`; ajustado el roadmap en consecuencia (T2.1/T2.2 no dependen de ninguno y pueden arrancar). T0.14 (interceptor de idempotencia, también `PENDIENTE`) recomendado como dependencia nueva de T2.5 (ingreso de mercadería), mismo motivo que AD-10. Agregado **T2.13** (nuevo, importación CSV — `DECISIONES_PENDIENTES.md` C2 ya lo exigía, faltaba en el roadmap). Diseño técnico completo en la spec: incremento atómico de `stock_actual` para `ENTRADA` (sin lock — Postgres serializa el `UPDATE`), `SELECT...FOR UPDATE` solo para `AJUSTE` con `delta` negativo (mismo patrón que la fase 08 de `auth`); Stryker acotado a `stock.service.ts` únicamente, tal como lista BLUEPRINT §9.8 literal. **Módulo bloqueado hasta resolver AMB-11/AMB-12 y ejecutar T0.12 antes de T2.3 (T0.11 antes de T2.12); T2.1 y T2.2 pueden arrancar ya.** | agente |
| 2026-08-23 | T2.1 — ABM de marcas, categorías, talles y colores (`products`) | VERDE | commit `80d1257` en rama `feat/T2.1-catalog-listas` (ramificada de `feat/T1.4-login-frontend`, todavía sin mergear a `main`) — 4 recursos casi idénticos (`brands`, `categories`, `colors` con `nombre` único + `activo`; `sizes` además con `orden`, BLUEPRINT §3.2), servicios/controllers explícitos sin abstracción genérica compartida (mismo estilo que `auth`). Sin `@Roles()`: gestionar catálogo no está en la lista de exclusiones de SELLER de BLUEPRINT §5.1 — alcanza con `AuthGuard` global. `sizes` ordena por `orden` (S, M, L, XL), no alfabético. Baja lógica vía `activo:false`. 24 tests unitarios + 12 de integración nuevos, 53/53 y 55/55 en verde, lint y build verde. No tocó nada de T2.2 en adelante — sigue bloqueado el resto de la etapa por AMB-11/AMB-12 y T0.11/T0.12 (ver fase 06). | agente |
| 2026-08-23 | T2.2 — ABM de productos (`products`) | VERDE | commit `c3faf73` en rama `feat/T2.2-products-abm` (ramificada de `feat/T2.1-catalog-listas`, todavía sin mergear a `main`) — `GET /products` paginado en el servidor (BLUEPRINT §12.4) con filtros `brandId`/`categoryId`/`activo`; `GET /products/:id` incluye `variants` (vacío hasta T2.3, es solo la relación de Prisma); POST/PATCH sin `@Roles()`, mismo motivo que T2.1; FK inválida (`brandId`/`categoryId` inexistente) traducida de P2003 a 400 en vez de 500. Corrección de nomenclatura en la spec de fase 06: `PaginatedResult` usa `itemCount`, no `total` — el linter local `no-number-money` trata cualquier "total" (incluso dentro de "totalItems") como sospechoso de ser plata. 10 tests unitarios + 11 de integración nuevos, 63/63 y 66/66 en verde, lint y build verde. Sigue bloqueado el resto de la etapa por AMB-11/AMB-12 y T0.11/T0.12. | agente |
| 2026-08-23 | T0.12 — Helpers de `Decimal` y redondeo comercial (Etapa 0, `common/money`) | VERDE | commit `6a6dc23` en rama `feat/T0.12-money-helpers` (ramificada de `feat/T2.2-products-abm`, todavía sin mergear a `main`) — `roundCurrency` (redondeo comercial explícito, medio hacia arriba, nunca depende de la config global de decimal.js), `lineSubtotal`/`applyPercentage` (reglas 1 y 3 de BLUEPRINT §9.3), `prorate` (regla 5 / AD-18: reparte un total entre líneas por peso, ajusta el residuo de redondeo en la línea de mayor neto con desempate por menor índice). Primitivas componibles — no arma el cálculo completo de una venta, eso es de `sales`. Incluye los dos tests obligatorios de §9.3 (15% de descuento sobre $2999; prorrateo de 3 líneas con residuo), verificados numéricamente antes de escribirlos, no a mano. 10 tests nuevos, 73/73 unitarios y 66/66 integración en verde, lint y build verde. **Desbloquea T2.3, T2.9, T2.10, T2.11** (ver fase 06 de `products`/`variants`) — T2.3 sigue esperando además la resolución de AMB-11. | agente |
| 2026-08-23 | AMB-11 — resolución (`products`/`variants`) | RESUELTA | `state/AMBIGUITIES.md`, `state/reports/modulo-products-variants-spec.md` — el PO aprobó la recomendación: `OWNER`-only para editar `precioVenta` manualmente, completar el costo en la grilla de alta, y el ingreso de mercadería. **T2.3 desbloqueado** (junto con T2.5 y T2.11, ya cubiertos por T0.12 en VERDE). | agente |
| 2026-08-23 | T2.3 — ABM de variantes (`products`) | VERDE | commit `db6c89f` en rama `feat/T2.3-variants-abm` (ramificada de `feat/T2.2-products-abm`, todavía sin mergear a `main`) — `POST /products/:id/variants` OWNER-only (AMB-11), escribe 2 filas de `price_history` con origen `ALTA` (precio y costo) en la misma transacción que la creación; `precioVenta`/`costoActual` viajan como string en el body (`@IsDecimal`), nunca `number` — un JSON number ya pasó por el redondeo de punto flotante de `JSON.parse` antes de llegar al DTO. SKU único, barcode único y `UNIQUE NULLS NOT DISTINCT` de (product,size,color) traducidos a 409 con mensaje específico según la constraint violada; FK inválida (sizeId/colorId) a 400; precio/costo ≤ 0 a 400. `PATCH /variants/:id` (sku/barcode/activo, cualquier rol) separado de `PATCH /variants/:id/price` (OWNER-only, AMB-11) porque cambiar el precio también escribe `price_history` (origen `MANUAL`) en transacción — no puede ser un campo más de un PATCH genérico. RN-3 (costo solo `OWNER`) resuelto con mapeo condicional según rol, no `omit` de Prisma; **corregido también en `ProductsService.findOne` (T2.2)**, que hasta ahora devolvía las variantes de `GET /products/:id` sin filtrar — no era explotable sin variantes con costo real, pero violaba RN-3 apenas existieran (ahora). **Hallazgo de tooling, no corregido (fuera de alcance):** `nest build` excluye `**/*spec.ts` del type-check (`tsconfig.build.json`) y `ts-jest` no falló por un error de tipos real (argumento faltante) en un test existente — lo atrapó recién `tsc --noEmit` corrido a mano; vale la pena revisarlo en otra fase. 31 tests unitarios + 20 de integración nuevos, 92/92 y 86/86 en verde, `tsc --noEmit` limpio, lint y build verde. | agente |
| 2026-08-23 | Fase 04a — T2.4 (`stock.service`), tests primero | ROJO (esperado) | commit `12eeff3` en rama `feat/T2.4-stock-service` (ramificada de `feat/T2.3-variants-abm`, todavía sin mergear a `main` — renombrada de `feat/T2.4-stock-service-tests` al agregar la implementación real, ver la fila siguiente), sesión aislada — no se leyó ninguna implementación de `src/modules/`, solo ROADMAP.md (T2.4), BLUEPRINT.md (AD-4, §3.3, §5.2, invariantes 1/5/6, §7, §9.3, §9.4) y `state/reports/modulo-products-variants-spec.md` (RN-4, RN-5, RN-8, RN-10, secciones 3-9), más `common/money`, `schema.prisma`, `prisma.service.ts` y los `test/integration/*.spec.ts` ya existentes (solo por el patrón mecánico). Tests escritos: **18 casos** — 9 unitarios (`stock.service.spec.ts`, Prisma mockeado: camino feliz de `registrarEntrada`/`registrarAjuste`, RN-4/AD-6 costeo por último costo, motivo obligatorio con delta positivo y negativo, ajuste que dejaría stock negativo con el mensaje de negocio de §7, caso límite en exactamente 0, y que ninguno de los dos métodos abre su propia transacción — contrato sección 4.2) + 9 de integración (`stock.integration.spec.ts`, Postgres real, todo el stock de prueba sembrado a través del propio servicio, nunca escribiendo `stock_actual` a mano: mismos casos que la unitaria pero verificando el invariante 1 (`stock_actual == SUM(delta)`) contra la base real después de cada operación, más el caso de concurrencia explícito — dos ajustes negativos simultáneos, cada uno válido por separado pero que juntos dejarían el stock en -1, repetido en 10 iteraciones con datos frescos, mismo patrón que la fase 08 de `auth` para el último OWNER activo). Incluye el stub mínimo de `StockService` (única excepción de esta fase: cada método solo hace `throw new Error('T2.4 todavía no implementado')`, sin ninguna validación, ni siquiera la más obvia). **18/18 en rojo confirmados**, todos por esa excepción real del stub (no por error de compilación/importación — `tsc --noEmit` limpio); el resto de la suite (92 unitarios + 86 integración de T2.1-T2.3) sigue en verde sin tocar. Hallazgo de paso, no bloqueante: el CHECK de base para "AJUSTE siempre con motivo" que la spec del módulo (sección 3, invariante 6) da por agregado en la fase 01 no está en la migración `20260823002959_init` — no bloquea esta fase (los tests de motivo obligatorio no dependen de esa constraint, verifican el rechazo desde el propio servicio), pero la implementación real de T2.4 y/o una migración futura deberían cerrar esa brecha. | agente |
| 2026-08-23 | T2.4 — `stock.service` (`registrarEntrada`/`registrarAjuste`) | VERDE | commit `c327ca6` en rama `feat/T2.4-stock-service` — implementado contra los 18 tests de la fase 04a **sin modificar ninguna aserción existente**. `registrarEntrada`: incremento atómico de `stock_actual` (sin lock, Postgres serializa el `UPDATE`), actualiza `costo_actual` (AD-6). `registrarAjuste`: motivo siempre obligatorio; `delta >= 0` incremento atómico igual que entrada; `delta < 0` primero `SELECT ... FOR UPDATE` (§9.4), lee `stock_actual` ya con el lock tomado vía Prisma normal, valida que el resultado no quede negativo (invariante 5, sin la excepción de `permitir_venta_sin_stock` — RN-5) y recién ahí escribe. **Hallazgo real de la fase 04a:** ningún test cubría RN-10/AD-16 (todo cambio de `costo_actual`, incluido por ingreso de mercadería, debe quedar en `price_history`) — implementado igual porque lo exige la spec, con un test unitario y uno de integración nuevos agregados para cubrirlo (cobertura nueva, no una aserción ablandada). **Hallazgo de tooling:** el mock de `tx` en `stock.service.spec.ts` intersecaba `Prisma.TransactionClient` con las propiedades `jest.Mock`, generando 19 falsos positivos de lint (`unbound-method`/`no-unsafe-assignment`) que la fase 04a no detectó porque solo corrió `tsc`/jest/build, nunca `npm run lint` — separado en un tipo `MockTx` propio + cast puntual al llamar al servicio real (mismo patrón que `variants.service.spec.ts`). **Fix de un test flaky fuera del módulo** (expuesto por este ticket, no causado por él): `users.integration.spec.ts` comparaba `prisma.user.count()` global antes/después de una request, que ahora puede correr en paralelo con el `beforeAll`/`afterAll` de `stock.integration.spec.ts` contra la misma base — cambiado a verificar puntualmente que el email de prueba no exista; confirmado estable en 3 corridas seguidas de la suite completa. 31 tests en verde (10 unitarios + 21 de integración, incluida la carrera real de dos ajustes negativos simultáneos, 10 iteraciones), `tsc --noEmit` limpio, lint limpio, build verde. | agente |
| 2026-08-23 | T2.6 — `POST /stock/ajustes` (`stock`) | VERDE | commit `b10fc07` en rama `feat/T2.6-ajuste-stock` (ramificada de `feat/T2.4-stock-service`, todavía sin mergear a `main`) — controller delgado sobre `stock.service.registrarAjuste` (T2.4): abre la transacción (el servicio nunca abre la suya propia), delega la lógica de negocio ya probada, devuelve la variante actualizada. Todo `OWNER`-only (RN-5, literal — a diferencia de `products`/`variants` no hay ninguna acción abierta a `SELLER` acá). Verifica que la variante exista **antes** de llamar al servicio: sin ese chequeo, un `variantId` inválido terminaba en un error crudo de Prisma (P2003 o P2025 según el signo del delta) sin traducir — 500 en vez del 404 que corresponde a la capa HTTP; no se tocó `stock.service.ts` para esto, es responsabilidad de la capa HTTP validar antes de llamar a un servicio interno que confía en recibir un id válido. 8 tests de integración nuevos (401, 403 `SELLER`, 400 motivo/DTO/mass-assignment, 404 variante inexistente, 409 stock negativo, 201 delta positivo y negativo con verificación de `stock_movements`). 104/104 integración (3 corridas seguidas sin flakes), 102/102 unitarios, `tsc --noEmit` limpio, lint y build verde. | agente |
| 2026-08-23 | T2.7 — `GET /variants/search` (`products`) | VERDE | commit `59c5951` en rama `feat/T2.7-buscador-unificado` (ramificada de `feat/T2.6-ajuste-stock`, todavía sin mergear a `main`) — buscador unificado (RN-11): nombre de producto, SKU o código de barras en un solo campo, `OR` con `contains` insensible a mayúsculas; sin `q`, lista todo lo activo paginado. Solo variantes activas de productos activos. Paginado en el servidor (RN-12), abierto a cualquier rol (no está en la lista de exclusiones de `SELLER` de §5.1); RN-3 sigue aplicando — `costoActual` se oculta si quien busca no es `OWNER`. Ruta `variants/search` registrada antes de `variants/:id` a propósito (Nest/Express matchea por orden de declaración, `:id` es greedy). **Bug real encontrado corriendo la suite completa varias veces seguidas** (no se reproducía con un archivo solo): `stock-ajustes.integration.spec.ts` (T2.6) usaba `prisma.user.findFirstOrThrow()` sin filtro para el `userId` del movimiento de stock — bajo ejecución en paralelo con otros archivos podía agarrar el usuario de OTRO archivo, dejando un `stock_movements` huérfano que rompía el `afterAll` de ese otro archivo (P2003 al borrar sus propios usuarios); corregido para usar el `ownerId` propio del archivo, confirmado estable en 5 corridas seguidas de la suite completa después del fix. 14 tests nuevos (4 unitarios + 10 de integración, incluido el edge case de la spec "código de barras que no existe: lista vacía, no 404"). 106/106 unitarios, 114/114 integración, `tsc --noEmit` limpio, lint y build verde. | agente |
| 2026-08-23 | T2.5 — bloqueada, se reabre T0.14 | **BLOQUEADO** | Al ejecutar T2.5 (`docs/build-protocol/04-ticket-execution.md`), `ROADMAP.md` la listaba dependiendo de T0.14 (idempotencia), todavía `PENDIENTE` — se detuvo la ejecución y se informó el bloqueo en vez de improvisar, tal como exige la fase. El PO eligió construir T0.14 primero. | agente |
| 2026-08-23 | T0.14 — Interceptor de idempotencia (Etapa 0, `common/idempotency`) | VERDE | commit `00859c9` en rama `feat/T0.14-idempotencia` (ramificada de `feat/T2.7-buscador-unificado`, todavía sin mergear a `main`) — **alcance literal de BLUEPRINT §9.7**: `sales`/`returns`/`cash_movements`/`expenses`, las cuatro tablas que ya tienen `idempotencyKey` en el schema; confirmado antes de construir (`grep` sobre `schema.prisma`) que `stock_movements` no tiene esa columna y que el blueprint nunca la lista ahí — extender la protección a T2.5 hubiese exigido una migración que el blueprint no pide. **El PO decidió explícitamente no extenderlo** (ver `ROADMAP.md`, nota bajo la Etapa 2) — T2.5 queda desbloqueada sin esa dependencia, aceptando el riesgo de doble click en el ingreso de mercadería. `IdempotencyInterceptor` (exige el header `Idempotency-Key`, 400 si falta/vacío) + `@IdempotencyKey()` (lo expone como parámetro) + `withIdempotency(write, findExisting)` (si `write()` choca contra la constraint única de `idempotency_key`, P2002, devuelve la fila existente en vez de fallar — "no es un error, se devuelve la operación original con 200", §9.7 textual; un P2002 en cualquier otra columna se propaga tal cual). Probado contra Postgres real usando `expenses` directo por Prisma (ninguno de sus 4 consumidores tiene ticket propio todavía) — incluye el caso real de doble click: dos writes simultáneos con la misma clave, uno gana, el otro recibe el mismo resultado, nunca dos filas. 13 tests nuevos (9 unitarios + 4 de integración). 115/115 unitarios, 118/118 integración (3 corridas sin flakes), `tsc --noEmit` limpio, lint y build verde. **T2.5 desbloqueada** (ver ajuste de dependencia en `ROADMAP.md`). | agente |
| 2026-08-23 | T2.5 — `POST /stock/entradas` (`stock`) | VERDE | commit `69ab2b4` en rama `feat/T2.5-ingreso-mercaderia` (ramificada de `feat/T0.14-idempotencia`, todavía sin mergear a `main`) — controller delgado sobre `stock.service.registrarEntrada` (T2.4, VERDE), mismo patrón que T2.6: la transacción se abre acá (el servicio nunca abre la suya), y se agregan en la capa HTTP las dos validaciones que el servicio no hace porque confía en quien lo llama — variante inexistente → 404 (sin esto, P2003/P2025 crudo de Prisma → 500) y `costoUnitario <= 0` → 400, antes de abrir la transacción. Todo `OWNER`-only (AMB-11, RESUELTA). DTO `costoUnitario` viaja como STRING con `@IsDecimal` (BLUEPRINT §9.3). **Sin `Idempotency-Key`**: decisión explícita del PO (ver fila T0.14 arriba y `ROADMAP.md`) — extender T0.14 a `stock_movements` hubiese exigido una migración fuera de lo que pide el blueprint; riesgo de doble click aceptado a propósito, documentado en `state/reports/modulo-products-variants-spec.md`. 9 tests de integración nuevos (401, 403 `SELLER`, 400 cantidad ≤ 0, 400 costoUnitario ≤ 0, 400 más de 2 decimales, 400 mass-assignment, 404 variante inexistente, y dos casos de camino feliz: una entrada individual con verificación completa de `stock_movements`/`price_history` origen `INGRESO_MERCADERIA`, y dos entradas sucesivas confirmando AD-6 — el costo queda en el último valor ingresado). 115/115 unitarios, 127/127 integración (3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint y build verde. | agente |
| 2026-08-23 | T2.8 — Chequeo de reconciliación del invariante 1 (`stock`) | VERDE | commit `a182cba` en rama `feat/T2.8-reconciliacion-invariante-1` (ramificada de `feat/T2.5-ingreso-mercaderia`, todavía sin mergear a `main`) — BLUEPRINT §6 exige, para las tres primeras invariantes, "un chequeo de reconciliación ejecutable", no solo un test aislado por variante. `stock.service.reconciliar()`: agrega TODAS las variantes (activas e inactivas — RN-7: una variante dada de baja con stock > 0 sigue contando) contra la suma de sus propios `stock_movements`, devuelve solo las que no cuadran. **Detalle de diseño no trivial encontrado en este ticket:** las dos lecturas (`variant.findMany` + `stockMovement.groupBy`) van dentro de la misma transacción Prisma en `REPEATABLE READ`, no como dos queries sueltas — sin eso, una escritura real entre una consulta y la otra (un ingreso de mercadería concurrente) podía dejar a las dos consultas viendo un instante distinto del sistema y reportar un desajuste que en realidad nunca existió; un falso positivo inaceptable para un chequeo cuyo propósito es detectar corrupción real. Es de solo lectura, no compite con el contrato de "`stock.service` nunca abre su propia transacción" (ese contrato es para quien escribe). 5 tests unitarios nuevos (Prisma mockeado) + 2 de integración contra Postgres real: un conjunto de variantes con entradas y ajustes válidos, incluida una dada de baja con stock > 0, reconcilia sin diferencias (prueba RN-7 explícitamente); y una variante con `stock_actual` alterado a propósito por fuera de `stock.service` aparece en la lista con los valores exactos — prueba que el chequeo detecta un desajuste real, no solo confirma el camino feliz. 120/120 unitarios, 129/129 integración (3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint y build verde. | agente |
| 2026-08-23 | T2.9 — `GET /variants/:id/price-history` (`products`) | VERDE | commit `fe94c6d` en rama `feat/T2.9-price-history` (ramificada de `feat/T2.8-reconciliacion-invariante-1`, todavía sin mergear a `main`) — AD-16 ya exigía que todo cambio de `precio_venta`/`costo_actual` quedara registrado (valor anterior, nuevo, usuario, fecha); eso ya lo escribían `create()` (ALTA, T2.2), `updatePrice()` (MANUAL, T2.3) y `stock.service.registrarEntrada()` (INGRESO_MERCADERIA, T2.5). T2.9 era exponer ese historial: `variants.service.getPriceHistory()`, `OWNER`-only a nivel de ruta (spec del módulo §4.1: "incluye costo — RN-3", sin necesidad de filtrar filas como `hideOwnerOnlyFields`), paginado (mismo patrón `page`/`pageSize` que el resto del módulo, sin abstracción compartida), orden más reciente primero. 404 si la variante no existe. 3 tests unitarios + 4 de integración nuevos (403 `SELLER`, 200 `OWNER` con las 4 filas esperadas en el orden correcto, paginado, 404). **Bug real encontrado corriendo la suite:** el `afterAll` de `variants.integration.spec.ts` no borraba `stock_movements` — no hacía falta hasta ahora porque ningún test de ese archivo tocaba stock; el nuevo caso ejercita `POST /stock/entradas` para generar la fila `INGRESO_MERCADERIA`, y el borrado de la variante fallaba por la FK `stock_movements_variant_id_fkey`. Corregido agregando ese `deleteMany`, y limpiados a mano los datos huérfanos que la corrida fallida había dejado en la base de test. 123/123 unitarios, 133/133 integración (3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint y build verde. | agente |
| 2026-08-23 | T2.10 — Actualización masiva de precios por porcentaje (`products`) | VERDE | commit `01e67b2` en rama `feat/T2.10-actualizacion-masiva-precios` (ramificada de `feat/T2.9-price-history`, todavía sin mergear a `main`) — RN-9/A5: `POST /prices/bulk-update/preview` (no escribe nada) y `POST /prices/bulk-update/apply` (escribe `price_history` con `origen = MASIVO` por variante afectada), `OWNER`-only, filtro por `brandId`/`categoryId`/`variantIds` combinables con AND, sin filtro = todo el catálogo activo (mismo criterio que `GET /variants/search`). Cálculo vía `applyPercentage` (T0.12, §9.3 regla 3); porcentaje puede ser negativo (rebaja) — el blueprint no restringe el signo. **Decisión consultada al PO antes de implementar:** la spec pedía `Idempotency-Key` en `apply`, pero `price_history` no tiene esa columna (igual que `stock_movements` en T2.5) y acá el motivo es más fuerte — una aplicación escribe N filas, no una, y el mecanismo de T0.14 está pensado para deduplicar una fila por clave. El PO eligió no protegerlo, mismo criterio que T2.5 (ver `modulo-products-variants-spec.md` §4.1, actualizada). Todo o nada: si cualquier variante del lote quedaría con `precioVenta <= 0`, se rechaza la operación completa (400, SKU identificado) antes de escribir nada. `apply()` lee y escribe dentro de la misma transacción (el cálculo usa el precio consistente con lo que se escribe); sin `FOR UPDATE` — BLUEPRINT §9.4 exige ese lock específicamente para stock, acá no hay invariante de stock en juego (mismo criterio que `updatePrice()`, T2.3). 9 tests unitarios + 10 de integración nuevos. 132/132 unitarios, 143/143 integración (3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint y build verde. | agente |
| 2026-08-23 | T2.11 — Alta de variantes por grilla (`products`) | VERDE | commit `ba22000` en rama `feat/T2.11-alta-por-grilla` (ramificada de `feat/T2.10-actualizacion-masiva-precios`, todavía sin mergear a `main`) — RN-8/§12.2: `POST /products/:id/variants/grid` crea N variantes (talle×color) de una sola vez a partir de una grilla ya completada. **Contradicción real encontrada y resuelta en la propia spec del módulo, sin bloquear:** la fila de matriz de permisos (§8) sugería `SELLER` con "columna de costo deshabilitada", pero la fila del contrato de API (§4.1) y la nota de resolución de AMB-11 (§10) decían `OWNER`-only — la lectura "SELLER sin costo" es irreconciliable con el modelo (`costoActual` NOT NULL, sin mecanismo para fijarlo después de crear salvo un ingreso de mercadería que duplicaría stock), y coincide con el precedente ya VERDE de `create()` (T2.2/T2.3). Implementado `OWNER`-only entero; spec corregida en ambos lugares (de paso, corregida también la fila de `POST /products/:id/variants` suelta, que decía "cualquiera autenticado" pese a que T2.3 ya la implementó `OWNER`-only). `createGrid()` agregado a `VariantsService` (reusa `translateWriteError`/`assertPositive`), inyecta `StockService` — `ProductsModule` ahora importa `StockModule`. Todo en una única transacción (sin lock de filas: cada variante es nueva, sin contención posible). Por fila: `variant.create()` + `price_history` ALTA para `PRECIO_VENTA` + `stock.service.registrarEntrada()` para el stock inicial (RN-8 textual: "nunca se escribe stock_actual directo"). SKU autogenerado cuando falta (patrón `P{productId}-{TALLE}-{COLOR}`, BLUEPRINT §12.2 exige "un patrón, editables" sin fijar cuál). 9 tests unitarios + 6 de integración nuevos. 141/141 unitarios, 149/149 integración (3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint y build verde. | agente |
| 2026-08-23 | T0.11 — Helpers de formato es-AR (Etapa 0, `frontend/src/lib/format.ts`) | VERDE | commit `21f3e5a` en rama `feat/T0.11-formato-es-ar` (ramificada de `feat/T2.11-alta-por-grilla`, todavía sin mergear a `main`) — `formatCurrency` ("$ 12.500,50", prefijo armado a mano en vez de `style: 'currency'` de Intl para evitar variaciones de espaciado entre entornos), `formatDate` ("dd/mm/aaaa", convertido a `America/Argentina/Buenos_Aires` aunque el backend mande UTC — no confundir con T0.7, que es agrupación de negocio, todavía `PENDIENTE`), `formatDateTime` ("dd/mm/aaaa HH:mm", 24 horas) y `formatNumber` (cantidades no monetarias, separador de miles es-AR). **Hallazgo real de la fase:** el frontend no tenía ningún framework de testing (sin Jest/Vitest, sin script `test`, CI del job de frontend corría lint+build pero nunca tests) — gap que ninguna fase anterior había cubierto y que CLAUDE.md regla 8 no permite ignorar. Agregado como parte de este ticket en vez de bloquear: Vitest (emparejamiento estándar para Vite, cero-config vía el mismo `vite.config.ts`), wireado a `.github/workflows/ci.yml` igual que el job de backend. 14 tests, todos verificados contra el `Intl` real del entorno (no a mano). `tsc -b`, lint y build del frontend limpios. | agente |
| 2026-08-23 | T2.12 — Pantallas de catálogo, stock e ingreso de mercadería (`frontend`) | VERDE | commit `570e6d3` en rama `feat/T2.12-pantallas-catalogo-stock` (ramificada de `feat/T0.11-formato-es-ar`, todavía sin mergear a `main`) — primer ticket de UI real más allá de `auth`. `AppLayout.tsx` (nav + usuario + rol + cerrar sesión) reemplaza el `HomePage` placeholder de T0.5 (eliminado). `RequireOwner.tsx`: guard de UI para rutas que siempre involucran costo (grilla, alta suelta) — defensa en profundidad además del 403 del backend, para BLUEPRINT §12.6 ("el vendedor nunca ve, en ninguna pantalla, costos ni resultados"). Pantallas: `CatalogPage` (buscador unificado, T2.7), `NewProductPage`/`ProductForm` (compartido alta/edición), `ProductDetailPage` (variantes + acciones), `VariantGridPage` (T2.11: elegir talles/colores, generar grilla, completar por fila o "aplicar a todas"), `NewVariantPage` (variante suelta, sin talle/color). **Dos bugs reales encontrados y corregidos verificando en el navegador (no solo con tests):** (1) `NumberInput.onChange` de Mantine devuelve `string`, no `number`, cuando el input usa `decimalScale`/`fixedDecimalScale`/`prefix` — los checks `typeof value === 'number'` dejaban el estado vacío en silencio en los 6 campos de dinero/cantidad de la pantalla (confirmado con un `console.log` temporal contra el `Intl` real); corregido con un helper compartido (`lib/number-input.ts`, con tests) más `decimalSeparator`/`thousandSeparator` es-AR (antes mostraba "$ 0.00" en vez de "$ 0,00"). (2) El patrón `Menu`/`Menu.Dropdown` de Mantine no reflejaba contenido en el DOM en este entorno — reemplazado por botones inline, más simple y sin dependencia de portales. Verificado a mano contra el backend real: login OWNER/SELLER, alta de producto, grilla completa (generar + aplicar a todas + editar filas + submit, SKU autogenerado), editar variante/precio, ingreso de mercadería, ajuste de stock — los cinco flujos con round-trip real a Postgres confirmado vía network requests; ocultamiento de costo/acciones para SELLER y bloqueo de `RequireOwner` en `/grilla` confirmados. 26 tests (lógica pura — helpers de formato, grilla, `number-input`; las pantallas se verificaron a mano en el navegador, sin infraestructura de testing de componentes React todavía, BLUEPRINT §9.8 no la exige como categoría, el E2E de estos flujos queda para la Fase 14). `tsc -b`, lint y build del frontend limpios. | agente |
| 2026-08-23 | AMB-12 — resolución (`products`/`variants`, carga inicial) | RESUELTA | `state/AMBIGUITIES.md` — el PO aprobó la recomendación de la fase 06: plantilla propia de columnas para la importación CSV, sin esperar la respuesta de B4 (`DECISIONES_PENDIENTES.md`). Si B4 revela un formato existente de la clienta más adelante, el ajuste queda acotado al mapeo de columnas del importador, no reabre el ticket. **T2.13 desbloqueado.** | agente |
| 2026-08-23 | T2.13 — Importación de catálogo por CSV (`products`) | VERDE | commit `475bc8f` en rama `feat/T2.13-importacion-csv` (ramificada de `feat/T2.12-pantallas-catalogo-stock`, todavía sin mergear a `main`) — **Etapa 2 completa (T2.1–T2.13, todos VERDE).** `POST /products/import`, `OWNER`-only, body `{ csv: string }` (texto plano, no multipart — sin agregar `multer` sin infraestructura de upload existente). Plantilla propia (AMB-12): `nombre,descripcion,marca,categoria,talle,color,sku,barcode,precio,costo,stock` (obligatorias: `nombre`, `precio`, `costo`, `stock`). Cada fila en su propia transacción — una fila inválida se reporta como error sin abortar el resto (`DECISIONES_PENDIENTES.md` C2, "validación con reporte de errores línea por línea"), a diferencia del todo-o-nada de T2.10/T2.11 porque acá cada fila es un registro lógicamente independiente. Producto/marca/categoría/talle/color se buscan por nombre (case-insensitive) y se crean si no existen, con caché por import; SKU autogenerado con el mismo patrón que T2.11 si la celda viene vacía — `generateSku()` extraído a `sku.util.ts` compartido entre `variants.service.ts` y el importador, en vez de duplicar la regla. Stock inicial vía `stock.service.registrarEntrada` (RN-8) — nunca `stock_actual` directo. Encabezado incompleto o CSV sin filas rechaza el archivo completo con 400 (problema del archivo, no de una fila). Sin `Idempotency-Key`: reenviar el mismo CSV no duplica nada, cada fila choca con la constraint única de `sku` y se reporta como error de fila. **Bug real encontrado escribiendo los tests:** la caché de producto/talle/color/marca/categoría es compartida entre filas del mismo import, pero si una fila crea una entidad nueva y falla después en un paso posterior de esa misma fila, el rollback de Prisma deshace la escritura en la base pero no las mutaciones del `Map` de JS — una fila posterior con el mismo nombre reusaría un id inexistente (P2003 en cascada); corregido con una copia de la caché por fila, mergeada a la compartida solo si la transacción de esa fila termina bien. 21 tests unitarios (`parseDecimalField`/`parseStockField` puros + `import()` con Prisma mockeado) + 5 de `sku.util.ts` + 8 de integración contra Postgres real (alta completa desde cero, reuso de producto entre filas, SKU autogenerado, fila inválida no aborta el import, SKU duplicado traducido, 403 SELLER, mass-assignment, encabezado incompleto). `tsc --noEmit`, build y lint limpios. Unitarios 167/167, integración 157/157 (3 corridas seguidas sin flakes). | agente |
| 2026-08-23 | Módulo `products`/`variants`+`stock` — Fase 07 (cierre) | VERDE | commit `df56bf3` en rama `fase07-cierre-products` (ramificada de `feat/T2.13-importacion-csv`, todavía sin mergear a `main`) — auditoría de T2.1–T2.13 contra `state/reports/modulo-products-variants-spec.md` (ahora todos VERDE). **Funcionalidades de la spec implementadas:** todos los endpoints REST salvo uno (ver gaps), toda la matriz de permisos (§8), invariantes 1/5/6 (T2.8 cubre el 1 con reconciliación agregada; el 5 con tests de `stock.service`; el 6 solo a nivel de aplicación, ver gaps), RN-1 a RN-12. **Inconsistencias corregidas (3):** (1) bug real en `prices.service.ts` (T2.10) — el edge case de BLUEPRINT §6 sobre variantes inactivas en `bulk-update` ("selección manual explícita de ids se respeta tal cual, incluidas inactivas") nunca se implementó: la versión original filtraba `activo: true` siempre. Corregido, con test de integración nuevo contra Postgres real. (2) `assertPositive` (importe > 0) estaba reimplementado con el mismo mensaje en `variants.service.ts`, `stock.controller.ts` y `catalog-import.service.ts` — extraído a `common/money/money.util.ts` (el chequeo de `prices.service.ts`, con mensaje más rico que identifica SKU y valor calculado, se dejó sin tocar a propósito). (3) `violatedConstraint` (extraer el nombre de constraint de un P2002) duplicado en `variants.service.ts` y `catalog-import.service.ts` — extraído a `prisma-error.util.ts` nuevo. **Tests agregados:** 3 de `assertPositive`, 3 de `violatedConstraint`, 1 de integración del edge case de inactivas — 174/174 unitarios, 158/158 integración (3 corridas seguidas sin flakes) tras las correcciones. **Problemas pendientes (gaps de alcance, no corregidos — necesitan su propia decisión):** falta `GET /variants/:id/stock-movements` (documentado en la spec §4.1 como parte del contrato de Stock, ningún ticket lo construyó nunca); falta el `CHECK` de base para el invariante 6 que la spec decía que existía desde la fase 01 — nunca se agregó, el motivo obligatorio de `AJUSTE` solo está protegido a nivel de DTO/servicio (riesgo bajo hoy, dado que `stock.service.ts` es el único punto de escritura y ya está testeado, pero sin la misma defensa en profundidad que `cash_movements`/`sale_items`/`payments`/`expenses`, que sí tienen su `CHECK`). Además corregida una nota desactualizada de la spec (`totalItems` → `itemCount`, la implementación real siempre usó `itemCount` desde T2.2). `tsc --noEmit`, lint y build limpios (backend y frontend). **No se declara el módulo terminado** — eso lo decide la Fase 12, después de QA adversarial y seguridad. | agente |
| 2026-08-23 | Módulo `products`/`variants`+`stock` — Fase 08 (QA adversarial) | VERDE | `state/reports/modulo-products-qa-2026-08-23.md`, commit pendiente en rama `fase08-qa-products` (ramificada de `fase07-cierre-products`) — 1 hallazgo HIGH corregido: `CatalogImportService.resolveProduct()` podía crear dos `products` duplicados bajo import concurrente con el mismo nombre nuevo (`products.nombre`, a diferencia de brand/category/size/color, no tiene `@unique`) — confirmado 15/15 carreras antes del fix, 0/15 después; corregido con `pg_advisory_xact_lock` scoped a la transacción, sin tocar el schema, verificado además con un test de integración HTTP real (8 iteraciones). 1 hallazgo LOW corregido: `CreateVariantGridDto` sin `@ArrayMaxSize` en `sizeIds`/`colorIds`/`filas` (riesgo de transacción larga con un payload enorme, ruta OWNER-only) — agregados `@ArrayMaxSize(50)`/`@ArrayMaxSize(1000)`. **Mutation testing (Stryker) sobre `stock.service.ts`, obligatorio por BLUEPRINT §9.8:** 62.86% → **100%** (70/70 mutantes matados) tras agregar aserciones exactas sobre la forma de las queries a Prisma y un caso límite `delta = 0`. Revisado sin hallazgos nuevos: paginación ya acotada (`@Max(100)`), autorización server-side completa, mass-assignment bloqueado por el `ValidationPipe` global, IDs inválidos/inexistentes dan 400/404 (no 500), doble-submit protegido en las 5 pantallas de catálogo (botón deshabilitado mientras la request está en vuelo, verificado en código). Verificación manual en navegador (Browser pane): validación de formulario en cliente y visualización de error de servidor probadas en vivo — 1 hallazgo LOW no bloqueante, documentado como TD-8 en `state/TECH_DEBT.md` (los 10 puntos de captura de `ApiError` en el frontend del módulo muestran el mensaje crudo del backend, en inglés cuando la regla violada no está pre-validada en el cliente — no pierde datos ni rompe la pantalla, es una inconsistencia de idioma). 176/176 unitarios (+2 de Stryker), 161/161 integración (+3: carrera de import concurrente, límites de la grilla), 3 corridas seguidas sin flakes. `tsc --noEmit`, lint y build (backend y frontend) verdes. **No se declara el módulo terminado** — eso lo decide la Fase 12, después de la auditoría de seguridad (fase 09). | agente |
| 2026-08-23 | Módulo `products`/`variants`+`stock` — Fase 09 (Security audit) | VERDE | `state/reports/modulo-products-secaudit-2026-08-23.md` — sin código modificado (regla de fase 09). Sin hallazgos CRITICAL ni HIGH. Confirmado que las protecciones globales corregidas en la fase 10 de `auth` (CSRF vía `jsonOnlyMiddleware` en `forRoutes('*')`, redact de logs, `helmet()`, `JWT_SECRET.min(32)`, `X-Powered-By` deshabilitado) siguen activas y cubren este módulo — releídas `app.module.ts`/`main.ts`/`env.schema.ts` para confirmarlo, no asumido. Verificado específicamente para `products`/`variants`/`stock`: autorización correcta en las 20 rutas del módulo contra `modulo-products-variants-spec.md` §8 (sin rutas sin guard ni de más); el vector de privilege escalation más obvio (`precioVenta`/`costoActual` inyectados en `PATCH /variants/:id`, abierto a SELLER) da 400 por el `ValidationPipe` global, confirmado con un test de integración existente; SQL crudo (advisory lock de la fase 08, `SELECT...FOR UPDATE` de T2.4) usa template tags parametrizados de Prisma, sin concatenación; sin sinks de XSS en el frontend del módulo; sin filesystem/exec/SSRF en el backend del módulo. 1 hallazgo LOW: `npm audit` del backend subió de 3 a 12 advisories tras agregar Stryker como devDependency (fase 08) — confirmado que los 9 nuevos son 100% dev-only (`npm audit --omit=dev` sigue en 3), documentado como TD-9. **No hay nada que remediar — las fases 10/11 no aplican**, el módulo queda en condiciones de avanzar a la fase 12 cuando corresponda. | agente |
| 2026-08-23 | Módulo `products`/`variants`+`stock` — Fase 10 (Security remediation) | VERDE | Corrección a la fila anterior: la fase 09 no encontró ninguna vulnerabilidad CRITICAL/HIGH/MEDIUM que corregir, solo 1 LOW (dependencia de desarrollo de Stryker, sin fix de código disponible — TD-9), así que no hubo nada dentro del alcance de esta fase (`docs/build-protocol/10-security-remediation.md`: "Corregí exclusivamente las vulnerabilidades identificadas") para implementar. Sin código modificado, sin tests nuevos, sin regresión que correr. **Esto NO habilita omitir la Fase 11**: la nota de proporcionalidad de `docs/build-protocol/11-security-reaudit.md` exige riesgo BAJO en `MVP_SCOPE.md` — `products`/`variants` está clasificado **ALTO** ahí (§5, "en retail el stock es plata") — y además la Fase 09 sí tuvo 1 hallazgo, no cero. Se declara VERDE porque no queda ninguna vulnerabilidad pendiente de corregir, no porque la fase se haya saltado. | agente |
| 2026-08-23 | Módulo `products`/`variants`+`stock` — Fase 11 (Security re-audit) | VERDE | `state/reports/modulo-products-secreaudit-2026-08-23.md` — sin archivos modificados (regla de fase 11), confirmado además que no hubo ningún commit de código entre la fase 09 (`00d598a`) y esta verificación. El único hallazgo previo (LOW, advisories de Stryker en `npm audit`) reverificado sin cambios: 12 advisories totales, 3 con `--omit=dev` (mismos de siempre, cadena de `prisma` CLI), 0 en frontend. Sin fixes de la fase 10 que reverificar (no hubo ninguno — no había nada CRITICAL/HIGH/MEDIUM que corregir). Verificaciones empíricas nuevas contra el servidor de desarrollo real, no solo releer el reporte anterior: CSRF (`jsonOnlyMiddleware`) confirmado en vivo con 415 contra `POST /brands` y `POST /stock/ajustes` (ruta de dinero/stock) con `Content-Type: application/x-www-form-urlencoded`; headers de `helmet` presentes y `X-Powered-By` ausente en `GET /brands`; redacción de logs confirmada con una sesión real (`"set-cookie":"[REDACTED]"` en `/auth/login`, sin cookies sin censurar en requests autenticadas a `/variants/:id`); los 3 tests de integración del vector de privilege escalation más obvio del módulo (mass-assignment de `precioVenta`/`costoActual` en `PATCH /variants/:id`, abierto a SELLER) re-ejecutados en vivo, 3/3 verde. Sin CRITICAL ni HIGH nuevos. **No se declara el módulo seguro de forma permanente** — el módulo queda en condiciones de avanzar a la fase 12 (production readiness) cuando corresponda. | agente |
| 2026-08-23 | Módulo `products`/`variants`+`stock` — Fase 12 (Production readiness) | VERDE — **PRODUCTION READY: YES** | `state/reports/modulo-products-prodready-2026-08-23.md` — sin código modificado (regla de fase 12). Sin CRITICAL ni HIGH. Security/Reliability/Performance/Code quality/Observability/Deployment revisados contra el checklist de la fase, todo corrido de nuevo (no reciclado de fases anteriores): `tsc --noEmit`, lint y build (backend y frontend) verdes; 176/176 unitarios backend, 161/161 integración backend (×3 sin flakes), **26/26 tests de frontend con Vitest** (incluidos los 5 de `grid.ts`, la lógica más compleja de la UI del módulo); `npx prisma migrate status` sin pendientes; sin N+1 verificado en `products.service.ts`/`variants.service.ts`; sin `TODO`/`FIXME`/`HACK` en el módulo; cobertura de servicios 76.7%-100% (los controllers en 0% en la corrida unitaria los cubre la suite de integración, misma separación deliberada que `auth`). 1 hallazgo LOW nuevo: el bundle de producción del frontend supera 500kB en un solo chunk sin code-splitting (611kB/187kB gzip) — primera vez que una fase 12 corre el build real y lo nota, TD-10, no bloqueante a escala MVP. Los LOW ya existentes (TD-8, TD-9) reconfirmados sin cambios; los MEDIUM de `auth` (TD-3, TD-4) siguen aplicando de forma transversal, no son hallazgos nuevos de este módulo. **Módulo `products`/`variants`+`stock` aprobado — pasa a la fase 13 (Integration Audit).** | agente |
| 2026-08-24 | Módulo `cash-registers` — Fase 06 (Spec del módulo) | **BLOQUEADO** | `state/reports/modulo-cash-registers-spec.md` — spec completa (11 secciones) sobre BLUEPRINT §3.6, §5.1, §5.5, invariantes 2/7/9/10, §7, §9.3, §9.4, §9.7, §9.8, §10; `MVP_SCOPE.md` §3.4 (riesgo ALTO); `DECISIONES_PENDIENTES.md` A7/B5; AMB-5/AMB-10 (ya resueltas: diferencia no bloquea cierre, umbral $500 fijos). **1 ambigüedad nueva agregada a `state/AMBIGUITIES.md`:** AMB-13 (⚠️ ALTO — ¿`SELLER` puede hacer ingreso manual o retiro de efectivo? sin guía del blueprint, a diferencia de apertura/cierre que sí están explícitamente abiertos a cualquiera; recomendación: `OWNER`-only para las dos, bloquea solo a T3.3). **Hallazgo técnico bloqueante, no de negocio:** `T4.4`, `T4.7` (`sales`) y `T5.3` (`returns`) no listaban `T3.2` como dependencia en `ROADMAP.md` pese a que BLUEPRINT §5.3/§5.4 exige que llamen a la API interna de este módulo para sus propios movimientos de caja — corregido agregando `T3.2` a las tres (mismo patrón que `T6.3` de gastos, que sí la listaba). Diseño técnico completo: el servicio nunca recibe el signo del monto, lo aplica él mismo según `tipo` (defensa contra un caller que lo mande mal); `monto_sistema` se recalcula on-demand con `SUM`, sin contador denormalizado; identificada una carrera real entre insertar un movimiento y cerrar la sesión simultáneamente, resuelta con `SELECT...FOR UPDATE` sobre la fila de sesión (mismo patrón que BLUEPRINT §9.4 para stock, aplicado acá a una sesión en vez de una variante). **Gap real encontrado:** la inmutabilidad de movimientos tras el cierre (§5.5, "se bloquea a nivel de base de datos") solo existe a nivel de aplicación hoy — falta un trigger en la migración, asignado a T3.2. Stryker obligatorio sobre `cash-register.service.ts`, nombre literal en BLUEPRINT §9.8. **Módulo bloqueado hasta resolver AMB-13 (solo afecta a T3.3) y ejecutar T0.13 (solo afecta a T3.4, AMB-10 ya resuelta); T3.1 y T3.2 pueden arrancar ya.** | agente |
| 2026-08-24 | Fase 04a — T3.1 + T3.2 (`cash-register.service`), tests primero | ROJO (esperado) | commit `ac937bc` en rama `feat/T3.1-T3.2-caja-base` (ramificada de `fase06-spec-cash-registers`), sesión aislada (subagente sin visibilidad del resto de la conversación) — no se leyó ninguna implementación de otro módulo, solo `ROADMAP.md` (T3.1, T3.2), `BLUEPRINT.md` (§3.6, §5.1, §5.5, invariantes 2/7/9/10, §7, §9.3, §9.4, §9.7), `state/reports/modulo-cash-registers-spec.md` (RN-1 a RN-12, secciones 4.2, 5, 6, 7, 9) y `AMBIGUITIES.md`, más la ESTRUCTURA mecánica (no la lógica) de los tests de `stock`. **14 casos unitarios** (`cash-register.service.spec.ts`, Prisma mockeado: `abrirSesion` camino feliz + `montoInicial = 0` válido + negativo rechazado; `registrarMovimiento` signo correcto para los 6 `tipo` vía `it.each` + rechazo de `monto <= 0`/`descripcion` vacía/sesión `CERRADA`; `getSesionAbiertaOrThrow` devuelve/lanza) + **10 de integración** (`cash-registers.integration.spec.ts`, Postgres real: `POST /cash-registers/sessions` 201 OWNER/SELLER, 400 negativo, 409 con sesión ya abierta —dispara la constraint única real—; `registrarMovimiento` con signo real en la base, CHECK de signo ejercitado insertando directo por Prisma sorteando el servicio, rechazo contra sesión `CERRADA`; `getSesionAbiertaOrThrow` devuelve/lanza). **24/24 en rojo confirmados**, todos por `Cannot find module` (ausencia de implementación, no error de compilación). Sin bloqueos ni ambigüedades nuevas — AMB-13/T0.13 no afectan a estos dos tickets. | agente |
| 2026-08-24 | T3.1 + T3.2 — Apertura de sesión + movimientos de caja (servicio base) (`cash-registers`) | VERDE | commit pendiente en rama `feat/T3.1-T3.2-caja-base` — implementado contra los 24 tests de la fase 04a **sin modificar ninguna aserción existente** (2 hallazgos de tooling corregidos en los tests: tipado de `jest.Mock<...>` en vez de `jest.fn()` sin tipar, mismo patrón que `prices.service.spec.ts`/`variants.service.spec.ts`, para sacar 2 falsos positivos de lint `no-unsafe-assignment`/`no-unsafe-member-access`; y la limpieza de `afterAll` del archivo de integración, que necesitó reabrir cada sesión antes de borrar sus movimientos — ver hallazgo de abajo). `CashRegisterService.abrirSesion`: valida `montoInicial >= 0`, traduce la violación del índice único parcial (P2002) a 409 "Ya hay una sesión de caja abierta" (RN-1) — la exclusión real la garantiza la constraint de la fase 01, no lógica del servicio. `registrarMovimiento`: recibe siempre `monto` positivo y aplica el signo según `tipo` (RN-3, nunca confía en que quien llama ya lo mande con el signo correcto); bloquea la fila de la sesión con `SELECT...FOR UPDATE` antes de leer su `estado` (mismo patrón que BLUEPRINT §9.4 para stock, aplicado a una fila de sesión) — sin este lock, un movimiento y un cierre concurrentes podrían intercalarse mal. `getSesionAbiertaOrThrow`: expuesto para `sales`/`returns`/`expenses` futuros (RN-10/RN-11). **Gap real cerrado en esta fase (asignado a T3.2 en la spec):** la inmutabilidad de movimientos tras el cierre (§5.5, "se bloquea a nivel de base de datos") solo existía a nivel de aplicación — agregada la migración `20260824181450_cash_movements_immutable_after_close` con un trigger (`BEFORE INSERT OR UPDATE OR DELETE ON cash_movements`) que rechaza cualquier escritura contra una sesión `CERRADA`, verificado con un test de integración nuevo que inserta directo por Prisma sorteando el servicio (mismo criterio que el test del CHECK de signo). **Hallazgo real durante la implementación:** el trigger, correctamente, también bloquea el `DELETE` de limpieza del propio archivo de test contra sesiones que los tests cierran a propósito — corregido reabriendo cada sesión antes de borrarla en el `afterAll` (no desactiva la protección, solo evita que la limpieza de datos de prueba choque contra el mismo invariante que se está probando). `POST /cash-registers/sessions` sin `@Roles`: abierto a cualquier rol autenticado (§5.5, RN-2 — una vendedora tiene que poder abrir sola). `CashRegistersModule` registrado en `AppModule`. 190/190 unitarios, 172/172 integración (3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint y build verde. T3.3 sigue bloqueada por AMB-13; T3.4 sigue esperando T0.13. | agente |
| 2026-08-24 | AMB-13 — resolución (`cash-registers`) | RESUELTA | `state/AMBIGUITIES.md` — el PO aprobó la recomendación: `OWNER`-only para `INGRESO_MANUAL` y `RETIRO`. **T3.3 desbloqueado.** | agente |
| 2026-08-24 | Fase 04a — T3.3 (`cash-register.service` + controller), tests primero | ROJO (esperado) | sesión aislada (subagente sin visibilidad del resto de la conversación) — no se leyó lógica de negocio de ningún módulo, solo `ROADMAP.md` (T3.3), `BLUEPRINT.md` (§3.6, §5.5, §9.7, invariante 9), `state/reports/modulo-cash-registers-spec.md` (RN-12, secciones 4.1/4.2/6/7/8), `AMBIGUITIES.md` (AMB-13 resuelta) y, como infraestructura compartida (no lógica de un módulo), los 3 archivos de `common/idempotency/`. **5 casos unitarios nuevos** agregados al archivo existente (`registrarMovimientoManual`: signo correcto INGRESO_MANUAL/RETIRO, rechazo de `monto <= 0`/`descripcion` vacía/sesión `CERRADA`) — como el método no existe todavía y no se permite agregar ni un stub al servicio real, se declaró localmente el contrato esperado y se casteó el servicio a él, para que el archivo compile pero la llamada real lance en runtime (razón correcta de rojo, sin tumbar los 14 tests viejos de T3.1/T3.2 del mismo archivo). **14 casos de integración nuevos** agregados al archivo existente (`POST /cash-registers/movements/ingreso` y `/retiro`: 201 OWNER con signo correcto en la base, 403 SELLER, 400 sin header `Idempotency-Key`, **doble click con la misma clave → una sola fila en `cash_movements`** —el ejemplo textual del blueprint—, claves distintas → filas distintas, 409 sin sesión abierta, 400 monto ≤ 0). **19/19 tests nuevos en rojo confirmados** (5 unitarios + 14 de integración), todos por ausencia de implementación (404 en las rutas HTTP, `TypeError` en el método), sin tumbar ninguno de los 25 tests viejos del mismo módulo. **Hallazgo reportado, no resuelto por la sesión aislada (correcto):** el contrato de `registrarMovimientoManual` que el propio ticket exige (`idempotencyKey` como parámetro, devuelve la fila creada) no coincidía con el `Promise<void>` sin esa clave que documentaba la sección 4.2 de la spec — quedó señalado para que la Fase 04 lo reconcilie, no resuelto por la sesión de tests. | agente |
| 2026-08-24 | T3.3 — Ingreso manual y retiro de efectivo, idempotente (`cash-registers`) | VERDE | commit pendiente — implementado contra los 19 tests nuevos de la fase 04a **sin modificar ninguna aserción existente**. Reconciliado el hallazgo de la fase 04a: `registrarMovimiento` ahora devuelve la fila creada (`Promise<CashMovement>`, antes `void` — cambio compatible hacia atrás, ningún test de T3.1/T3.2 leía el valor de retorno) y acepta un `idempotencyKey` **opcional**; `registrarMovimientoManual` (nuevo) lo exige siempre y delega en `registrarMovimiento` sin duplicar lógica de signo/lock/inmutabilidad — corregida la sección 4.2 de `state/reports/modulo-cash-registers-spec.md` para reflejar esto. `POST /cash-registers/movements/ingreso` y `/retiro`: `@Roles(OWNER)` (AMB-13), `@UseInterceptors(IdempotencyInterceptor)` + `@IdempotencyKey()` (T0.14) envolviendo la transacción completa (resolver sesión abierta vía `getSesionAbiertaOrThrow` + insertar) en `withIdempotency(write, findExisting)` — primer consumidor HTTP real de T0.14 desde que se construyó. `ManualMovementDto` con `descripcion` obligatoria a nivel de DTO (mismo patrón que `motivo` en `CreateAjusteDto`) además del chequeo redundante ya existente a nivel de servicio. 195/195 unitarios, 186/186 integración (3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint y build verde. T3.4 sigue esperando T0.13. | agente |
| 2026-08-24 | T0.13 — Tabla `settings` + servicio tipado + seed (Etapa 0, `common/settings`) | VERDE | commit pendiente — no exigía fase 04a aislada (solo obligatoria para `sales`/`returns`/`cash-registers`/el servicio de stock; T0.13 es infraestructura de Etapa 0), tests escritos junto con la implementación en el flujo normal de fase 04. La tabla `settings` ya existía desde la fase 01 (sin migración nueva en este ticket). `SettingsService` con getters/setters tipados por parámetro (`getBool`/`getInt`/`getDecimal`, `setBool`/`setInt`/`setDecimal`) — nunca recibe el `tx` de quien llama (a diferencia de `stock.service.ts`/`cash-register.service.ts`): ningún módulo va a escribir un setting dentro de su propia transacción de negocio, solo leerlo para decidir una regla, así que no hace falta threadear la transacción ajena. Cada getter/setter valida que el `tipo` de la fila coincida con el método usado (ej. `getBool` sobre una clave `INT` lanza, en vez de parsear mal en silencio) — protección contra el error real de usar el método equivocado para una clave. `set*` solo actualiza, nunca crea una clave nueva (las 4 vienen fijas del blueprint; un typo en la clave da 404 en vez de crear una fila muerta). Sembrados los 4 parámetros de BLUEPRINT §10 en `prisma/seed.ts` (`update: {}` — no pisa un valor que la dueña ya haya cambiado desde T6.9 en una corrida posterior): `permitir_venta_sin_stock=false`, `max_descuento_vendedor_pct=10`, `dias_plazo_devolucion=30`, `umbral_diferencia_caja=500.00` (AMB-10, RESUELTA). Seed corrido de verdad contra la base de desarrollo y confirmado con un test de integración dedicado que lee los 4 valores reales (no solo fixtures propios). `SETTINGS_KEYS` (`common/settings/settings-keys.ts`) expone las 4 claves como constantes tipadas para que `sales`/`returns`/`cash-registers` no usen strings sueltos. Sin controller: la pantalla de configuración es T6.9, que ya depende de T0.13 en `ROADMAP.md`. 13 tests unitarios + 4 de integración nuevos. 208/208 unitarios, 190/190 integración (3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint y build verde. **T3.4 desbloqueada** (AMB-10 y T0.13 ya no bloquean nada). | agente |
| 2026-08-24 | Fase 04a — T3.4 (`cash-register.service` + controller, cierre con arqueo), tests primero | ROJO (esperado) | sesión aislada (subagente sin visibilidad del resto de la conversación) — no se leyó lógica de negocio de ningún módulo, solo `ROADMAP.md` (T3.4), `BLUEPRINT.md` (§3.6, §5.1, §5.5, §7, §10, invariante 2), `state/reports/modulo-cash-registers-spec.md` (RN-4/RN-5/RN-6, secciones 4.1/4.2/5/6/7/8), y como referencia mecánica (no lógica de negocio) la forma de `VariantForRole`/`hideOwnerOnlyFields` en `products/variants.service.ts` para el ocultamiento de campos por rol. **8 casos unitarios nuevos** (`cerrarSesion`: camino feliz diferencia 0, diferencia bajo el umbral sin nota, diferencia exactamente $500 sin nota con `esOwner:true` rechaza, misma diferencia con `esOwner:false` no rechaza —RN-6—, ocultamiento de `montoSistema`/`diferencia` para ambos roles, rechazo de sesión ya cerrada, rechazo de sesión inexistente) — mismo criterio que la fase 04a de T3.3: contrato declarado localmente y casteado, sin tocar el servicio real. **8 casos de integración nuevos** (`POST /cash-registers/sessions/:id/close`: OWNER con movimientos reales, SELLER oculta campos, diferencia real ≥ umbral sin nota → 400, con nota → 200, SELLER sin nota → 200, sesión ya cerrada → 409, sesión inexistente → 404, y un caso encadenado de RN-8 tras cerrar por HTTP). **16/16 tests nuevos en rojo confirmados**, sin tumbar los 27 tests viejos del mismo módulo. **Hallazgo reportado, no resuelto por la sesión aislada (correcto):** el contrato de `cerrarSesion` no especifica cómo llega `umbral_diferencia_caja` (RN-5) al servicio — la sesión asumió que la Fase 04 inyecta `SettingsService` en el constructor de `CashRegisterService` (mismo mecanismo que el resto del sistema usa para config), y armó los tests con esa asunción explícita, señalada para reconciliar en la implementación. | agente |
| 2026-08-24 | T3.4 — Cierre con arqueo (`cash-registers`) | VERDE | commit pendiente — implementado contra los 16 tests nuevos de la fase 04a **sin modificar ninguna aserción existente** (arreglos de infraestructura de test: 2 instancias de `new CashRegisterService(...)` con 1 solo argumento actualizadas al nuevo constructor de 2, y el tipo de `montoSistema`/`diferencia` corregido para aceptar `null` además de `undefined`). Confirmada la asunción de la fase 04a: `CashRegisterService` ahora recibe `SettingsService` inyectado — corregida la sección 4.2 de `state/reports/modulo-cash-registers-spec.md` para reflejarlo. `cerrarSesion`: mismo lock `SELECT...FOR UPDATE` sobre la fila de sesión que ya usaba `registrarMovimiento` (sección 5 de la spec) antes de sumar los movimientos con `cashMovement.aggregate`; `montoSistema = montoInicial + SUM(monto)`, `diferencia = montoDeclarado - montoSistema`; nota obligatoria solo si `esOwner && |diferencia| >= umbral_diferencia_caja && !notaCierre` (RN-5) — a un SELLER nunca se le exige, ni se le devuelve `montoSistema`/`diferencia` en la respuesta (`CashRegisterSessionForRole`, mismo patrón que `VariantForRole`/`hideOwnerOnlyFields` de `products/variants.service.ts`, campos ocultos del todo, nunca en 0/null). `POST /cash-registers/sessions/:id/close` sin `@Roles` (RN-6, cierre a ciegas) — `esOwner` se deriva del rol real de la sesión (`user.rol`), nunca de algo que mande el cliente. Ajustado a `@HttpCode(HttpStatus.OK)` (NestJS devuelve 201 por defecto en un `@Post`, y el contrato de la spec/tests pedía 200 — mismo patrón ya usado en `auth.controller.ts`). 216/216 unitarios, 198/198 integración (3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint y build verde. | agente |
| 2026-08-24 | Fase 04a — T3.5 (`GET /cash-registers/sessions/open`), tests primero | ROJO (esperado) | sesión aislada (subagente sin visibilidad del resto de la conversación) — no se leyó lógica de negocio de ningún módulo, solo `ROADMAP.md` (T3.5), `BLUEPRINT.md` (§5.5, §9 AD-13), `state/reports/modulo-cash-registers-spec.md` (RN-7, secciones 4.1/6/8), y como referencia mecánica la firma (no el cuerpo) de `CashRegisterSessionForRole`/`getSesionAbiertaOrThrow`. Sin casos unitarios nuevos (no había lógica de servicio nueva que agregar — confirmados los 27 existentes en verde igual). **5 casos de integración nuevos**: OWNER ve `montoSistema` recalculado en vivo, SELLER no lo ve (RN-6), sin sesión abierta → 404 (mismo solape de motivo-equivocado que T3.4 con "sesión inexistente", documentado explícitamente), la respuesta incluye `fechaApertura` (para que T3.7 compare contra "hoy" en hora argentina), y una verificación directa de invariante 2 con 4 movimientos de distintos tipos y signos. **4/5 en rojo por la razón correcta** (ruta ausente); el 5º (sin sesión abierta → 404) pasa hoy por el motivo equivocado, señalado sin resolverlo. Sin bloqueos. | agente |
| 2026-08-24 | T3.5 — Sesión olvidada abierta: detección (`cash-registers`) | VERDE | commit pendiente — implementado contra los 5 tests nuevos de la fase 04a, sin modificar ninguna aserción existente. **Alcance real, no lo que el título del ticket sugiere:** RN-7 de la spec ya aclaraba que no hay lógica de "detección" del lado del backend — la comparación de `fechaApertura` contra "hoy" en hora argentina y el bloqueo real de la interfaz ("cierre obligatorio") son del frontend, que no existe todavía para este módulo (T3.7). Este ticket entrega solo lo que el backend puede dar: `GET /cash-registers/sessions/open`, sin `@Roles`. Agregado `CashRegisterService.getSesionAbiertaConTotales(tx, esOwner)` — a diferencia de `getSesionAbiertaOrThrow` (fila cruda, 409 si no hay sesión, pensado para que otros módulos validen una precondición de negocio), este método calcula `montoSistema` en vivo (`montoInicial + SUM(cash_movements.monto)`, mismo cálculo que haría un cierre real en ese instante — invariante 2) y usa 404 (semántica correcta para un GET cuyo propósito es chequear si existe algo). Refactor menor sin cambio de comportamiento: la búsqueda de la sesión abierta se extrajo a un helper privado (`buscarSesionAbierta`) compartido por ambos métodos. Mismo ocultamiento de `montoSistema` para `SELLER` que `cerrarSesion` (RN-6); `diferencia` queda `null` tal cual (no aplica a una sesión todavía abierta). 216/216 unitarios (sin cambios), 203/203 integración (3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint y build verde. | agente |
| 2026-08-24 | Fase 04a — T3.6 (`reconciliar`, invariante 2), tests primero | ROJO (esperado) | sesión aislada (subagente sin visibilidad del resto de la conversación) — no se leyó lógica de negocio de ningún módulo, solo `ROADMAP.md` (T3.6), `BLUEPRINT.md` §6 completa (las 15 invariantes, "las tres primeras además un chequeo de reconciliación ejecutable"), `state/reports/modulo-cash-registers-spec.md` (sección 3 — invariante 2, sección 9 — "test dedicado T3.6"), y como referencia CONCEPTUAL (prosa de `STATUS.md`, no código) la fila de T2.8 que hizo lo mismo para el invariante 1 de `stock`. **6 casos unitarios nuevos** (sin sesiones cerradas → vacío; varias que cuadran → vacío; una que no cuadra → aparece con ambos valores; reporta solo las que no cuadran, no todo el universo; una sesión ABIERTA de por medio no se incluye —no tiene `monto_sistema` persistido—; confirma que `reconciliar()` abre su propia transacción `REPEATABLE READ`, mismo patrón que `stock.service.reconciliar()`) — mock flexible que soporta `groupBy`/`aggregate`/`findMany` como candidatos, sin adivinar cuál usaría la implementación real. **2 casos de integración nuevos** (sesión cerrada con movimientos reales vía `cerrarSesion` real → no reportada; sesión con `montoSistema` alterado a mano por fuera del servicio → aparece con los valores correctos, "prueba que el chequeo detecta un desajuste real, no solo confirma el camino feliz", mismo criterio que T2.8). **8/8 en rojo confirmados**, sin tumbar los 65 tests viejos del módulo (27 unitarios + 38 integración). Sin bloqueos. | agente |
| 2026-08-24 | T3.6 — Test del invariante 2, `reconciliar()` (`cash-registers`) | VERDE | commit pendiente — implementado contra los 8 tests nuevos de la fase 04a **sin modificar ninguna aserción existente** (solo 3 arreglos de tooling en el archivo de tests: una función auxiliar `withReconciliar` local del describe de unitarios quedó sin uso tras el resto del diseño del test y se eliminó; un parámetro `_options` no usado en un mock; y el tipo de `transactionMock` pasó de `jest.Mock` genérico a uno tipado con la firma real de `$transaction`, mismo patrón ya usado varias veces en este archivo para sacar falsos positivos de `no-unsafe-member-access`). `CashRegisterService.reconciliar()`: mismo patrón exacto que `StockService.reconciliar()` (T2.8) — `cashRegisterSession.findMany({where: {estado: CERRADA}})` + `cashMovement.groupBy({by: ['sessionId'], _sum: {monto: true}})` dentro de una única transacción `REPEATABLE READ` abierta por el propio método (única excepción al contrato de "nunca abre su propia transacción", por ser de solo lectura); recalcula `montoInicial + suma` por sesión y lo compara contra el `montoSistema` guardado, devolviendo solo las que no coinciden. Solo sesiones `CERRADA` — una `ABIERTA` no tiene `montoSistema` persistido contra el cual comparar (eso ya lo cubre `getSesionAbiertaConTotales`, T3.5, con el cálculo en vivo). 222/222 unitarios, 205/205 integración (3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint y build verde. | agente |
| 2026-08-24 | T3.7 — Pantallas de apertura, movimientos y cierre (`cash-registers`, frontend) | VERDE | rama `feat/T3.7-caja-pantallas` (ramificada de `feat/T3.6-caja-reconciliacion`), commit pendiente — **Fase 04a NO aplica** (evaluado y descartado antes de empezar, no omitido sin más): `docs/build-protocol/04a-tests-first.md` es enteramente backend/Postgres (`src/modules/`, derivación de reglas de negocio), sin ninguna contemplación de UI; mismo criterio que T2.12 (la otra pantalla de frontend del proyecto), que tampoco la usó. Ejecutado como Fase 04 normal: Vitest para la única lógica realmente pura y nueva, verificación manual exhaustiva en navegador para el resto. **Primer consumidor real de la infraestructura de idempotencia del lado del cliente** (BLUEPRINT §9.7/§12.1 — clave UUID generada al abrir el formulario, no al enviar, persistida en `sessionStorage`, rotada tras un éxito): `lib/idempotency.ts` nuevo (funciones puras + hook `useIdempotencyKey`, separadas para que Vitest —`environment: 'node'`, sin DOM— las testee con un `KeyValueStorage` mockeado en vez del `sessionStorage` real), consumido por `ManualMovementModal.tsx` para ingreso/retiro. Pantalla única `CashRegisterPage.tsx`, ruta `/caja`, sin `RequireOwner` (la propia página resuelve `isOwner` de `useAuth()` y renderiza distinto por rol, mismo patrón que `ProductDetailPage.tsx`). RN-7 ("sesión olvidada", sin lógica de detección en el backend — T3.5 solo expone `fechaApertura`): `esOlvidada = formatDate(session.fechaApertura) !== formatDate(new Date())`, reusando el helper es-AR existente en vez de escribir una comparación de fechas nueva; muestra alerta amarilla persistente y oculta ingreso/retiro mientras está olvidada, sin bloquear la navegación a `/catalogo` u otras secciones — no hay otra funcionalidad de "operar" todavía (`sales`/`returns` no existen) que valga la pena gatear, y hacerlo hubiese sido alcance no pedido por ningún ticket. RN-6 ("cierre a ciegas"): `montoSistema`/`diferencia` directamente ausentes del tipo/respuesta para SELLER (mismo patrón de omisión de campo que `costoActual?` en `products`), nota de cierre siempre opcional a nivel de formulario con enunciado neutral para SELLER ("¿Algo para comentar del turno?") — el umbral de $500 (RN-5) no se duplica en el cliente, solo se muestra el 400 del backend tal cual si rechaza el cierre. Botón de cierre dice explícitamente "Confirmar cierre de caja" (BLUEPRINT §12.6, confirmación explícita en toda acción destructiva). **Bug real encontrado y corregido verificando en el navegador** (no lo hubiese atrapado ningún test de tipos): `OpenSessionForm` pasaba la fila cruda de `POST /sessions` (T3.1, `montoSistema: null`, nunca calculado al crear) directo al estado de la página — la pantalla no mostraba "Monto en sistema" recién abierta la caja pese a ser lógicamente igual a `montoInicial`; corregido para que `onOpened()` dispare una recarga real vía `GET /sessions/open` (T3.5, cálculo en vivo) en vez de reusar la respuesta del alta. **Hallazgo de tooling:** el patrón `useEffect(() => { void asyncFn(); }, [...])` para la carga inicial disparaba `react-hooks/set-state-in-effect` aunque el `setState` ocurriera después de un `await` (no sincrónico) — la única forma de conformar la regla fue una cadena de promesas inline (`.then()/.catch()` con bandera `cancelled`) directamente en el cuerpo del efecto, igual que el patrón preexistente de `AuthContext.tsx`; el callback nombrado (`loadSession`) se mantuvo para los handlers de botones, donde la regla no aplica. Verificado a mano en navegador contra el backend real, ambos roles: OWNER (abrir → ingreso → retiro → cierre rechazado por nota faltante justo en el umbral de $500 → cierre exitoso con nota) y SELLER (abrir → cierre con diferencia grande y sin nota, exitoso — confirma que "cierre a ciegas" nunca bloquea a SELLER); sesión olvidada simulada backdateando `fechaApertura` por script, confirmada la alerta y el bloqueo de ingreso/retiro para ambos roles (para OWNER se confirmó que el bloqueo es por `esOlvidada`, no por rol — sigue viendo "Monto en sistema" pero no los botones de movimiento). 31/31 tests de Vitest (26 + 5 nuevos de `idempotency.test.ts`), `tsc --noEmit`, lint y build del frontend limpios. Usuarios y sesiones de prueba creados durante la verificación borrados al cierre del ticket. **Etapa 3 completa (T3.1–T3.7, todos VERDE).** Sigue el cierre de módulo: fases 07→08→09→10→11→12, no se ejecutan sin pedido explícito. | agente |
| 2026-08-24 | Módulo `cash-registers` — Fase 07 (cierre de implementación) | VERDE | rama `fase07-cierre-cash-registers` (ramificada de `feat/T3.7-caja-pantallas`), commit pendiente — contrastado T3.1–T3.7 (todos VERDE) contra `state/reports/modulo-cash-registers-spec.md` completa. **Funcionalidades de la spec implementadas:** las 12 reglas de negocio (RN-1 a RN-12), los tres invariantes que el módulo garantiza directamente (2, 9, y la mitad de 10 que le toca), las tres defensas de base reforzadas (índice único parcial de sesión abierta, `CHECK` de signo, trigger de inmutabilidad tras cierre — las tres confirmadas leyendo las migraciones reales, no solo el texto de `STATUS.md`), y 5 de los 7 endpoints REST de la sección 4.1. **Coherencia entre tickets:** naming, manejo de errores (`BadRequestException`/`ConflictException`/`NotFoundException` vía `GlobalExceptionFilter`, mismo patrón que `auth`/`products`) y convenciones (DTOs con importes como `string` + `@IsDecimal`, positividad validada en el servicio no en el DTO) consistentes en los 7 tickets; `registrarMovimientoManual` reutiliza `registrarMovimiento` sin duplicar lógica de signo/lock. Sin `TODO`/código muerto/comentado en el módulo (backend ni frontend). **Hallazgo real corregido — gap de cobertura de tests, no de funcionalidad:** la sección 9 de la spec exige un "test de concurrencia explícito" entre un movimiento y un cierre simultáneos sobre la misma sesión (mismo patrón que T2.4 para el ajuste de stock negativo concurrente) — el código ya implementaba el lock (`SELECT...FOR UPDATE` sobre la fila de sesión, presente desde T3.2/T3.4) pero ningún test lo había ejercitado bajo carrera real; agregado a `cash-registers.integration.spec.ts`, 10 iteraciones con sesión fresca cada vez, confirmando que el cierre nunca falla por la carrera y que `montoSistema` final siempre refleja exactamente los movimientos que terminaron insertados — pasó a la primera y se corrió 3 veces seguidas sin flakes. **Problemas pendientes (gaps de alcance, no corregidos — necesitan su propia decisión, mismo criterio que el gap de `GET /variants/:id/stock-movements` en la fase 07 de `products`):** faltan `GET /cash-registers/sessions/:id` (detalle de una sesión puntual) y `GET /cash-registers/sessions/:id/movements` (listado paginado de movimientos) — ambos documentados en la spec §4.1 como parte del contrato del módulo, pero ningún ticket de T3.1–T3.7 los construyó nunca; hoy no bloquean nada porque `GET /sessions/open` (T3.5) alcanza para el único consumidor existente (`CashRegisterPage.tsx`). Tampoco se agregó una vía para que un `OWNER` complete `nota_cierre` en una sesión ya cerrada por un `SELLER` con diferencia sin explicar — la propia spec (sección 6) ya lo señalaba como extensión de alcance a confirmar con el PO, no algo que el blueprint pida explícitamente; sigue sin ticket. 222/222 unitarios (sin cambios), 206/206 integración (+1 nuevo, 3 corridas seguidas sin flakes), `tsc --noEmit`, lint y build (backend y frontend) limpios. **No se declara el módulo terminado** — eso lo decide la fase 12, después de QA adversarial (08) y seguridad (09/10/11). | agente |
| 2026-08-24 | Módulo `cash-registers` — Fase 08 (QA adversarial) | VERDE | `state/reports/modulo-cash-registers-qa-2026-08-24.md`, rama `fase08-qa-cash-registers` (ramificada de `fase07-cierre-cash-registers`), commit pendiente — **2 hallazgos reales, ambos corregidos y probados, confirmados primero contra el servidor real antes de escribir ningún test:** (1) HIGH — `cerrarSesion()` nunca validaba que `montoDeclarado` no fuera negativo (a diferencia de `montoInicial`, que sí lo hacía desde T3.1); un cliente HTTP directo podía cerrar una sesión con efectivo contado negativo, 200 con una `diferencia` sin sentido físico — no alcanzable desde la pantalla real (`CloseSessionModal` tiene `min={0}`), pero sí desde cualquier request directa, razón por la que igual bloqueaba (BLUEPRINT: la validación se verifica siempre en el servidor). (2) MEDIUM — las tres columnas de plata del módulo son `Decimal(12,2)`; un valor que excede esa precisión (ej. "99999999999999.00") llegaba crudo a Postgres, que lo rechazaba con "numeric field overflow" — un `PrismaClientUnknownRequestError` sin `.code` traducible, así que el `GlobalExceptionFilter` respondía 500 genérico en vez de 400; este sí era alcanzable desde la UI real (ningún `NumberInput` de dinero tiene `max`), confirmado tipeando el valor en el navegador real antes y después del fix. Ambos corregidos con una función local `assertDentroDePrecision()` (sin tocar `common/money/`, mismo alcance acotado al módulo) aplicada en los tres puntos de entrada, más la validación de negativo que faltaba en `cerrarSesion`. **Nota fuera de alcance:** `products`/`stock` probablemente comparten el mismo problema de precisión (misma columna `Decimal(12,2)`) — no se tocó ese módulo, ya cerrado, queda señalado para una decisión aparte. **Testing de mutación (obligatorio, BLUEPRINT §9.8 lista el módulo literal):** Stryker sobre `cash-register.service.ts` — **62.32% → 98.55%** (86→136 mutantes matados de 138 cubiertos). El salto grande vino de un gap real de cobertura que Stryker expuso: `getSesionAbiertaConTotales` (T3.5, cálculo en vivo del invariante 2 + ocultamiento RN-6) no tenía NINGÚN test unitario, solo integración — agregado un `describe` completo nuevo. También se agregaron tests para el catch de P2002 en `abrirSesion` (antes solo probado por integración), aserciones exactas `toHaveBeenCalledWith(...)` sobre la forma de las queries a Prisma en varios métodos, un caso de `descripcion` solo-espacios (probaba que hacía falta `.trim()`), y el recorte/default-null de `notaCierre`. **2 sobrevivientes documentados, no forzados** (mismo criterio que el protocolo permite): el texto literal de los dos `$queryRaw` de lock — estructuralmente imposible de matar a nivel de mock unitario (el mock ignora el contenido de la query), pero el comportamiento real que protegen ya está probado empíricamente por el test de concurrencia de la fase 07. Revisado sin hallazgos nuevos: IDOR (no aplica, RN-2, sesión compartida sin noción de "propia"), manipulación de IDs (404 limpio), autorización server-side (`esOwner` siempre derivado de `user.rol`, nunca del cliente), mass assignment (`ValidationPipe` global), SQL injection (Prisma parametrizado), doble click en UI (botones deshabilitados, sin cambios desde T3.7). 242/242 unitarios (+16 desde la fase 07), 209/209 integración (+3, 3 corridas seguidas sin flakes), `tsc --noEmit`, lint y build (backend y frontend) limpios. Verificación manual en navegador (Browser pane) del Hallazgo 2 antes y después del fix. **No se declara el módulo aprobado** — sigue la auditoría de seguridad (fase 09), sin la cual no puede pasar a producción. | agente |
| 2026-08-24 | Módulo `cash-registers` — Fase 09 (Security audit) | VERDE | `state/reports/modulo-cash-registers-secaudit-2026-08-24.md`, rama `fase09-secaudit-cash-registers` (ramificada de `fase08-qa-cash-registers`) — **sin código modificado** (regla explícita de la fase). Recorridas las 20 categorías del checklist con verificación empírica contra el servidor real donde fue posible, no solo lectura de código: autenticación (401 real sin cookie y con JWT inválido en 3 rutas del módulo), CSRF (415 real contra `POST /sessions` con `Content-Type: application/x-www-form-urlencoded`, confirmado específicamente en una ruta de este módulo, no asumido desde `auth`), headers de `helmet`/ausencia de `X-Powered-By` (confirmados en una respuesta real del módulo). **Sin CRITICAL ni HIGH.** RN-6 ("cierre a ciegas", el punto central de seguridad del módulo) reconfirmado: `montoSistema`/`diferencia` genuinamente ausentes del JSON para SELLER (no `null`/`0`), sin ninguna ruta que los filtre. **2 observaciones LOW, ninguna bloqueante:** longitud del header `Idempotency-Key` sin cota (infraestructura compartida de `common/idempotency/`, fuera del alcance de este módulo); ausencia de rate limiting en `/movements/ingreso`/`retiro` (`OWNER`-only, ya requiere sesión autenticada — mismo criterio ya aplicado en `products`/`stock`, que tampoco lo tienen, no es una inconsistencia nueva). **Deuda técnica reconfirmada, no nueva:** `npm audit --omit=dev` sigue en 3 high, misma cadena de `prisma` CLI ya documentada como TD-9 — confirmado que ningún commit de T3.1–T3.7 ni de las fases 07/08 tocó `package.json`. **No se declara el módulo seguro de forma permanente** — sin nada que remediar, la fase 10 no tiene trabajo, pero la fase 11 (re-auditoría) igual corresponde: `cash-registers` está clasificado **ALTO** en `MVP_SCOPE.md` §5 ("Dinero"), mismo criterio que bloqueó saltear la fase 11 en `products`. | agente |
| 2026-08-24 | Módulo `cash-registers` — Fase 10 (Security remediation) | VERDE | rama `fase10-secremediation-cash-registers` (ramificada de `fase09-secaudit-cash-registers`) — la fase 09 no encontró ningún CRITICAL/HIGH/MEDIUM que corregir, solo 2 LOW (longitud de `Idempotency-Key` sin cota; sin rate limiting en movimientos manuales), así que no hubo ningún fix de código dentro del alcance de esta fase (`docs/build-protocol/10-security-remediation.md`: "Corregí exclusivamente las vulnerabilidades identificadas") — mismo caso que la fase 10 de `products`. Sin código modificado, sin tests nuevos, sin regresión que correr. **Lo que sí cerró esta fase:** las 2 LOW de la fase 09 no habían quedado registradas en `state/TECH_DEBT.md` todavía (`QUALITY_GATE.md`, sección "Deuda técnica": "Todo MEDIUM/LOW aceptado está registrado... con responsable y motivo") — agregadas como **TD-11** (Idempotency-Key sin cota) y **TD-12** (sin rate limiting en ingreso/retiro), cerrando ese ítem del gate. **Esto NO habilita omitir la Fase 11**: `cash-registers` está clasificado **ALTO** en `MVP_SCOPE.md` — se declara VERDE porque no queda ninguna vulnerabilidad pendiente de corregir, no porque la fase se haya saltado. | agente |
| 2026-08-24 | Módulo `cash-registers` — Fase 11 (Security re-audit) | VERDE | `state/reports/modulo-cash-registers-secreaudit-2026-08-24.md`, rama `fase11-secreaudit-cash-registers` (ramificada de `fase10-secremediation-cash-registers`) — **sin archivos modificados** (regla de la fase), solo verificación independiente. Confirmado primero que no hubo ningún commit de código entre la fase 09 (`6c01a42`) y esta verificación — la fase 10 fue enteramente documentación (`TECH_DEBT.md`/`STATUS.md`). Las 2 LOW de la fase 09 (TD-11, TD-12) siguen exactamente igual, ahora con seguimiento formal, ninguna bloqueante. Verificaciones empíricas nuevas contra el servidor real (no solo relectura del reporte anterior): autenticación (401 real sin cookie), CSRF (415 real contra `POST /sessions` con body form-urlencoded), headers de `helmet`/ausencia de `X-Powered-By` — los tres reconfirmados con el mismo resultado que la fase 09; los 12 tests de integración de RN-6 (cierre a ciegas, ocultamiento de `montoSistema`/`diferencia` para SELLER) re-ejecutados en vivo, 12/12 verde; los 3 tests de integración de los dos hallazgos de la fase 08 (validación de magnitud/signo de montos) re-ejecutados, 3/3 verde — confirma que esos fixes siguen vigentes, sin revertirse ni debilitarse en ningún commit posterior. Sin CRITICAL ni HIGH nuevos. **No se declara el módulo seguro de forma permanente** — el módulo queda en condiciones de avanzar a la fase 12 (production readiness) cuando corresponda. | agente |
| 2026-08-24 | Módulo `cash-registers` — Fase 12 (Production readiness) | VERDE — **PRODUCTION READY: YES** | `state/reports/modulo-cash-registers-prodready-2026-08-24.md`, rama `fase12-prodready-cash-registers` (ramificada de `fase11-secreaudit-cash-registers`) — sin código modificado (regla de la fase). Security/Reliability/Performance/Code quality/Observability/Deployment revisados contra el checklist, todo corrido de nuevo (no reciclado de fases anteriores): `tsc --noEmit`, lint y build (backend y frontend) verdes; 242/242 unitarios, 209/209 integración (×3 sin flakes), 31/31 Vitest; cobertura de `cash-register.service.ts` (solo unitarios) 100% statements/lines/functions, 88.46% branches; `npx prisma migrate status` sin pendientes; sin N+1 en ningún endpoint HTTP (5 queries totales en el servicio, ninguna dentro de un loop); sin `TODO`/`FIXME`/código muerto. **Hallazgo de performance menor, no bloqueante:** `reconciliar()` escanea todas las sesiones `CERRADA`/movimientos sin paginar, pero no está expuesto por ningún endpoint HTTP (confirmado, sin match en el controller) — es una función de mantenimiento interna, riesgo menor que `GET /users` (TD-5), no se registra como TD nuevo. Sin hallazgos nuevos de seguridad (RN-6 reconfirmado una vez más). Riesgos remanentes: TD-11/TD-12 (LOW, propias de este módulo), los 2 endpoints GET documentados en la spec sin ticket (ya señalados en fase 07), y TD-3/TD-4 de `auth` que siguen aplicando de forma transversal. **Módulo `cash-registers` aprobado — pasa a la fase 13 (Integration Audit).** | agente |
| 2026-08-24 | Módulo `sales` — Fase 06 (Spec del módulo) | **BLOQUEADO** | `state/reports/modulo-sales-spec.md` — spec completa (11 secciones) sobre BLUEPRINT AD-3/5/6/8/9/10/14/18/19, §3.4, §5.1, §5.3, invariantes 3/4/5/6/7/9/10/12/13/15, §7, §9.3, §9.4, §9.7, §9.8, §12.1, §12.4, §12.6; `MVP_SCOPE.md` (riesgo ALTO); dependencias `stock`/`cash-registers`/`settings` confirmadas VERDE. **1 ambigüedad nueva agregada a `state/AMBIGUITIES.md`:** AMB-14 (⚠️ ALTO — mecanismo de autorización de `OWNER` para un descuento por encima del tope, en el momento de la venta; el número del tope ya está resuelto por AMB-3, falta el *cómo*; recomendación: contraseña de supervisor verificada sin cambiar la sesión activa; bloquea solo a T4.3). **Hallazgo técnico bloqueante, no de negocio, el más importante de esta fase:** `stock.service.ts` (`products`/`stock`, ya VERDE desde la fase 12) no expone lo que `sales` necesita para descontar/revertir stock — su propia spec (fase 06 de `products`/`variants`) había reservado `descontarPorVenta`/`revertirPorAnulacion` explícitamente para este momento, pero nunca se construyeron, y la fase 07 de ese módulo no lo detectó. Asignado directamente (no es ambigüedad): `descontarPorVenta` a **T4.1**, `revertirPorAnulacion` a **T4.7**, con firmas exactas y la regla de que ninguno de los dos toma su propio lock (lo toma `sales.service` una sola vez, ordenado por id, sobre todas las variantes de la venta — BLUEPRINT §9.4 literal). **Dos hallazgos técnicos menores, incorporados a tickets existentes:** el invariante 4 (`total >= 0`) no se sigue automáticamente de las otras tres reglas — un `ajuste_redondeo` negativo puede violarlo aunque `descuento_total` sea válido (ejemplo numérico verificado en la spec) — validación explícita + `CHECK` recomendado, asignado a T4.6; y el chequeo de "sesión de caja abierta" no toma lock hasta que se registra el movimiento de caja, que no ocurre en una venta 100% tarjeta — ventana de concurrencia angosta mitigada tomando el lock siempre desde el paso 1, asignado a T4.1. Contrastado además contra el ROADMAP: los 11 tickets ya listados (T4.1–T4.11) cubren el módulo completo, sin necesidad de tickets nuevos — los tres hallazgos de arriba son ajustes de alcance dentro de tickets existentes, agregados también como notas en `ROADMAP.md`. **Módulo bloqueado hasta resolver AMB-14 (solo afecta a T4.3); T4.1, T4.2, T4.4–T4.11 pueden arrancar ya**, incorporando el trabajo adicional de `stock.service.ts` y los dos `CHECK` recomendados. | agente |
| 2026-08-24 | Fase 04a — T4.1 (`sales.service`), tests primero | ROJO (esperado) | commit `6d12f89` en rama `feat/T4.1-sales-venta-base` (ramificada de `fase06-spec-sales`), sesión aislada (subagente sin visibilidad del resto de la conversación) — no se leyó ninguna implementación de `src/modules/` salvo import de tipo de `stock/stock.service.ts` y `cash-registers/cash-register.service.ts` (nunca su contenido), solo `ROADMAP.md` (T4.1 y las notas de hallazgos técnicos de la fase 06), `BLUEPRINT.md` (AD-3/4/5/8/9/10/14/18/19, §3.4, §5.3, invariantes 3/4/5/6/7/9/10/12/13/15, §7, §9.3, §9.4, §9.7) y `state/reports/modulo-sales-spec.md` (RN-1 a RN-10, secciones 3/4.2/4.3/5/6/9), más la ESTRUCTURA mecánica (no la lógica) de los tests de `cash-registers`. **Contrato de `SalesService` definido en esta sesión** (no existía): `crearVenta(tx, { userId, items, payments })`, sin `discounts`/`ajusteRedondeo` explícitos todavía (T4.3/T4.6) — con ambos fijos en 0, `neto_linea = subtotal_linea` coincide exactamente con lo que el prorrateo general daría en este caso, sin reimplementarlo. **21 casos** — 13 unitarios (`sales.service.spec.ts`, Prisma mockeado: camino feliz efectivo/tarjeta/mixto, varios pagos EFECTIVO se suman en un solo movimiento de caja, RN-7 —agregación de cantidad por variante para validar, pero un `descontarPorVenta` por línea, BLUEPRINT §5.3 literal—, `permitir_venta_sin_stock`, invariante 3, el lock de sesión de caja tomado SIEMPRE aunque no haya pago en efectivo —hallazgo real de la fase 06—, lock de variantes ordenado por id antes de leer stock; 1 `it.todo` documentado para "total negativo" del invariante 4, no alcanzable en T4.1 con `ajuste_redondeo` fijo en 0, señalado para T4.6) + 8 de integración (`sales.integration.spec.ts`, Postgres real, instanciando `SalesService` directo sin controller —no es parte del alcance de T4.1—: camino feliz con las 5 tablas verificadas coherentes entre sí, venta 100% tarjeta sin `cash_movements`, RN-7 con dos líneas de la misma variante, tres rechazos con rollback completo verificado consultando la base, **concurrencia T4.9** explícita —última unidad, 5 iteraciones—, y **concurrencia del lock temprano de caja** —venta 100% tarjeta vs. cierre simultáneo, 5 iteraciones, mismo patrón que el test de la fase 07 de `cash-registers`). **21/21 en rojo confirmados**, todos por `Cannot find module` (ni `SalesService` ni `stock.service.descontarPorVenta` existen todavía), resto de la suite (242 tests) sin tocar. Sin ambigüedades nuevas — AMB-14 no afecta a este ticket. | agente |
| 2026-08-24 | T4.1 — Servicio de venta transaccional (`sales`) | VERDE | commit pendiente en rama `feat/T4.1-sales-venta-base` (ramificada de `fase06-spec-sales`) — implementado contra los 21 tests de la fase 04a **sin modificar ninguna aserción existente** (solo 2 arreglos de tooling: el tipo local de un test de concurrencia (`{id: number}` → `Sale` real, ya que la sesión aislada no conocía el shape del retorno) y varios `jest.Mock` sin tipar en `sales.service.spec.ts` que disparaban `no-unsafe-*` de lint una vez que el servicio real existía). **Hallazgo técnico central de la fase 06, cerrado en este ticket:** `stock.service.ts` (`products`/`stock`) no exponía `descontarPorVenta` — agregado con la firma exacta de la spec (`tipo: VENTA`, `referenciaTipo: SALE`, sin lock propio — lo toma `sales.service` una sola vez por toda la venta, ordenado por id, antes de llamar a este método por línea), con 5 tests unitarios nuevos escritos en esta misma fase (no en una sesión aislada aparte: el contrato ya estaba fijado por la spec del módulo y por los mocks de la propia fase 04a de T4.1, sin margen para "inventar" el comportamiento). `SalesService.crearVenta` sigue el flujo de 11 pasos documentado por la fase 04a: sesión abierta + lock SIEMPRE (hallazgo de concurrencia de la spec) → agregación de cantidad por variante (RN-7) → lock de variantes ordenado por id (BLUEPRINT §9.4 literal) → validación de stock (`permitir_venta_sin_stock`) → congelado de precio/costo por línea (`neto_linea = subtotal_linea` porque `descuento_total`/`ajuste_redondeo` están fijos en 0, matemáticamente equivalente al prorrateo general en este caso) → validación `SUM(payments) == total` (invariante 3) → creación nested de `sale`+`items`+`payments` → `descontarPorVenta` una vez por línea (no por variante agregada, BLUEPRINT §5.3 literal) → `cash-register.service.registrarMovimiento` solo por la suma de pagos EFECTIVO (invariante 7). `descripcionSnapshot` queda como `"Variante #{id}"` — valor honesto y no vacío sin inventar datos que T4.2 recién va a construir (nombre+talle+color). **Hallazgo real de infraestructura de test, no de este ticket, pero necesario para que la suite completa fuera confiable:** `sales.integration.spec.ts` y `cash-registers.integration.spec.ts` corriendo en workers paralelos de Jest colisionaban contra el índice único global de "una sola sesión de caja abierta" — un problema de concurrencia ENTRE archivos, no dentro de uno, invisible hasta que `sales` fue el segundo archivo en tocar `cash_register_sessions`; corregido agregando `maxWorkers: 1` a `test/jest-integration.json` (test de integración corre serial, correcto para datos compartidos con constraints globales). De paso, el `afterAll` de `sales.integration.spec.ts` (escrito por la fase 04a) tenía el mismo bug de reapertura en lote que ya se había corregido en `cash-registers` — reescrito para procesar una sesión por vez, de punta a punta. Limpiados a mano los datos residuales que las corridas fallidas habían dejado en la base de test (~40 sesiones y sus ventas/movimientos asociados) antes de confirmar la suite estable. **T4.9 (test de concurrencia, ticket separado en el ROADMAP) queda satisfecho como resultado natural de probar correctamente el locking de T4.1** — el test de "dos ventas simultáneas por la última unidad" ya existe y pasa en `sales.integration.spec.ts`; no se marca T4.9 VERDE acá por disciplina de un-ticket-a-la-vez, pero su ejecución futura debería reconocer que el trabajo ya está hecho, no rehacerlo. 222/222 unitarios de `stock`+`sales` (5 nuevos de `descontarPorVenta`) + 14/14 de `sales.service.spec.ts` (261/262 unitarios totales del backend, 1 `it.todo` documentado para T4.6), 217/217 integración (+8 nuevos, 3 corridas seguidas sin flakes tras el fix de paralelismo), `tsc --noEmit` limpio, lint limpio, build verde. T4.2 desbloqueada. | agente |
| 2026-08-24 | Fase 04a — T4.2 (`descripcion_snapshot`, congelado AD-5), tests primero | ROJO (esperado) | commit `5080422` en rama `feat/T4.2-sales-congelado-snapshot` (ramificada de `feat/T4.1-sales-venta-base`), sesión aislada (subagente sin visibilidad del resto de la conversación) — no leyó `sales.service.ts` para decidir comportamiento (solo extrajo la firma pública de `crearVenta`, autorizado explícitamente por la fase 04a), solo `ROADMAP.md` (T4.2), `BLUEPRINT.md` (AD-5, §3.4 —la fila literal de `descripcion_snapshot`—, §5.3) y `state/reports/modulo-sales-spec.md`. **6 casos de integración nuevos** (`sales-snapshot.integration.spec.ts`, Postgres real, instanciando `SalesService` directo): camino feliz (nombre+talle+color); variante sin talle ni color (AD-15, nullable) sin `"null"`/`"undefined"` literal; con talle sin color; con color sin talle; **el más importante — vender, modificar producto/talle/color/precio/costo DESPUÉS, confirmar que la línea vieja no se movió** (AD-5, congelado literal); dos ventas de la misma variante en momentos distintos, cada una reflejando su propio momento. **6/6 en rojo confirmados** por la razón correcta: `descripcion_snapshot` todavía devolvía el placeholder `"Variante #{id}"` de T4.1 — confirmado corriendo los tests, no asumido; verificado aparte que `precio_unitario`/`costo_unitario` ya congelaban bien desde T4.1. **Ambigüedad señalada, no resuelta:** el formato exacto de `descripcion_snapshot` (separador, orden) no está fijado por BLUEPRINT ni por la spec — los tests son agnósticos a eso, solo exigen contención de nombre/talle/color y ausencia de `null` literal. Sin tumbar ningún test viejo (261 unitarios + 217 integración siguen en verde). | agente |
| 2026-08-24 | T4.2 — Congelado formal de `descripcion_snapshot` (`sales`) | VERDE | commit pendiente en rama `feat/T4.2-sales-congelado-snapshot` — implementado contra los 6 tests de la fase 04a **sin modificar ninguna aserción existente**. `tx.variant.findMany` (paso 5 de `crearVenta`) extendido para traer `product.nombre`/`size.nombre`/`color.nombre` además de precio/costo/stock; `descripcionSnapshot` se arma filtrando las partes ausentes (`size`/`color` nullable, AD-15) y uniendo con `" - "` — formato propio, sin especificación de blueprint que lo fije (señalado por la fase 04a, sin bloquear). Reemplaza el placeholder `"Variante #{id}"` de T4.1. **Arreglo de infraestructura de test, no debilitamiento:** el mock `buildVariantRow` de `sales.service.spec.ts` (T4.1) no incluía `product`/`size`/`color` — 11 tests unitarios de T4.1 rompían con "Cannot read properties of undefined" apenas el servicio real empezó a consultar esos campos; agregados al fixture con valores de prueba fijos, sin tocar ninguna aserción (esos tests nunca verificaban el contenido de `descripcionSnapshot` más allá de "no vacío"). 261/262 unitarios (sin cambios de conteo, solo el fixture corregido), 223/223 integración (+6 nuevos, 3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint limpio, build verde. T4.3 sigue esperando AMB-14; T4.4–T4.11 pueden avanzar. | agente |
| 2026-08-25 | AMB-14 — resolución (`sales`) | RESUELTA | `state/AMBIGUITIES.md` — el PO aprobó la recomendación: contraseña de un `OWNER` en el propio formulario de descuento, verificada por el backend contra las credenciales reales (mismo mecanismo de hash que el login) sin emitir cookie nueva ni cambiar la sesión activa de quien está cobrando — nunca se acepta un `autorizado_por_user_id` crudo del body sin verificar nada. **T4.3 desbloqueado.** | agente |
| 2026-08-25 | Alcance de T4.3 achicado a propósito (`sales`) | — | `state/AMBIGUITIES.md` (nota "Construcción diferida" bajo AMB-14) y `state/ROADMAP.md` (nota bajo Etapa 4) — la clienta hoy no tiene empleados, es la única usuaria, siempre `OWNER`, que se autoriza a sí misma trivialmente. T4.3 construye descuentos reales, tope duro y tope del vendedor evaluado solo si `esOwner === false`, pero **sin** el mecanismo de autorización por contraseña de AMB-14 — si un `SELLER` real algún día lo excede, la venta se rechaza sin vía de autorización todavía (bloqueo simple, no bug), y ese mecanismo queda como agregado chico para cuando haga falta. Decisión del PO, no ambigüedad técnica. | agente |
| 2026-08-25 | Fase 04a — T4.3 (`sales.service`, descuentos), tests primero | ROJO (esperado) | commit `099b305` en rama `feat/T4.3-sales-descuentos` (ramificada de `docs/t4-3-alcance-diferido`), sesión aislada (subagente sin visibilidad del resto de la conversación) — no leyó `sales.service.ts`, solo `ROADMAP.md` (T4.3 y la nota de alcance achicado), `BLUEPRINT.md` (§5.3, AD-18, §9.3, invariantes 4/12), `state/reports/modulo-sales-spec.md` (RN-4/RN-5) y `state/AMBIGUITIES.md` (AMB-14 con la nota de construcción diferida) — más `sales.service.spec.ts`/`sales.integration.spec.ts` existentes, autorizados explícitamente como tooling de test, no como lógica de negocio. **Contrato ampliado de `crearVenta` definido en esta sesión:** `discounts?: [...]` y **`esOwner: boolean` obligatorio** (mismo patrón que `cerrarSesion` de `cash-registers` — lo resuelve el futuro controller desde `user.rol`, nunca el cliente). **21 casos nuevos** — 12 unitarios (`sales.service.spec.ts`: tope duro para cualquier rol, tope del vendedor solo si `esOwner === false` —incluido que `esOwner: true` con 50% de descuento ni siquiera consulta `settingsService.getInt`—, dos descuentos del 8% agregados rechazan sobre el total, registro de `sale_discounts` con `autorizadoPorUserId` siempre `null` en este alcance, prorrateo real vía `prorate()`, los dos tests obligatorios de BLUEPRINT §9.3 en el flujo completo, invariante 12) + 9 de integración (`sales-discounts.integration.spec.ts`, Postgres real, mismo patrón que `sales.integration.spec.ts`). **Hallazgo de la propia sesión, corregido antes de cerrar:** dos tests de rechazo de tope estaban diseñados con el pago ya restando el descuento, lo que los hacía pasar "por casualidad" vía el chequeo de pagos existente en vez de probar el tope real — detectado en la primera corrida, corregido armando el pago sobre el monto ingenuo (sin descontar) para que el rechazo solo pueda venir del chequeo de tope. **Ajuste de tooling sobre los 14 tests viejos de T4.1/T4.2:** se les agregó `esOwner: true` (no cambia ninguna aserción, evita que dejen de compilar cuando la Fase 04 haga el campo obligatorio de verdad). **21/21 en rojo confirmados** (reconfirmado por esta sesión: 12/12 unitarios fallan, mayoría por invariante 3 porque el código actual no resta el descuento del total), sin tocar los 262 tests viejos del proyecto. Sin ambigüedades nuevas. | agente |
| 2026-08-25 | T4.3 — Descuentos, prorrateo real (`sales`) | VERDE | commit pendiente en rama `feat/T4.3-sales-descuentos` — implementado contra los 21 tests de la fase 04a **sin modificar ninguna aserción existente** (solo arreglos de tooling: tipo `porcentaje`/`monto` de `DiscountInputT43` ajustado a `monto` opcional, ya que dos tests de la propia fase 04a esperaban que el servicio calculara `monto` a partir de `porcentaje` sin que el caller lo mande — un mismatch real entre mi diseño inicial de la interfaz y lo que la fase 04a ya había fijado, corregido a favor de los tests; y `esOwner: true`/`esOwner: false` agregado a las 10 llamadas de `crearVenta` en los archivos de integración de T4.1/T4.2, que no lo tenían). `crearVenta` ahora: calcula `descuentoTotal` de `discounts[]` (con `applyPercentage` si viene `porcentaje`, nunca confía en un `monto` calculado por el cliente); valida el tope duro `0 ≤ descuentoTotal ≤ subtotal` siempre; evalúa el tope del vendedor (`max_descuento_vendedor_pct`) solo si `esOwner === false` — con `esOwner === true` ni siquiera consulta el setting; usa `prorate()` de `common/money` (T0.12) para `neto_linea`/`neto_unitario` reales en vez del atajo trivial de T4.1/T4.2; escribe `sale_discounts` nested con `autorizadoPorUserId` siempre `null` (AMB-14 diferida). **`CHECK` de base agregado** (asignado a este ticket desde la fase 06): migración `20260825131851_sales_descuento_total_check` — `sales_descuento_total_check CHECK (descuento_total >= 0 AND descuento_total <= subtotal)`, defensa en profundidad igual que el `CHECK` de signo de `cash_movements`. **Hallazgo real evitado, no un bug corregido:** `prisma migrate dev --create-only` propuso, como parte del diff automático, `DROP INDEX variants_product_size_color_key` — un índice único real (`NULLS NOT DISTINCT`, agregado a mano en la migración inicial, nunca representado en `schema.prisma`, mismo patrón que el `CHECK` de signo y el trigger de inmutabilidad) del que depende `variants.service.ts` (T2.3) para traducir variantes duplicadas a 409. Prisma lo veía como "drift" solo porque no está en el schema declarativo — de ningún modo estaba roto en la base real (confirmado con `pg_indexes` antes de tocar nada). Se descartó el diff automático y se escribió la migración a mano con únicamente el `CHECK` nuevo; se aplicó con `prisma migrate deploy` (no `dev`, para no volver a disparar el diff) y se confirmó después que el índice sigue intacto. 274/275 unitarios (+13, 1 `it.todo` heredado de T4.1 sigue pendiente de T4.6), 232/232 integración (+9, 3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint limpio, build verde, `prisma migrate status` sin pendientes. **Etapa 4 sin bloqueos — T4.4 en adelante puede avanzar.** | agente |
| 2026-08-25 | Fase 04a — T4.4 (`sales.service`, pagos), tests primero | ROJO (esperado) | commit `cce0415` en rama `feat/T4.4-sales-pagos` (ramificada de `feat/T4.3-sales-descuentos`), sesión aislada (subagente sin visibilidad del resto de la conversación) — no leyó `sales.service.ts`, solo `ROADMAP.md` (T4.4), `BLUEPRINT.md` (§5.3 paso 5, §3.4, AD-3, AD-8, invariantes 3/7), `state/reports/modulo-sales-spec.md` y `schema.prisma` (modelo `Payment`, enum `PaymentMetodo`). **Dato dado por esta sesión, no descubierto por el subagente:** T4.4 ya estaba mayormente construido como parte necesaria de T4.1 (no se puede validar una venta sin resolver pagos desde el principio) — se le pidió explícitamente verificar a fondo en vez de asumir que había que construir desde cero, y confirmar empíricamente un hallazgo ya sospechado: `payments` tiene `CHECK (monto > 0)` en la base, pero nada en `crearVenta` lo valida antes de escribir — un pago de `$0` (o negativo) combinado con otro que igual complete el total pasa el chequeo de invariante 3 sin problema y explota contra el `CHECK` crudo (`PrismaClientUnknownRequestError`, código Postgres 23514) en vez de un 400 limpio. **14 casos nuevos** — 7 unitarios + 7 de integración (`sales-payments.integration.spec.ts`, nuevo). **Resultado, honesto y explícito:** 10 de los 14 YA PASABAN contra la implementación existente (N pagos de los 4 métodos del enum, mezcla de EFECTIVO con otros, campo `referencia` persistido tal cual o `null`) — confirma que T4.1 efectivamente ya cubría la mayor parte de este ticket. **4 en rojo por la razón real** (2 unitarios + 2 integración): el hallazgo del `CHECK` crudo, reproducido empíricamente contra Postgres real antes de escribir la aserción, no asumido. Sin ambigüedades — la spec ya tenía precedente de criterio para este tipo de gap (`cantidad > 0` en `sale_items`, sección 6). | agente |
| 2026-08-25 | T4.4 — Validación de pagos, cierre del gap de `payments.monto` (`sales`) | VERDE | commit pendiente en rama `feat/T4.4-sales-pagos` — implementado contra los 14 tests de la fase 04a **sin modificar ninguna aserción existente y sin ningún arreglo de tooling** (los 10 tests que ya pasaban siguieron pasando sin tocar nada). Único cambio real: `assertPositive(p.monto, 'El monto de cada pago')` (de `common/money/money.util.ts`, ya existente desde T0.12) por cada pago, ANTES de sumarlos — mismo criterio ya aplicado en el resto del sistema para evitar que un `CHECK` de base llegue sin traducir (`stock.service.ts`, `cash-register.service.ts`). Confirma que T4.4 estaba, en los hechos, prácticamente terminado desde T4.1 — este ticket lo cierra formalmente con verificación real (no solo lo asume) y corrige el único gap genuino que encontró. 281/282 unitarios (+7, 1 `it.todo` heredado sigue pendiente de T4.6), 239/239 integración (+7, 3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint limpio, build verde. T4.5 en adelante puede avanzar. | agente |
| 2026-08-25 | Fase 04a — T4.5 (`sales.service`, idempotencia), tests primero | ROJO (esperado) | commit `7da5315` en rama `feat/T4.5-sales-idempotencia` (ramificada de `feat/T4.4-sales-pagos`), sesión aislada — no leyó `sales.service.ts`, solo `ROADMAP.md` (T4.5), `BLUEPRINT.md` §9.7/AD-10, `state/reports/modulo-sales-spec.md` (RN-9), `schema.prisma` (`sales.idempotency_key`, `@unique` desde la fase 01) y `common/idempotency/idempotency.util.ts`/`idempotency.interceptor.ts` (infraestructura ya VERDE de T0.14, leídos completos como tooling). **Alcance de T4.5 recortado de antemano por esta sesión** (no reinterpretado por el subagente, ver nota en `ROADMAP.md`): `sales` no tiene `SalesController` todavía (T4.1–T4.4 construyeron únicamente `SalesService`), así que no hay ruta HTTP donde aplicar `IdempotencyInterceptor`/`@IdempotencyKey()` de verdad — el ticket se acota a que `crearVenta` acepte `idempotencyKey: string` y lo persista tal cual, probando el mecanismo de punta a punta armando manualmente `withIdempotency(() => prisma.$transaction(tx => crearVenta(tx, input)), ...)` en el test, exactamente como lo haría el futuro controller. **7 casos nuevos** — 2 unitarios (`crearVenta` pasa `idempotencyKey` tal cual a `tx.sale.create`, dos claves distintas no se pisan) + 5 de integración nuevos (`sales-idempotency.integration.spec.ts`, nuevo: mismo `Idempotency-Key` secuencial → misma venta sin duplicar fila/stock/caja; mismo `Idempotency-Key` disparado en simultáneo con `Promise.allSettled` (5 iteraciones) → nunca dos filas; `Idempotency-Key` distinta → dos ventas sin interferencia; 2 tests de constraint de base ejercitada directo por Prisma, sorteando el servicio). **5 en rojo por la razón correcta** (`idempotencyKey` no se persiste todavía — `null` en la base); **2 en verde a propósito**, documentados como regresión permanente del índice único ya existente desde la fase 01, no como caso mal escrito. `tsc --noEmit` limpio, lint limpio, resto de la suite intacto (281/281 unitarios, 239/239 integración). | agente |
| 2026-08-25 | T4.5 — `idempotencyKey` obligatorio en `crearVenta` (`sales`) | VERDE | commit `2b3522f` en rama `feat/T4.5-sales-idempotencia` — implementado contra los 7 tests de la fase 04a **sin modificar ninguna aserción existente**. `CrearVentaInput` gana `idempotencyKey: string` obligatorio, persistido tal cual en `tx.sale.create({ data: { ..., idempotencyKey: input.idempotencyKey } })`. `crearVenta` **no** se envuelve a sí mismo con `withIdempotency` — no es dueño de su propio `tx` (contrato desde T4.1), esa responsabilidad queda para el futuro `SalesController` (T4.10/T4.11), que ya tiene el patrón probado empíricamente en `sales-idempotency.integration.spec.ts`. **Arreglo de tooling esperado, no debilitamiento:** el campo nuevo obligatorio rompía la compilación de ~50 llamadas preexistentes a `crearVenta` en `sales.service.spec.ts` y los 4 archivos de integración de T4.1–T4.4 (que no lo tenían) — se les agregó `idempotencyKey` (valor fijo en los tests unitarios mockeados, `randomUUID()` en los de integración contra Postgres real) sin tocar ninguna aserción existente; se amplió también el tipo local `CrearVentaInputT43` (usado por las sesiones aisladas de T4.3/T4.4) para incluir el campo. 283/283 unitarios (+2), 244/244 integración (+5, 3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint limpio, build verde. T4.6 en adelante puede avanzar. | agente |
| 2026-08-25 | Fase 04a — T4.6 (`sales.service`, ajuste de redondeo), tests primero | ROJO (esperado) | commit `48416e4` en rama `feat/T4.6-sales-redondeo` (ramificada de `feat/T4.5-sales-idempotencia`), sesión aislada — no leyó `sales.service.ts`, solo `ROADMAP.md` (T4.6) y su nota en prosa "Hallazgos técnicos menores (fase 06)" (`total >= 0` no se sigue automáticamente de las otras reglas del invariante 4, asignado a T4.6), `BLUEPRINT.md` §9.3/AD-14, `state/reports/modulo-sales-spec.md` RN-6 y el ejemplo numérico de la sección 3 (`subtotal=$0.50, descuento_total=$0.50, ajuste_redondeo=-$0.90 → total=-$0.90`), y `schema.prisma` (`sales.ajuste_redondeo`, `Decimal(12,2)`, `@default(0)`, ya existente desde la fase 01). Convirtió en test real el `it.todo` que T4.1 había dejado reservado explícitamente para este ticket ("total resultante negativo... queda pendiente de T4.6, que es donde ajuste_redondeo deja de ser fijo"). **14 casos nuevos** — 10 unitarios nuevos + 2 de compatibilidad hacia atrás (pasan hoy a propósito, mismo criterio que la regresión de `esOwner` en T4.3) + 4 de integración (`sales-rounding.integration.spec.ts`, nuevo). **10 en rojo por la razón correcta**: el servicio ignora `ajusteRedondeo` por completo hoy, así que todo lo que depende de que se lea/valide/sume falla contra "Los pagos no cubren el total de la venta" (descuadre, no el error esperado) en vez del mensaje real; incluye el límite `|ajuste_redondeo| < 1`, el cálculo del `total`, el caso motivador de AD-14 (15% sobre $2.999 + ajuste -$0.15 → $2.549 exactos) y que el prorrateo use el total final. **1 test de integración adicional en rojo por una razón distinta y legítima**: el `CHECK` de base recomendado por la fase 06 para `total >= 0` (`sales_total_check`) todavía no existe — un `INSERT` directo con `total < 0` se resuelve sin error, documentado explícitamente como ausencia real de la migración, no trabajo de esta fase. `tsc --noEmit` limpio, resto de la suite intacto (285/295 unitarios en verde — los 10 nuevos en rojo —, 244/244 integración de módulos anteriores sin tocar). | agente |
| 2026-08-25 | T4.6 — Ajuste de redondeo, `total >= 0` explícito (`sales`) | VERDE | commit `efb4258` en rama `feat/T4.6-sales-redondeo` — implementado contra los 14 tests de la fase 04a **sin modificar ninguna aserción existente**. `CrearVentaInput` gana `ajusteRedondeo?: Prisma.Decimal.Value` (opcional, default `0` — compatible con las ~70 llamadas preexistentes de T4.1-T4.5 que no lo mandan). Validación temprana `|ajuste_redondeo| < 1` (RN-6) antes de calcular nada; `total = subtotal - descuento_total + ajuste_redondeo`; y el hallazgo real de la fase 06 cerrado explícitamente: `total < 0` se rechaza aparte, porque `0 ≤ descuento_total ≤ subtotal` y `|ajuste_redondeo| < 1` combinados NO garantizan `total >= 0` por sí solos (ejemplo: subtotal=$0.50, descuento=$0.50, ajuste=-$0.90 → total=-$0.90). El prorrateo (`prorate()`, T4.2/T4.3) ya usaba la variable `total` final — sin cambios ahí, el test de "usa el total FINAL" confirma que no hacía falta tocarlo. **`CHECK` de base agregado** (asignado a este ticket desde la fase 06): migración `20260825151217_sales_total_check` — `sales_total_check CHECK (total >= 0)`, defensa en profundidad igual que `sales_descuento_total_check` (T4.3). **Mismo hallazgo de drift que T4.3, reconfirmado:** `prisma migrate dev --create-only` volvió a proponer `DROP INDEX variants_product_size_color_key` (el mismo índice real, `NULLS NOT DISTINCT`, no representado en `schema.prisma`) — descartado el diff automático otra vez, migración escrita a mano con únicamente el `CHECK` nuevo, aplicada con `prisma migrate deploy`, índice confirmado intacto con `pg_indexes` después. 295/295 unitarios (+12, sin `it.todo` pendientes en `sales`), 248/248 integración (+4, 3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint limpio, build verde, `prisma migrate status` sin pendientes. T4.7 en adelante puede avanzar. | agente |
| 2026-08-25 | Fase 04a — T4.7 (`sales.service`, anulación de venta), tests primero | ROJO (esperado) | commit `89daf51` en rama `feat/T4.7-sales-anulacion` (ramificada de `feat/T4.6-sales-redondeo`), sesión aislada — interrumpida a mitad de tarea por el límite de uso de la sesión y retomada con `SendMessage` tras el reseteo, sin perder contexto ni trabajo ya hecho. No leyó la lógica de negocio de `sales.service.ts` (`anularVenta` no existe todavía ahí); sí leyó `stock/stock.service.ts` completo (permitido explícitamente — este ticket agrega ahí un método nuevo, `revertirPorAnulacion`, nada que "espiar"). Fuentes: `ROADMAP.md` T4.7; `state/reports/modulo-sales-spec.md` RN-8, sección 4.2 (firma de `revertirPorAnulacion`), secciones 5/6/7/8/9; `BLUEPRINT.md` AD-19, invariantes 13/15; `schema.prisma` (`Sale.estado`, `Return`, `CashMovementTipo`/`StockMovementTipo.ANULACION`). Contrato fijado de antemano por esta sesión (no reinterpretado): `anularVenta(tx, { saleId, userId, esOwner })`, mismo patrón de `esOwner` que `crearVenta` — sin `SalesController` todavía, el servicio mismo rechaza si `!esOwner` (CLAUDE.md regla 7). **27 casos nuevos**, todos en rojo por "el método no existe" (`Property 'anularVenta' does not exist` / `TypeError: ... is not a function`) — aceptado explícitamente para este ticket en particular, a diferencia de T4.3/T4.5/T4.6 (que ampliaban un método ya existente): 11 unitarios de `anularVenta` (solo OWNER, venta inexistente, ya anulada, con devolución, con pago `CREDITO_DEVOLUCION`, sesión distinta, camino feliz efectivo/tarjeta/mixto, `estado: ANULADA`, dos líneas de la misma variante) + 5 unitarios de `StockService.revertirPorAnulacion` (mismo estilo que los 5 de `descontarPorVenta` en T4.1) + 9 de integración (`sales-anulacion.integration.spec.ts`, nuevo — incluida la concurrencia explícita: dos `anularVenta` simultáneos sobre la misma venta, 5 iteraciones). Resto de la suite intacto (295/311 unitarios, 248/257 integración — las diferencias son exactamente los casos nuevos en rojo). Sin ambigüedades sin resolver — todo el contrato salió del prompt del ticket. | agente |
| 2026-08-25 | T4.7 — Anulación de venta (`sales`) | VERDE | commit `59499ed` en rama `feat/T4.7-sales-anulacion` — implementado contra los 27 tests de la fase 04a **sin modificar ninguna aserción existente**. `SalesService.anularVenta(tx, input)`: rechaza si `!esOwner` (RN-8, sin leer nada de la base); toma `SELECT id FROM sales WHERE id = $1 FOR UPDATE` (BLUEPRINT §9.4) antes de leer nada más, evitando que dos anulaciones concurrentes de la misma venta reviertan stock/caja dos veces; venta inexistente → 404; ya `ANULADA` → 409; con una `Return` existente (`tx.return.findFirst`) → 409 (AD-19, invariante 13); con algún pago `CREDITO_DEVOLUCION` → 409 (invariante 15); fuera de la sesión de caja `ABIERTA` actual → 409; revierte stock por línea vía el nuevo `StockService.revertirPorAnulacion` (extiende `stock.service.ts`, mismo criterio de asignación que `descontarPorVenta` en T4.1 — sin lock propio, incremento atómico); mueve caja solo por lo cobrado en `EFECTIVO` (invariante 7), con `monto` siempre positivo — `cash-register.service.registrarMovimiento` ya aplica el signo negativo él mismo porque `ANULACION` no está en `TIPOS_POSITIVOS` (confirmado leyendo el archivo, no reimplementado); marca `estado: ANULADA` y devuelve la venta actualizada. La concurrencia de la fase 04a (5 iteraciones, `Promise.allSettled`) confirma empíricamente que el lock de la fila de venta cumple su función: una sola anulación tiene éxito, la otra rechaza con "ya está anulada", el stock nunca queda revertido dos veces. 311/311 unitarios (+16), 257/257 integración (+9, 3 corridas seguidas sin flakes, concurrencia incluida), `tsc --noEmit` limpio, lint limpio, build verde. T4.8 en adelante puede avanzar. | agente |
| 2026-08-25 | Fase 04a — T4.8 (`sales.service`, invariantes), tests primero | ROJO (esperado) | commit `500caa8` en rama `feat/T4.8-sales-invariantes` (ramificada de `feat/T4.7-sales-anulacion`), sesión aislada — no leyó lógica de negocio de `sales.service.ts` (`reconciliar` no existe todavía ahí); leyó `stock.service.ts`/`cash-register.service.ts` solo como referencia CONCEPTUAL de sus propios `reconciliar()` (T2.8/T3.6). **Hallazgo real de esta sesión, no una reinterpretación:** el título de T4.8 ("Tests de invariantes 3, 4, 5 y 7") es engañoso — `BLUEPRINT.md` §6 exige "un chequeo de reconciliación ejecutable" para las tres primeras invariantes (1 `stock`, 2 `cash-registers`, 3 `sales`), y el propio roadmap ya usó el mismo patrón de título dos veces para ese propósito exacto (T2.8 → `StockService.reconciliar()`; T3.6 → `CashRegisterService.reconciliar()`) — T4.8 construye `SalesService.reconciliar()` para el invariante 3, siguiendo el mismo criterio, no una decisión nueva de esta sesión. Además, por indicación explícita de `state/reports/modulo-sales-spec.md` sección 9, se agregaron tests dedicados de los invariantes 12 y 13/15 (fuera de la lista original del ticket, pero "invariantes que este módulo sí garantiza"). **17 casos nuevos**: 7 unitarios de `reconciliar()` (mismo tipo `SalesServiceWithReconciliar` casteado + constructor casteado, mismo truco que T2.8/T3.6, para no romper la compilación de los ~150 tests preexistentes de T4.1-T4.7 en el mismo archivo) + 10 de integración (`sales-invariantes.integration.spec.ts`, nuevo: 3 de `reconciliar()` contra Postgres real, incluido un caso que corrompe `sales.total` a mano por fuera del servicio para confirmar que el chequeo detecta un desajuste real — mismo criterio que T2.8/T3.6 — + 7 tests dedicados de invariantes 4/5/7/12/13/15). **10 en rojo por la razón correcta** (`reconciliar` no existe, `TypeError`). **7 en verde, honesto y esperado:** los tests dedicados de invariantes 4, 5, 7, 12, 13 y 15 pasan contra la implementación existente — T4.1-T4.7 ya los garantizan por construcción; el valor del ticket es el test explícito y dedicado, no un bug encontrado (mismo criterio que T4.4 documentó para sus 10/14 tests que ya pasaban). Resto de la suite intacta: 311/318 unitarios (7 nuevos en rojo, el resto de T4.1-T4.7 sin tocar) y 264/267 integración (3 nuevos en rojo). Sin ambigüedades sin resolver. | agente |
| 2026-08-25 | T4.8 — `reconciliar()` (invariante 3) + tests dedicados de invariantes 4/5/7/12/13/15 (`sales`) | VERDE | commit `4fc49c9` en rama `feat/T4.8-sales-invariantes` — implementado contra los 17 tests de la fase 04a **sin modificar ninguna aserción existente**. `SalesService.reconciliar()`: mismo patrón exacto que `StockService.reconciliar()` (T2.8) y `CashRegisterService.reconciliar()` (T3.6) — `sale.findMany({select:{id,total}})` + `payment.groupBy({by:['saleId'],_sum:{monto:true}})` dentro de una única transacción `REPEATABLE READ` que el propio método abre (única excepción al contrato de "nunca abre su propia transacción", de solo lectura); sin filtro por `estado` — una venta `ANULADA` no toca `payments`/`total` (RN-8), el invariante 3 tiene que seguir cumpliéndose igual para ella, mismo criterio que T2.8 con variantes inactivas (RN-7); devuelve solo las ventas donde `total` no coincide con `SUM(payments.monto)`. Los tests dedicados de invariantes 4/5/7/12/13/15 no requirieron ningún cambio de código — ya pasaban, confirmando que se sostienen por construcción desde T4.1-T4.7. 318/318 unitarios (+7), 267/267 integración (+10, 3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint limpio, build verde. **Etapa 4 sin invariantes pendientes de test explícito.** T4.9 en adelante puede avanzar (T4.9, concurrencia de última unidad, ya satisfecha funcionalmente por los tests de T4.1, según nota propia de esta bitácora — falta declararla VERDE formalmente). | agente |
| 2026-08-25 | T4.9 — Test de concurrencia, última unidad (`sales`) | VERDE | sin commit de código — cierre formal, no reconstrucción. Confirmado leyendo `docs/build-protocol/04-ticket-execution.md` que las dependencias (T4.1) están VERDE, y releyendo la fila de T4.1 en esta misma bitácora (2026-08-24): el test que este ticket pide ("dos ventas simultáneas de la última unidad") ya se escribió y se implementó como parte necesaria de T4.1 — no se puede probar el lock de variantes ordenado por id (BLUEPRINT §9.4) sin ejercitarlo bajo carrera real, así que la fase 04a de T4.1 ya lo incluyó (`describe('T4.9 — concurrencia...')`, literal, en `sales.integration.spec.ts`, 5 iteraciones con `Promise.allSettled`: exactamente una de las dos ventas simultáneas tiene éxito, la otra rechaza con "Stock insuficiente", el stock final queda en 0 con un solo `stock_movement`). Este ticket no reconstruye nada — verifica que la afirmación de T4.1 sigue siendo cierta hoy, no solo la asume: `git status` confirma cero archivos modificados (más allá del `.env.example` preexistente, nunca tocado); el test se corrió aislado 3 veces seguidas (`npx jest -t "T4.9"`, cada corrida ejercitando sus 5 iteraciones internas) sin flakes; la suite de integración completa del backend sigue en 267/267 (confirmado en la corrida de cierre de T4.8, sin cambios desde entonces). `tsc --noEmit`, lint y build no aplican — sin código tocado. **Etapa 4: T4.1–T4.9 todos VERDE.** Sigue T4.10 (pantalla de venta) y T4.11 (pantalla de cobro), los primeros puntos donde existirá un `SalesController`/`SalesModule` real. | agente |
| 2026-08-25 | T4.10 — Pantalla de venta con teclado y lector (`sales`, frontend) | VERDE | commit `fe42b81` en rama `feat/T4.10-venta-pantalla` (ramificada de `feat/T4.9-sales-concurrencia-ultima-unidad`). **Fase 04a NO aplica** (frontend, mismo criterio ya usado en T2.12/T3.7). Alcance real recortado a propósito y documentado en `ROADMAP.md`: BLUEPRINT §12.1 describe venta+cobro como un flujo único, pero sin `SalesController` todavía (T4.1-T4.9 solo `SalesService`) esta pantalla arma el carrito y nunca envía nada al backend — `F2` muestra un aviso honesto de que el cobro es T4.11, con el borrador y su `Idempotency-Key` (generada al entrar, `lib/idempotency.ts` de T3.7 reusado sin cambios) ya listos en `sessionStorage`. `SalePage.tsx` (`/venta`, agregado a `AppLayout` y `router.tsx`): buscador con foco permanente (`GET /variants/search`, T2.7, sin cambios), coincidencia exacta de SKU/código de barras agrega sin confirmación, texto abre lista navegable (flechas + Enter), escanear el mismo artículo incrementa cantidad en vez de duplicar línea; atajos `F4` (modal de descuento propio, porcentaje o monto excluyentes como T4.3), `Ctrl`+`+`/`-`/`Supr` sobre la línea seleccionada, `Esc` cancela con confirmación. Lógica de carrito/persistencia extraída a módulos puros (`cart.ts`, `draft-storage.ts`, mismo patrón que `lib/idempotency.ts`) — plata operada en **centavos enteros** (`number`), nunca `Decimal` fraccionario como `number`: sin librería Decimal en el frontend, la vista previa evita drift de punto flotante con aritmética entera; el cálculo autoritativo sigue siendo `crearVenta` en el backend. **Bug real encontrado y corregido durante la verificación manual en navegador** (no en código estático): `Modal` de Mantine (v9.5.1) no reaccionaba a `opened={estado}` en un componente siempre montado — confirmado con logs de estado de React mostrando el cambio correcto mientras el modal nunca aparecía en el DOM; corregido montando cada modal recién cuando hace falta (`{flag && <Modal opened>...}`, mismo patrón que ya usaba el modal de descuento, que sí funcionaba desde el principio) — aplicado a los 3 modales de la pantalla. Verificación manual completa en navegador real (backend + frontend corriendo, login con el OWNER sembrado): búsqueda por texto, agregado por coincidencia exacta de SKU y de código de barras (incrementa cantidad en el segundo escaneo), descuento por porcentaje aplicado y visible en el total, selección de línea por mouse y por teclado, `Ctrl`+`+` incrementa cantidad, `Ctrl`+`Supr` quita la línea, `Esc` con confirmación limpia el carrito y rota la clave de idempotencia, `F2` muestra el aviso con el total estimado y la clave — y persistencia del borrador confirmada recargando la página real (F5) en varios puntos del flujo. 29 tests nuevos de Vitest (`cart.test.ts`, `draft-storage.test.ts`), 60/60 en verde; `tsc -b`, lint y build (frontend) limpios. T4.11 (pantalla de cobro) es el siguiente punto natural — ahí se construye el `SalesController`/`SalesModule` real y se consume el borrador armado acá. | agente |
| 2026-08-25 | Fase 04a — T4.11 (`sales`, `SalesController` HTTP), tests primero | ROJO (esperado) | commit `2938a78` en rama `feat/T4.11-cobro-backend` (ramificada de `feat/T4.10-venta-pantalla`), sesión aislada — no leyó lógica de negocio de `sales.service.ts` (solo la firma exportada de `CrearVentaInput`/`crearVenta`, permitido explícitamente); leyó `cash-registers.controller.ts`/`cash-registers.module.ts` (T3.3) completos como plantilla MECÁNICA del patrón ya establecido (Roles/CurrentUser/IdempotencyInterceptor/withIdempotency), no como lógica de negocio de `cash-registers`. **Alcance de T4.11 fijado de antemano por esta sesión** (ver nota en `ROADMAP.md`): un solo endpoint, `POST /sales`, sin `@Roles()` (RN-1: "cualquiera autenticado, es el trabajo del vendedor"); `GET /sales`, `GET /sales/:id` y `POST /sales/:id/anular` quedan fuera — están en la tabla de rutas de la spec pero ningún ticket los reserva todavía y la pantalla de cobro no los necesita. `esOwner` se resuelve siempre del rol real de la sesión, nunca del body. **14 casos nuevos** en `sales-controller.integration.spec.ts` (nuevo) — camino feliz (OWNER; SELLER sin restricción de rol; descuento+ajuste de redondeo con total persistido `900.50`), idempotencia (sin header → 400; doble click misma fila; dos claves distintas → dos ventas), errores de negocio ya cubiertos por `crearVenta` pasando intactos hasta HTTP (sin sesión → 409; stock insuficiente → 409 con la cantidad disponible en el mensaje; pagos≠total → 400), validación de DTO (`items` faltante/vacío/con tipos incorrectos → 400), auth (sin cookie → 401; SELLER forzando `esOwner:true` con un descuento del 50% → 400, sin fila `Sale` creada — confirma que el campo falsificado no tiene ningún efecto). **14/14 en rojo confirmados** — cada request a `/sales` responde 404 porque `SalesController`/`SalesModule` no existen ni están registrados en `app.module.ts` (confirmado leyendo ese archivo como wiring, no como lógica de `sales`). Resto de la suite intacto: 267/267 integración, 318/318 unitarios. Sin ambigüedades sin resolver. | agente |
| 2026-08-25 | T4.11 (backend) — `SalesController`, `POST /sales` con idempotencia (`sales`) | VERDE | commit `a0412bf` en rama `feat/T4.11-cobro-backend` — implementado contra los 14 tests de la fase 04a **sin modificar ninguna aserción del archivo de tests**, salvo un arreglo real de infraestructura en su `afterAll`: reabría todas las sesiones de prueba en lote antes de borrarlas (mismo bug de "una sola sesión ABIERTA" ya corregido en T4.1/T4.7, reintroducido porque la sesión aislada de la fase 04a no tiene memoria de ese precedente) — reescrito para procesar una sesión por vez, de punta a punta, y corregido también el orden real de los `DELETE` (`sales.cash_register_session_id` es FK hacia la sesión: hay que borrar las ventas ANTES de poder borrar la sesión que referencian, no después). `CreateSaleDto` (con `SaleItemDto`/`SaleDiscountDto`/`SalePaymentDto` anidados, mismo patrón `@ValidateNested`/`@Type` que `create-variant-grid.dto.ts`) — deliberadamente sin campo `esOwner`: si el cliente lo manda, `forbidNonWhitelisted` (pipe global) lo rechaza con 400 antes de llegar al handler. `SalesController.crear()`: mismo patrón exacto que `POST /cash-registers/movements/ingreso` (T3.3) — `@UseInterceptors(IdempotencyInterceptor)` + `@IdempotencyKey()` + `withIdempotency()` envolviendo `prisma.$transaction(tx => salesService.crearVenta(tx, {...}))`, `esOwner: user.rol === 'OWNER'` resuelto del JWT verificado, nunca del body. `SalesModule` (importa `StockModule`/`CashRegistersModule`/`SettingsModule`, exporta `SalesService`) registrado en `AppModule` — primer controller de `sales`, después de T4.1-T4.9 construir solo el servicio. 318/318 unitarios (sin cambios, ninguna lógica de servicio tocada), 281/281 integración (+14, 3 corridas seguidas sin flakes tras el arreglo del `afterAll`), `tsc --noEmit` limpio, lint limpio, build verde. Sigue la pantalla de cobro (frontend) que consume este endpoint. | agente |
| 2026-08-25 | T4.11 (frontend) — Pantalla de cobro (`sales`) | VERDE | commit `628b3a5` en rama `feat/T4.11-cobro-backend` — **Fase 04a NO aplica** (frontend, mismo criterio que T4.10/T2.12/T3.7). `CobroPage.tsx` (`/venta/cobro`) lee el borrador de `SalePage` (T4.10, mismas claves de `sessionStorage` vía `draft-storage.ts`, ahora exportadas para compartirse entre las dos pantallas) y lo confirma contra `POST /sales`. Foco arranca en el selector de medio de pago (§12.1, literal): teclas `1`-`4` eligen medio Y precargan el importe con el saldo pendiente en el mismo paso (un click de mouse llega al mismo estado que el atajo de teclado, para no dejar un flujo secundario peor que el principal); `Enter`/`Tab` mueve el foco al importe; otro `Enter` confirma esa línea de pago. Con el foco en el importe las teclas numéricas escriben, nunca cambian el medio — el atajo `1`-`4` solo está atado al contenedor del selector. En efectivo, campo opcional "¿Cuánto entregó?" calcula el vuelto en pantalla, nunca viajó al backend (confirmado: el payload de `POST /sales` solo lleva `monto`, no `entregado`). Ajuste de redondeo (AD-14, "lo carga quien cobra") vive acá, no en T4.10 — `computeTotalCents` (`cart.ts`) gana un tercer parámetro opcional `ajusteCents` (default 0, compatible con las llamadas existentes de `SalePage`). "Confirmar venta" se deshabilita apenas se aprieta, usa la misma `Idempotency-Key` que T4.10 ya generaba sin consumir; al confirmar limpia el borrador, rota la clave, notifica (`@mantine/notifications`, primer consumidor real en el proyecto) y vuelve a `/venta` con el carrito vacío. `F2` de `SalePage` ahora navega de verdad a `/venta/cobro` en vez del aviso placeholder de T4.10. Lógica de pagos extraída a `payments.ts` (centavos enteros, mismo criterio que `cart.ts`), testeada con Vitest. **Bug real encontrado y corregido en la verificación manual** (mismo tipo que el de T4.10, en otro lugar): un `<Text>` con un `<Badge>` adentro renderiza `<p>` conteniendo un `<div>`, inválido en HTML — corregido con `component="span"` (acá el texto es inline dentro de una fila, a diferencia del `component="div"` de T4.10). **Verificación manual completa contra Postgres real** (backend + frontend corriendo, sesión de caja real abierta vía API): venta con un ítem, pago en efectivo exacto, y por separado un pago con vuelto ($2.000 entregados sobre $1.800, vuelto $200 mostrado correctamente); intento de cobro sin sesión de caja abierta mostró el 409 real del backend sin perder los pagos ya cargados, reintentado con éxito tras abrir la sesión; confirmado en la base tras cada corrida: `sales`/`sale_items`/`payments` correctos, `stock_actual` descontado, `cash_movement` tipo VENTA generado — datos de prueba limpiados y stock reconciliado al terminar. 71/71 Vitest (+11: `payments.test.ts` nuevo, `cart.test.ts` con 3 casos nuevos de `ajusteCents`), `tsc -b`, lint y build (frontend) limpios. **Etapa 4 completa: T4.1–T4.11, todos VERDE.** | agente |
| 2026-08-25 | Módulo `sales` — Fase 07 (cierre de módulo) | VERDE | commit `cfa2246` en rama `fase07-cierre-sales` — auditoría completa contra `state/reports/modulo-sales-spec.md`. **Funcionalidades de la spec**: implementadas todas las de alcance de T4.1–T4.11 (`crearVenta` con sus 11 pasos, `anularVenta`, `reconciliar` — invariante 3); **faltantes, por decisión de alcance ya documentada en T4.11 y sin ticket que las reserve en `ROADMAP.md`**: `GET /sales`, `GET /sales/:id`, `POST /sales/:id/anular` (el servicio `anularVenta` existe y está probado, pero no tiene ruta HTTP todavía). **Inconsistencia real corregida**: `variantId` inexistente o con `activo: false` devolvía 409 "Stock insuficiente: quedan 0 unidades" en vez del 400 "La variante no existe" que exige la spec (§7, RN-2) — el chequeo de stock (paso 6) usaba `variant?.stockActual ?? 0` como fallback y corría antes de que existiera ningún chequeo real de existencia; con `permitir_venta_sin_stock` en `false` (default de T0.13) el 409 salía siempre primero, y no había ningún test (unitario ni de integración) que cubriera el caso. Corregido con un paso 5b explícito de validación de existencia+actividad, insertado antes del chequeo de stock. **Además**: eliminado un chequeo de existencia redundante que quedó muerto en el paso 7 tras agregar el 5b (reemplazado por aserción no-null), y alineado el texto del error de tope de descuento con la cita literal de la spec ("El descuento no puede superar el subtotal"). **Riesgo detectado y neutralizado antes de romper nada**: el fixture `buildVariantRow()` de `sales.service.spec.ts` no incluía `activo`, lo que hubiera hecho fallar los ~150 tests preexistentes por `undefined` evaluando `!activo === true`; se agregó `activo: true` por default al fixture y al tipo `VariantRow` antes de correr la suite. **Tests agregados**: 3 unitarios (`variantId` inexistente, variante inactiva, mezcla de variante real+inexistente en la misma venta) + 2 de integración contra Postgres real (mismos casos, con rollback completo verificado). **Coherencia entre tickets, duplicación y dead-code**: revisados `sales.controller.ts` y las dependencias hacia `stock.service.ts` — sin hallazgos nuevos (un typo preexistente en `stock.service.ts`, ajeno a `sales`, detectado pero no tocado por estar fuera del módulo). **Resultado de validación**: 321/321 unitarios, 283/283 integración (3 corridas seguidas sin flakes), `tsc --noEmit` limpio, lint limpio, `nest build` limpio; frontend sin impacto (71/71 Vitest, `tsc -b` y lint limpios). **Problemas pendientes**: ninguno bloqueante — las rutas GET/anular siguen fuera de alcance a propósito, a la espera de un ticket futuro que las reserve. No se declara el módulo terminado (queda para Fase 12). | agente |
| 2026-08-25 | Módulo `sales` — Fase 08 (QA adversarial) | VERDE | `state/reports/modulo-sales-qa-2026-08-25.md` — commit pendiente en rama `fase08-qa-sales`. **Hallazgo real (severidad MEDIUM), corregido y probado**: un monto de pago/descuento o una `cantidad` de línea que desborda `Decimal(12,2)` (o `Decimal(5,2)` para `porcentaje`) llegaba crudo a Postgres, que lo rechazaba con "numeric field overflow" (código 22003) — 500 genérico en vez de 400, mismo patrón ya encontrado y corregido de forma independiente en la Fase 08 de `cash-registers`. Reproducido primero contra Postgres real (500 confirmado, chequeo desactivado a propósito), corregido con `assertDentroDePrecision()` local en `sales.service.ts` aplicada en seis puntos (subtotal de línea, subtotal de venta, monto y porcentaje de cada descuento, descuento total, total final, monto de cada pago) — cubre tanto un campo de entrada individual fuera de rango como la SUMA de partes válidas que desborda el agregado (`descuentoTotal`, `total` con `ajusteRedondeo`). **Ambigüedad nueva, no resuelta** (AMB-15 en `state/AMBIGUITIES.md`): `crearVenta` nunca chequea `variant.product.activo` — una variante activa de un producto dado de baja se puede vender igual, aunque el buscador de catálogo (RN-11) la excluye de la búsqueda; sin fuente que confirme si es intencional, queda para el PO. **Testing de mutación (Stryker, obligatorio)**: antes 83.12% (ya sobre el umbral del 80%, pero con gaps reales — la rama de "descuento sin porcentaje ni monto" con CERO tests, `.some()`/`.every()` de `CREDITO_DEVOLUCION` indistinguibles sin un caso de pago mixto, código muerto post-Fase-07 en el chequeo de stock, `descripcionSnapshot` con `size`/`color` null nunca probado, forma exacta de las queries a Prisma sin aserción); después **100.00%** (232 matados, 0 sobrevivientes, 0 sin cobertura, 2 "errores" documentados — mutación de las constantes de precisión que rompe la carga del módulo antes de correr ningún test, no un gap real). **Otras categorías adversariales** (rollback, datos huérfanos, requests duplicados, IDOR, manipulación de IDs, autorización server-side, mass assignment, SQL injection, doble click en la UI, error de servidor no rompe la pantalla): sin hallazgos nuevos, comportamiento ya correcto — detalle completo en el reporte. **Resultado de validación**: 336/336 unitarios, 284/284 integración (Postgres real, confirmado en dos corridas independientes tras un falso negativo por contención de recursos con Stryker corriendo en simultáneo), `tsc --noEmit`/lint/`nest build` limpios. No se declara el módulo terminado (queda para Fase 12). | agente |
| 2026-08-25 | Módulo `sales` — Fase 09 (security audit) | VERDE | `state/reports/modulo-sales-secaudit-2026-08-25.md` — sin código modificado (regla de la fase). Sin CRITICAL. **Un HIGH matizado (CSRF, sección 9)**: `jsonOnlyMiddleware` (global, Fase 10 de `auth`) bloquea el vector de `<form>` HTML nativo contra `POST /sales` — reconfirmado EN VIVO contra el servidor real (`application/x-www-form-urlencoded`/`multipart/form-data` → 415, antes de llegar a `AuthGuard`). El vector de fetch()/XHR cross-origin depende de que `FRONTEND_URL` (CORS) esté configurado como un único origen específico — no hay ningún test automatizado que fije ese contrato ni una defensa independiente de esa variable (`sameSite: 'none'` en producción ya removió la protección nativa del navegador). Frágil, no roto hoy — recomendado para Fase 10: test de integración que fije el rechazo del preflight de CORS contra un origen no autorizado, y evaluar un chequeo de `Origin`/`Referer` independiente. **Otras verificaciones en vivo contra `POST /sales`**: sin cookie → 401; cookie con JWT inválido → 401; ambas confirmadas empíricamente, no solo por código. **Tres observaciones LOW**: sin `@ArrayMaxSize` en `items`/`payments`/`discounts` de `CreateSaleDto` (mismo patrón ya señalado en `cash-registers` para `Idempotency-Key`); sin rate limiting en `POST /sales` (decisión de sistema ya existente, no específica de este módulo); `payments.referencia` sin formato validado (podría contener un número de tarjeta pegado a mano, pero la UI real de `CobroPage.tsx` nunca envía este campo hoy, bajando el riesgo práctico). Reconfirmación de TD-9 (`npm audit` de la cadena `prisma` CLI → `deepmerge-ts`, 3 high, sin cambios) — frontend sin vulnerabilidades. Sin hallazgos en authentication/authorization/access control/privilege escalation/IDOR/SQL injection/XSS/SSRF/path traversal/sensitive info exposure/secrets/logs/error handling/incorrect permissions/unauthorized endpoints. No se declara el módulo seguro de forma permanente — la Fase 10 tiene al menos el hallazgo de CSRF para remediar antes de la Fase 11 (re-auditoría). | agente |
| 2026-08-25 | Módulo `sales` — Fase 10 (security remediation) | VERDE | commit `8ff49aa` en rama `fase10-secremediation-sales` — `state/reports/modulo-sales-secremediation-2026-08-25.md`. Corrige exclusivamente los hallazgos de la Fase 09. **Hallazgo 1 (HIGH matizado, CSRF) corregido**: nuevo `OriginCheckMiddleware` (`common/security/origin-check.middleware.ts`), global en `AppModule.configure()` junto a `helmet()`/`jsonOnlyMiddleware`/`cookieParser()` — rechaza con 403 cualquier request POST/PUT/PATCH/DELETE cuyo header `Origin` (cuando el navegador lo manda) no coincida exactamente con `FRONTEND_URL`, segunda barrera independiente de que la configuración de CORS esté bien puesta. Aplicado global (mismo criterio que `jsonOnlyMiddleware`, que también nació de un hallazgo puntual de `auth`) porque la clase de vulnerabilidad no es específica de `sales`. **Hallazgo 2 (LOW) corregido**: `@ArrayMaxSize(500)` en `items`, `@ArrayMaxSize(20)` en `payments`/`discounts` de `CreateSaleDto` — mismo criterio que `create-variant-grid.dto.ts`. **Dos hallazgos LOW de la Fase 09 documentados como aceptados, no corregidos a propósito** (fuera del alcance de esta fase, regla explícita "no modifiques problemas no relacionados"): `referencia` de pago sin formato validado (corregirlo inventaría una regla de negocio que no existe); sin rate limiting en `POST /sales` (decisión de sistema ya existente, transversal a varios módulos). **Tests**: 346/346 unitarios (+10, `origin-check.middleware.spec.ts`), 288/288 integración (+4: `Origin` cross-site → 403; `Origin` real → 201; más de 500 `items` → 400; más de 20 `payments` → 400) — ningún test existente eliminado ni debilitado. **Regresión**: suite completa (26 archivos de integración, todos los módulos) corrida en verde después de cada fix. `tsc --noEmit`/lint/`nest build` limpios. **No se declara el módulo seguro** — corresponde a la Fase 11 confirmarlo de forma independiente, incluida una reconfirmación en vivo de `OriginCheckMiddleware` contra un despliegue real con `sameSite: 'none'` (verificado acá solo con `FRONTEND_URL` del entorno de test). | agente |
| 2026-08-25 | Módulo `sales` — Fase 11 (security re-audit) | VERDE | `state/reports/modulo-sales-secreaudit-2026-08-25.md` — sin archivos modificados (regla de la fase). Confirmado que entre la Fase 09 y la Fase 10 hubo exactamente un commit de código (`8ff49aa`), sin cambios adicionales no declarados. **Hallazgo 1 (HIGH matizado, CSRF) reconfirmado corregido EN VIVO**: `curl -X POST /sales -H "Origin: https://evil.example.com"` contra el servidor real → `403 "Origen no autorizado"` (antes del fix, en la Fase 09, ese mismo request pasaba hasta `AuthGuard`). Reconfirmado que el camino legítimo sigue intacto: `Origin` igual a `FRONTEND_URL` real y sin `Origin` en absoluto dan el mismo resultado que antes del fix (401 sin cookie, sin ningún falso positivo nuevo). 12/12 tests específicos del middleware (10 unitarios + 2 de integración) corridos de nuevo, verde. **Hallazgo 2 (LOW) reconfirmado corregido**: `@ArrayMaxSize` presente en el DTO actual, 2/2 tests de integración verdes. **Sin hallazgos nuevos** — el orden de registro de `OriginCheckMiddleware` no genera bypass (métodos no mutantes y el preflight `OPTIONS` real pasan sin tocar el chequeo, reconfirmado en vivo); headers de seguridad (`X-Content-Type-Options`, `X-Frame-Options`, `X-Powered-By` ausente) reconfirmados en vivo. **Regresión completa desde cero**: 346/346 unitarios, 288/288 integración (Postgres real, 26 archivos, todos los módulos). Las dos LOW restantes (sin rate limiting, `referencia` sin formato) siguen documentadas, sin agravarse, no bloqueantes. **Sin CRITICAL ni HIGH.** El módulo queda en condiciones de avanzar a la Fase 12 cuando corresponda — no se declara "listo para producción" acá. | agente |
| 2026-08-25 | Módulo `sales` — Fase 12 (production readiness) | VERDE | `state/reports/modulo-sales-prodready-2026-08-25.md` — sin código modificado (regla de la fase); única escritura fue bookkeeping (TD-13/TD-14 en `state/TECH_DEBT.md`, requisito de `QUALITY_GATE.md` que había quedado pendiente desde la Fase 10, corregido acá). **PRODUCTION READY: YES.** Sin CRITICAL ni HIGH (el HIGH matizado de CSRF de la Fase 09 sigue corregido y reconfirmado). Seguridad, reliability (rollback/locks reconfirmados), performance (sin N+1 accidental — el patrón "un stock_movement por línea" es requisito literal del blueprint, acotado por `@ArrayMaxSize(500)` desde la Fase 10; `reconciliar()` sin paginar pero no expuesto por HTTP, mismo criterio ya aceptado en `cash-registers`), code quality (sin duplicación/TODO/dead-code, mutación 100.00% en `sales.service.ts`), observability y deployment revisados de punta a punta, todo corrido de nuevo (no reciclado). **Tests**: 346/346 unitarios, 288/288 integración, 71/71 frontend — todo verde. Build backend y frontend limpios (bundle 642.92 kB/195.60 kB gzip, creció de forma esperable desde el baseline de `products`, TD-10 sigue igual, sin TD nuevo). Migraciones al día. **2 LOW nuevas registradas** (TD-13, `referencia` de pago sin formato; TD-14, sin rate limiting en `POST /sales`) más las transversales ya conocidas (TD-7/TD-9/TD-10/TD-11/TD-12). **Riesgos restantes documentados, ninguno bloqueante**: AMB-15 pendiente de resolución del PO; `GET /sales`/`GET /sales/:id`/`POST /sales/:id/anular` sin ruta HTTP (gap de alcance ya señalado en la Fase 07). **Módulo `sales` aprobado — pasa a la Fase 13 (Integration Audit).** | agente |
| 2026-08-25 | Módulo `returns` — Fase 06 (Spec del módulo) | **BLOQUEADO** | `state/reports/modulo-returns-spec.md` — spec completa (11 secciones) sobre BLUEPRINT AD-8/AD-17/AD-18/AD-19, §3.5, §5.4, §5.6, invariantes 7/8/10/11/13/14/15, §7, §9.3, §9.4, §9.7, §9.8, §12.4, §12.6; `MVP_SCOPE.md` (riesgo ALTO); dependencias `sales`/`settings`/`idempotencia`/`cash-registers` confirmadas VERDE. **1 ambigüedad nueva agregada a `state/AMBIGUITIES.md`:** AMB-16 (⚠️ ALTO — ¿el crédito `CREDITO_DEVOLUCION` de una devolución se consume solo dentro del mismo flujo atómico de cambio, o queda disponible como nota de crédito para una venta separada más adelante?; recomendación: solo atómico, coherente con AD-17; bloquea solo a T5.5). **Hallazgo técnico, no de negocio:** T5.5 (`CAMBIO`) reusa `SalesService.crearVenta` tal cual para la venta nueva, pero `CrearVentaPaymentInput` no tiene campo `returnId` — se agrega como cambio mínimo y aditivo dentro de la propia sesión de T5.5 (no un ticket aparte), sin romper tests existentes; el chequeo del invariante 14 vive en `returns.service.ts`, nunca en `sales` (evita dependencia circular). **Decisión de arquitectura de esta sesión (no ambigüedad de negocio):** `returns` expone su propio `GET /returns/sales/:numero` para buscar una venta y ver qué queda disponible por línea — no se reabre `SalesController` (ya cerrado, Fase 12) para esto. Contrastado contra el ROADMAP: los 7 tickets ya listados (T5.1–T5.7) cubren el módulo completo, sin necesidad de tickets nuevos — el ajuste de `sales.service.ts` se agregó como nota en `ROADMAP.md`, dentro de T5.5. **Módulo bloqueado hasta resolver AMB-16 (solo afecta a T5.5); T5.1–T5.4 y T5.6 pueden arrancar ya** (T5.7 depende de T5.5, así que sigue esa cadena). | agente |
| 2026-08-25 | AMB-16 — resolución (`returns`) | RESUELTA | `state/AMBIGUITIES.md` — el PO **rechazó la recomendación** ("solo atómico") y confirmó lo opuesto: "debería permitir, sería como una nota de crédito" — el crédito de una devolución queda disponible para una venta futura, sin relación con el momento ni la sesión que lo generó. **Alcance ampliado a propósito, no un simple ajuste:** la validación del invariante 14 deja de poder vivir solo en `returns.service.ts` (flujo atómico) — pasa a vivir en `SalesService.crearVenta` misma, con su propio lock sobre la fila de `Return` (mismo patrón BLUEPRINT §9.4), porque cualquier `POST /sales` futuro, sin relación con `returns`, puede intentar gastar un crédito viejo. **Ticket nuevo agregado a `state/ROADMAP.md`: T5.8** — "Aplicar crédito de una devolución a una venta posterior", con `GET /returns/:numero/credito` (nuevo, dueño `returns`) + una quinta opción de medio de pago en `CobroPage.tsx` (dueño `sales`, ya cerrado en Fase 12 — extensión aditiva). `state/reports/modulo-returns-spec.md` actualizado en las secciones 1/4/5/6/7/9/10/11 para reflejar el mecanismo final. **T5.5 desbloqueado — la Etapa 5 completa (T5.1–T5.8) puede arrancar siguiendo el orden de dependencias.** | agente |
| 2026-08-25 | Fase 04a — T5.1 (`returns.service`), tests primero | ROJO (esperado) | commit `19c9274` en rama `feat/T5.1-returns-devolucion-base` (ramificada de `fase06-spec-returns`), sesión aislada (subagente sin visibilidad del resto de la conversación) — no se leyó ninguna implementación de `src/modules/`, solo `ROADMAP.md` (T5.1 y Etapa 5), `BLUEPRINT.md` (AD-8/18/19, §3.5, §5.4 ignorando `CAMBIO`, invariantes 7/8/10/11/13, §9.3/9.4/9.7) y `state/reports/modulo-returns-spec.md` (RN-1 a RN-8, secciones 3/5/6/7/9). **Contrato de `ReturnsService` definido en esta sesión**: `crearDevolucion(tx, { saleId, items: [{saleItemId, cantidad, reingresaStock}], returnPayments: [{metodo, monto, referencia?}], userId, esOwner, idempotencyKey }): Promise<Return>` — sin `StockService` en el constructor (T5.1 no reingresa stock, eso es T5.2) ni campo `tipo` en el input (sin `CAMBIO`, eso es T5.5). **26 casos** — 17 unitarios (Prisma mockeado: 3 caminos felices —efectivo/tarjeta/mixto—, los DOS tests obligatorios de AD-18 —proporcional al neto original, y remanente exacto en la devolución que agota una línea—, rechazos de venta inexistente/anulada/cantidad excedida —request único y acumulado—/fuera de plazo sin autorización/sin sesión de caja/suma de reintegros, aceptación fuera de plazo con autorización, idempotencia, y los locks de sesión y `sale_items` de BLUEPRINT §9.4) + 9 de integración (Postgres real, ventas fixture creadas con `SalesService` real para tener `neto_linea` genuino, no inventado a mano). Verificado explícitamente en cada test que NINGUNA llamada espera un servicio de stock ni un `cash_movement` (T5.2/T5.3, fuera de alcance). **26/26 en rojo confirmados**, todos por `Cannot find module` (`ReturnsService` no existe), resto de la suite (346 unitarios/288 integración) sin tocar. Sin ambigüedades nuevas. | agente |
| 2026-08-25 | T5.1 — Devolución simple contra venta existente (`returns`) | VERDE | commit `bdfd394` en rama `feat/T5.1-returns-devolucion-base` — implementado contra los 26 tests de la Fase 04a **sin modificar ninguna aserción existente**. `ReturnsService.crearDevolucion` valida en orden: sesión de caja abierta (lock siempre, RN-2); venta existente y no `ANULADA` (RN-1/AD-19, `ConflictException` 409); lock de `sale_items` involucrados ordenado por id (BLUEPRINT §9.4); plazo de devolución con autorización de `OWNER` fuera de término (RN-3, AMB-2 RESUELTA); tope por línea considerando devoluciones previas acumuladas (RN-4/invariante 8); neto devuelto proporcional al `neto_linea` ORIGINAL, con remanente exacto en la devolución que agota la línea (RN-5/AD-18, los dos tests obligatorios); suma de reintegros exacta contra `total_devuelto` (RN-7/invariante 11), antes de escribir nada. **Hallazgo real, corregido en esta sesión (no en Fase 07/08, para no dejarlo pasar):** un reintento idempotente (`withIdempotency`, mismo patrón que `sales`/`cash-registers`) podía revalidar "cantidad disponible" contra el estado YA consumido por el primer intento exitoso — a diferencia de una venta, donde un reintento casi siempre encuentra stock de sobra, una devolución puede consumir EXACTO lo último disponible de una línea, así que el reintento veía "0 disponible" y rechazaba con un 400 de negocio ANTES de llegar al `create()` cuya violación de unicidad es lo único que `withIdempotency` detecta. Corregido con un cortocircuito explícito al principio de `crearDevolucion`: si ya existe una devolución con esa `idempotencyKey`, se devuelve tal cual, sin revalidar nada. **Arreglo de infraestructura de test** (mismo criterio que T4.2 de `sales`): `MockTx.return` ganó `findUnique` (default `null`) para que el mock soporte el nuevo chequeo — ninguna aserción existente cambió. 17/17 unitarios + 9/9 integración del ticket; 363/363 unitarios y 297/297 integración de la suite completa (Postgres real), `tsc --noEmit`/lint/`nest build` limpios. Alcance de T5.1 respetado: sin `tipo = CAMBIO` (T5.5), sin reingreso real de stock (T5.2), sin movimiento de caja real (T5.3), sin concurrencia real (T5.6). **T5.2 y T5.6 pueden avanzar ya.** | agente |
| 2026-08-25 | Fase 04a — T5.2 (`stock`/`returns`), tests primero | ROJO (esperado) | commit `fbe0a50` en rama `feat/T5.2-returns-reingreso-stock` (ramificada de T5.1 VERDE), sesión aislada — no se leyó lógica de negocio de `stock.service.ts`/`returns.service.ts`. **Contrato nuevo definido en esta sesión, desde `state/reports/modulo-returns-spec.md` sección 5 paso 12**: `StockService.reingresarPorDevolucion(tx, { variantId, cantidad, returnId, userId })` — delta positivo, `tipo: DEVOLUCION`, `referenciaTipo: RETURN`, sin lock propio, nunca falla por poco stock (mismo criterio que `revertirPorAnulacion`). `ReturnsService` gana `StockService` como cuarto colaborador. **12 casos nuevos** — 5 en `stock.service.spec.ts` (camino feliz, nunca falla con stock bajo/en 0, sin lock propio, dos reingresos independientes) + 5 en `returns.service.spec.ts` (delega una vez por línea con `reingresaStock: true`, nunca para `false`, orden después de `tx.return.create`, nunca si la devolución se rechaza) + 3 de integración (Postgres real: camino feliz, dos líneas reingresando solo una, rollback sin `stock_movement`). **2 aserciones de T5.1 actualizadas a propósito** (documentaban correctamente el alcance ACOTADO de T5.1, ahora superadas por T5.2): en el test de "camino feliz" de `returns.integration.spec.ts`, `stockMovs` pasa de esperar longitud 0 a esperar 1 (`tipo: DEVOLUCION`), y `stockActual` pasa de esperar 8 a esperar 10 (reingresado) — **ninguna otra aserción de los 17+9 tests de T5.1 se tocó**, incluida la infraestructura de mocks (`SaleItemRow.variantId`, `Deps.stockService`) agregada sin cambiar ningún resultado esperado viejo. Confirmado en rojo por ausencia de implementación (`tsc --noEmit`: "Expected 3 arguments, but got 4", "Property 'reingresarPorDevolucion' does not exist"). Resto de la suite (`sales`/`cash-registers`/`products`/`auth`) sin tocar. Sin ambigüedades nuevas. | agente |
| 2026-08-25 | T5.2 — Reingreso condicional de stock (`returns`/`stock`) | VERDE | commit `a93dcd9` en rama `feat/T5.2-impl` (ramificada de `feat/T5.2-returns-reingreso-stock`) — implementado contra los 12 tests nuevos + 2 aserciones actualizadas de la Fase 04a, **sin modificar ninguna otra aserción**. `StockService.reingresarPorDevolucion` agregado mirroreando `revertirPorAnulacion` (mismo archivo, mismo criterio: sin lock propio, incremento atómico, nunca falla por stock insuficiente). `ReturnsService.crearDevolucion`: después de `tx.return.create` (recién ahí existe `return.id`), un `reingresarPorDevolucion` por línea con `reingresaStock: true` (resolviendo `variantId` de la lectura de `sale_items` ya hecha en el paso 5), ninguno para `reingresaStock: false`. 373/373 unitarios, 300/300 integración (Postgres real), `tsc --noEmit`/lint/`nest build` limpios. `git status --short` confirma que solo se tocaron `returns.service.ts`/`stock.service.ts` — ningún archivo de test. Alcance restante de la Etapa 5 sin cambios: sin `tipo = CAMBIO` (T5.5), sin movimiento de caja real (T5.3), sin concurrencia real (T5.6). **T5.3, T5.4 y T5.6 pueden avanzar ya.** | agente |
| 2026-08-25 | Fase 04a — T5.3 (`returns`), tests primero | ROJO (esperado) | commit `3f8f0bb` en rama `feat/T5.3-returns-caja` (ramificada de T5.2 VERDE), sesión aislada. **Sin colaborador nuevo** — `ReturnsService` ya tenía `CashRegisterService` inyectado desde T5.1, solo se agregó una llamada nueva a `registrarMovimiento` (método ya existente, mismo que usa `sales` para `VENTA`/`ANULACION`). **10 casos nuevos** — 7 unitarios (100% efectivo, 100% tarjeta, mixto —solo la parte efectivo—, dos pagos efectivo declarados por separado —suma, un solo movimiento—, descripción con el número de la VENTA ORIGINAL, rechazo sin llamada, orden después de `tx.return.create`) + 3 de integración (Postgres real: camino feliz con `cash_movement` real, 100% tarjeta sin movimiento nuevo, rollback sin `cash_movement` de tipo `DEVOLUCION`). **1 aserción de integración actualizada** (el `cash_movement` del camino feliz de T5.1/T5.2 pasa de esperar 0 filas a esperar 1, con `monto` negativo por el `cash_movements_monto_sign_check`). Nota de la propia sesión: un `Grep` con patrón amplio devolvió de refilón el cuerpo de `registrarMovimiento` al buscar su firma — divulgado sin ocultarlo; el contrato ya venía dado textual en el prompt, así que no cambió nada de lo escrito. Resto de la suite (`sales`/`cash-registers`/`products`/`auth`/`stock`) sin tocar. Sin ambigüedades nuevas. | agente |
| 2026-08-25 | T5.3 — Movimiento de caja por reintegro en efectivo (`returns`) | VERDE | commit `9649b37` en rama `feat/T5.3-impl` (ramificada de `feat/T5.3-returns-caja`) — implementado contra los 10 tests nuevos + 1 aserción actualizada, **sin modificar ninguna otra aserción**. Después de reingresar stock (T5.2), si `SUM(returnPayments.monto WHERE metodo = EFECTIVO) > 0`, un único `registrarMovimiento` (`tipo: DEVOLUCION`, `referenciaTipo: RETURN`, monto positivo, `descripcion` con `sale.numero`). **Ajuste adicional de esta sesión** (mismo criterio que T5.2 con `stock_movements`): una aserción de T5.1 ("sin movimiento de caja") documentaba correctamente el alcance acotado de ese ticket, ahora superado por T5.3 — actualizada para reflejar que el reintegro 100% efectivo del camino feliz sí mueve la caja, sin tocar ninguna otra aserción de ese test. 380/380 unitarios, 303/303 integración (Postgres real), `tsc --noEmit`/lint/`nest build` limpios. `git status --short` confirma que solo se tocaron `returns.service.ts` y (por el ajuste señalado) `returns.service.spec.ts`. **T5.4, T5.5 (con AMB-16 ya resuelta) y T5.6 pueden avanzar ya.** | agente |
| 2026-08-26 | T5.4 — Reversión del costo congelado (`returns`) | VERDE | commit `0616f50` en rama `feat/T5.4-impl` (ramificada de T5.3 VERDE), Fase 04a en sesión aislada. **Sin ningún cambio de implementación** — hallazgo real, no forzado: `ReturnsService.crearDevolucion` (T5.1) ya copiaba `costoUnitario: saleItem.costoUnitario` sin condicionar por `reingresaStock`, satisfaciendo RN-6 de `modulo-returns-spec.md` desde el principio (el módulo nunca resta costo activamente — solo persiste el dato congelado para que `resultados`, Etapa 6, lo filtre por `reingresa_stock` al calcular el CMV, BLUEPRINT §5.6). **6 tests nuevos, agregados para confirmarlo explícitamente** (nada de esto se verificaba de forma directa hasta ahora): 3 unitarios (copia exacta del `costo_unitario` original, igual con `reingresaStock` true/false, dos líneas de costos distintos cada una con el suyo) + 3 de integración contra Postgres real, el más importante de los cuales vende con un `costoActual` de la variante, lo CAMBIA después en la base directamente, y confirma que la devolución sigue usando el costo VIEJO congelado en `sale_items` — prueba real de que lee el dato congelado, no el actual. Confirmado en dos corridas seguidas sin flakes: 383/383 unitarios, 305/305 integración, `tsc --noEmit`/lint limpios. Mismo criterio que un ticket cuyo objetivo ya estaba cubierto por trabajo previo: se documenta como VERDE con la constancia explícita de por qué, no se fuerza ningún cambio de código innecesario. **T5.5 (AMB-16 ya resuelta) y T5.6 pueden avanzar ya.** | agente |
| 2026-08-26 | Fase 04a — T5.5 (`returns`/`sales`), tests primero | ROJO (esperado) | commits `2635fd2` y `61ec601` en rama `feat/T5.5-returns-cambio` (ramificada de T5.4 VERDE), sesión aislada. **Contrato definido en esta sesión, desde AMB-16 resuelta**: `CrearDevolucionInput` gana `tipo?: ReturnTipo` (default `DEVOLUCION`) y `ventaNueva?: { items, payments, discounts?, ajusteRedondeo? }`; `ReturnsService` gana `SalesService` como quinto colaborador; `CrearVentaPaymentInput` gana `returnId?: number`. **Casos nuevos**: en `returns.service.spec.ts`, validación de forma (`CAMBIO` exige `ventaNueva` + exactamente un pago `CREDITO_DEVOLUCION`; `DEVOLUCION` simple rechaza ambos) confirmada ANTES de `getSesionAbiertaOrThrow` (orden explícito), camino feliz con precio igual/más caro/más barato, determinismo de `idempotencyKey` sufijada (`:cambio`); en `sales.service.spec.ts`, el bloque nuevo íntegro de invariante 14 (aceptación, `returnId` faltante/no correspondiente al medio, 404, exceso de crédito con mensaje exacto, límite exacto aceptado, orden de lock, dos devoluciones distintas en una venta); en `returns.integration.spec.ts` y `sales.integration.spec.ts` (Postgres real), cambio completo, prenda más cara con `cash_movement` real, rollback si `sales` rechaza, crédito gastado en una sesión de caja completamente distinta a la que lo generó, agotamiento de crédito a través de dos ventas sucesivas. **Incidente de esta sesión, recuperado sin pérdida**: la conexión del primer agente se cortó a mitad de tarea (justo antes de escribir los tests de integración del lado `sales`) — se verificó que el estado parcial no estaba corrupto (`git diff --stat` confirmó una inserción limpia, no un archivo truncado; una corrida en rojo con 20/20 fallos resultó ser un flake transitorio de conexión a Postgres, no un bug sistémico, reconfirmado con una segunda corrida) y se comiteó como checkpoint (`2635fd2`) antes de lanzar una sesión aislada de continuación con instrucción explícita de leer los tres bloques ya escritos como fuente de verdad y agregar solo lo que faltaba — esa segunda sesión cerró con `61ec601`. Confirmado en rojo por ausencia de implementación (`tsc --noEmit` con errores de tipo esperados). Resto de la suite sin tocar. Sin ambigüedades nuevas. | agente |
| 2026-08-26 | T5.5 — Cambio con crédito de devolución diferido (`returns`/`sales`, RN-9/AMB-16) | VERDE | commit `3653e65` en rama `feat/T5.5-impl` (ramificada de `61ec601`) — implementado contra los tests de la Fase 04a **sin modificar ninguna aserción existente**. `ReturnsService.crearDevolucion`: paso 0b valida la forma tipo/ventaNueva/crédito antes de la sesión de caja; pasos 14-15 (después de reingreso de stock y movimiento de caja) llaman `salesService.crearVenta` con el crédito como pago y reasignan `devolucion` al resultado de `tx.return.update` con `saleNuevaId`. `SalesService.crearVenta`: pasos 8b/8c agregados entre la validación de pagos existente y la suma-contra-total — todo pago `CREDITO_DEVOLUCION` exige `returnId` y viceversa, lock de las filas de `Return` referenciadas ordenado por id (BLUEPRINT §9.4), invariante 14 verificada sumando el crédito ya consumido (`payment.aggregate`, todas las ventas) más el pedido en esta misma venta contra `total_devuelto`. **Por AMB-16 (crédito diferido, no solo atómico), la invariante 14 vive en `sales.service.ts`, no en `returns.service.ts`** — cualquier `POST /sales` futuro y sin relación con el cambio original puede gastar un crédito viejo, así que la validación no puede depender de estar dentro del flujo atómico de `crearDevolucion`; `sales.service.ts` consulta `tx.return`/`tx.payment` directamente vía Prisma, sin importar `ReturnsService`, evitando una dependencia circular (mismo patrón que `anularVenta` ya usa contra `tx.return.findFirst`). **Dos bugs reales encontrados y corregidos en esta sesión**: (1) `returns.service.ts` no reasignaba `devolucion` tras `tx.return.update`, dejando `saleNuevaId` en `null` en el valor devuelto pese a que el `UPDATE` sí lo seteaba en la base — encontrado por un test de integración (`saleNuevaId` esperado no-null, recibido `null`); (2) `buildCreatedReturnFromCall` (helper de test) omitía los campos escalares de nivel superior del `Return` creado (`totalDevuelto`, `autorizadoPorUserId`, etc.), gap preexistente de infraestructura expuesto recién por un assert nuevo de T5.5 — corregido junto con el mock de `tx.return.update`, que pasó de devolver `{}` a simular la fusión real de Prisma (`{ ...última fila creada, ...data }`). Confirmado en dos corridas seguidas sin flakes: 401/401 unitarios, 313/313 integración (Postgres real), `tsc --noEmit`/lint/`nest build` limpios. `git status --short` confirma que solo se tocaron `returns.service.ts`/`sales.service.ts` y los tres archivos de test ya señalados — `.env.example` nunca se tocó. **T5.6, T5.7 y T5.8 pueden avanzar ya (T5.7 y T5.8 dependían de T5.5).** | agente |
| 2026-08-26 | T5.6 — Test de concurrencia real, invariante 8 (`returns`) | VERDE | commit `b9fedf0` en rama `feat/T5.6-concurrencia` (ramificada de T5.5 VERDE), sesión aislada. **Sin ningún cambio de implementación** — mismo patrón que T5.4/T4.9: el lock de `sale_items` ordenado por id (BLUEPRINT §9.4) ya lo toma `ReturnsService.crearDevolucion` desde T5.1, este ticket solo le agrega el test de concurrencia real que faltaba (no existía ninguno para `returns` hasta ahora — a diferencia de T4.9, que resultó ya satisfecho por T4.1, acá no había cobertura previa: se confirmó con `grep` antes de lanzar la sesión). **1 caso, 5 iteraciones con datos frescos** (`describe('... T5.6 concurrencia real (invariante 8)')`, `returns.integration.spec.ts`, Postgres real): por vuelta, una venta real de 10 unidades de una variante fresca, dos `crearDevolucion` reales de 6 unidades cada una lanzadas en paralelo (`Promise.allSettled`) sobre la MISMA línea — 6+6=12 supera lo vendido, pero cada 6 sola pasa el tope de RN-4 si se evaluara aislada, el caso exacto que ejercita el lock (sin él, las dos podrían leer el mismo acumulado "viejo" y pasar). En las 5 iteraciones: exactamente una promesa resuelve, la otra rechaza con el mismo mensaje (`/supera|disponible/i`) que ya usa el test secuencial de RN-4 existente — reusado, no inventado. Verificado además con una query real (`returnItem.aggregate`) que la suma en base nunca supera las 10 vendidas. **Confirmado VERDE de entrada** (reconfirmado por mí en dos corridas independientes, no solo por el reporte de la sesión aislada) — el lock de T5.1 sostiene el invariante 8 bajo concurrencia real, no solo en teoría. 401/401 unitarios, 314/314 integración (+1), `tsc --noEmit`/lint/`nest build` limpios. `git status --short` confirma que solo se tocó `returns.integration.spec.ts` — `.env.example` nunca se tocó. **T5.7 y T5.8 pueden avanzar ya (ambos dependían de T5.5, ya VERDE).** | agente |
| 2026-08-26 | Fase 04a — T5.7 backend (`returns.controller`), tests primero | ROJO (esperado) | commit `97ebafa` en rama `feat/T5.7-returns-controller` (ramificada de T5.6 VERDE), sesión aislada. Mismo criterio que T4.11: T5.7 es nominalmente "frontend" en el roadmap, pero primero necesita el controller HTTP que no existía (`ReturnsService`, T5.1-T5.6, completo y sin `ReturnsController`/`ReturnsModule`/DTOs, `AppModule` sin registrarlo). **Contrato definido en esta sesión**, reconciliando la sección 4 de `modulo-returns-spec.md` (desactualizada — describe un campo `creditoAplicado?` separado que no existe) contra la interfaz real exportada de `CrearDevolucionInput`: el crédito de un `CAMBIO` es una línea más de `returnPayments` con `metodo: CREDITO_DEVOLUCION`, sin campo aparte — no se escaló como ambigüedad, las dos fuentes primarias (RN-9 y la interfaz real) coinciden entre sí, solo la tabla-resumen de la sección 4 quedó vieja respecto de cómo T5.5 terminó implementando el mecanismo. **26 casos** en `returns-controller.integration.spec.ts` (Postgres real, plantilla mecánica de `sales-controller.integration.spec.ts`): camino feliz de `GET /returns/sales/:numero` (disponibilidad por línea, con y sin devolución previa, 404, `costoUnitario` solo para `OWNER` —resolución de ambigüedad de esta sesión, ver nota abajo—, 401), camino feliz de `POST /returns` DEVOLUCION y CAMBIO, idempotencia, errores de negocio ya validados por `crearDevolucion` confirmados intactos hasta HTTP, validación de DTO, auth/CSRF. **Incidente de esta sesión, sin pérdida**: un primer intento se cortó por un error de conexión antes de escribir nada (confirmado con `git status --short` limpio) — reintentado desde cero sin necesidad de recuperar nada parcial. 23/26 en rojo por 404 (ruta no registrada); los 3 restantes (venta inexistente ×2, Origin cross-site) coinciden con 404/403 por razones arquitectónicas independientes de la ruta faltante (`OriginCheckMiddleware` corre en `forRoutes('*')` antes de que el router intente resolver `/returns`), reconfirmado por mí corriendo el archivo dos veces antes de implementar. Resto de la suite (314/314 integración) sin tocar. | agente |
| 2026-08-26 | T5.7 (backend) — `ReturnsController`, `GET /returns/sales/:numero` y `POST /returns` (`returns`) | VERDE | commit `703caaa` en rama `feat/T5.7-returns-controller` — implementado contra los 26 tests de la fase 04a **sin modificar ninguna aserción** (el único cambio en el archivo de test fue reformateo de `eslint --fix`, confirmado con `git diff` línea por línea antes de comitear). `ReturnsController.crear` (`POST /returns`): mismo patrón exacto que `SalesController.crear` — `IdempotencyInterceptor`/`withIdempotency` envolviendo `prisma.$transaction(tx => returnsService.crearDevolucion(tx, {...}))`, `esOwner` resuelto siempre de `user.rol === 'OWNER'`, nunca del body. `ReturnsController.buscarVentaPorNumero` (`GET /returns/sales/:numero`): delega a un método nuevo, `ReturnsService.buscarVentaParaDevolucion` — no existía ninguna lectura así todavía; agrega `sale_items` con el acumulado de `return_items` previos dentro de su propia transacción `RepeatableRead` (mismo criterio que `reconciliar()` de `sales`/`cash-registers`/`stock`), calcula `dentroDePlazo` con la misma fórmula que `crearDevolucion` ya usa. `costoUnitario` se incluye en cada línea solo si `user.rol === 'OWNER'` (RN-10 de `sales`, mismo criterio ya aplicado en otros endpoints) — resolución de ambigüedad entre la lista de campos de la sección 4 de la spec (no lo menciona) y su tabla de permisos (sección 8, sí lo menciona con esa restricción de rol), documentada y no escalada por ser de bajo riesgo y reversible. `CreateReturnDto` (`class-validator`, `@ArrayMaxSize(500)` en `items`, `20` en `returnPayments`/`payments`/`discounts`, mismo criterio que `CreateSaleDto`) y `ReturnsModule` (importa `StockModule`/`CashRegistersModule`/`SalesModule`/`SettingsModule`, exporta `ReturnsService`) nuevos, registrado en `AppModule`. 26/26 del ticket (verde de punta a punta, sin ningún fix de por medio — confirmado en dos corridas seguidas antes de tocar el resto de la suite), 401/401 unitarios, 340/340 integración (+26), `tsc --noEmit`/lint/`nest build` limpios. `git status --short` confirma que solo se tocaron `app.module.ts`, `returns.service.ts`, los 3 archivos nuevos de `returns.controller.ts`/`returns.module.ts`/`dto/create-return.dto.ts`, y el test ya señalado — `.env.example` nunca se tocó. **Falta la mitad frontend de T5.7 (pantallas) para que el ticket completo del roadmap pase a VERDE** — mismo patrón que T4.11, que también quedó en dos filas separadas de esta bitácora antes de que el roadmap reflejara el ticket cerrado. | agente |
| 2026-08-26 | T5.7 (frontend) — Pantalla de devolución y cambio (`returns`) | VERDE | commit `cb6042b` en rama `feat/T5.7-returns-controller`. **Fase 04a NO aplica** (frontend, mismo criterio que T4.10/T2.12/T3.7). `DevolucionPage.tsx` (`/devoluciones`, agregada a `AppLayout`/`router.tsx`): busca una venta por `numero` (`GET /returns/sales/:numero`), muestra disponibilidad por línea, deja elegir cantidad y `reingresaStock`, y arma `POST /returns` como devolución simple o como cambio. `calc.ts` (13 tests Vitest nuevos) previsualiza en el cliente el mismo cálculo de AD-18 que usa el backend — proporcional redondeado (`Math.round`, coincide con `ROUND_HALF_UP` para valores positivos) o el remanente exacto (`netoLineaDisponible`) si la devolución agota la línea — para poder armar reintegros/pagos que sumen el total exacto antes de enviar; el backend vuelve a validar todo al confirmar, la pantalla nunca es la última palabra. **Cambio (RN-9)**: el crédito aplicado es `min(total a devolver, total de la venta nueva)` — prenda más cara: la diferencia se cobra aparte (`ventaNueva.payments`, constructor reusable `PaymentLinesBuilder`); prenda más barata: el resto se reintegra por los medios habituales (líneas extra de `returnPayments`, además del crédito). **Decisión de alcance documentada, no escalada**: sin persistencia de borrador en `sessionStorage` (a diferencia de `SalePage`/`CobroPage`) — un F5 a mitad de una devolución pierde la selección en curso, no la integridad (nada se envía hasta confirmar); la clave de idempotencia sí persiste (mismo mecanismo que `sales`, `lib/idempotency.ts`), así que un reenvío tras un F5 durante el propio submit nunca duplica la operación. **Verificación manual completa en navegador real** (backend + frontend corriendo, sesión OWNER real, datos de prueba propios): devolución simple de una línea con reintegro 100% efectivo — confirmado en la base que `returns`/`return_items`/`return_payments` quedaron correctos, stock reingresado, `cash_movement` tipo `DEVOLUCION` por `-$` generado; cambio con prenda más cara — crédito completo aplicado, diferencia de $500 cobrada aparte, `sale_nueva_id` vinculado, `payment` `CREDITO_DEVOLUCION` correctamente ligado a la venta nueva — ambos casos confirmados contra la base real, no solo por la respuesta HTTP 201. 84/84 Vitest (+13), `tsc -b`, lint y build (frontend) limpios (bundle 658.50 kB/199.47 kB gzip). **Etapa 5 (T5.1–T5.7) completa — T5.8 es el único ticket restante.** | agente |
| 2026-08-26 | Fase 04a — T5.8 (`returns`/`sales`), tests primero | ROJO (esperado) | commit `f84dbfb` en rama `feat/T5.8-credito-diferido` (ramificada de T5.7 VERDE), sesión aislada. **Punto de partida ya construido, reusado sin reconstruir**: `SalesService.crearVenta` (T5.5) ya valida el invariante 14 completo A NIVEL DE SERVICIO — el gap real es que la capa HTTP nunca lo ejercita, porque `SalePaymentDto` no tenía campo `returnId` (`forbidNonWhitelisted` lo rechazaba antes de llegar a ningún lado) y no existía `GET /returns/:numero/credito`. **12 casos** — 6 en `returns-controller.integration.spec.ts` (`GET /returns/:numero/credito`: sin consumo, `CAMBIO` a precio igual con crédito íntegro consumido, `CAMBIO` a precio menor con crédito parcial, número inexistente → 404, ambos roles pueden consultar, sin autenticación → 401) + 6 en `sales-controller.integration.spec.ts` (`POST /sales` con `returnId` real por HTTP: crédito exacto, crédito parcial + efectivo, exceso de crédito, `returnId` inexistente, `CREDITO_DEVOLUCION` sin `returnId`, `returnId` en pago no-crédito). **Matiz honesto documentado por la propia sesión, no escondido**: de los 12, solo 9 dieron rojo por la ausencia real de funcionalidad (`GET` → 404 de ruta; `POST` camino feliz → 400 por `forbidNonWhitelisted` rechazando el campo desconocido; `returnId` inexistente → 400 en vez del 404 esperado, mismo motivo); los otros 3 (exceso de crédito, sin `returnId`, `returnId` en pago no-crédito) ya daban el status esperado HOY, dos de ellos por la misma causa de whitelist coincidiendo por casualidad con el 400 esperado, y uno (`CREDITO_DEVOLUCION` sin `returnId`, que no manda ningún campo desconocido) genuinamente ya alcanzable por HTTP desde T5.5 sin cambiar nada — confirmado por mí corriendo el archivo, mismo resultado. Resto de la suite (352 tests) sin tocar. Sin ambigüedades nuevas — la spec (secciones 4/6/7/8/9) y el `ROADMAP.md` cubrían el contrato completo. | agente |
| 2026-08-26 | T5.8 (backend) — Crédito de devolución diferido, `GET /returns/:numero/credito` (`returns`/`sales`) | VERDE | commit `91f7081` en rama `feat/T5.8-credito-diferido` — implementado contra los 12 tests de la fase 04a **sin modificar ninguna aserción**. `SalePaymentDto` gana `returnId?: number` (`@IsOptional() @IsInt()`, sin validación de negocio en el DTO — eso ya lo hace `SalesService.crearVenta` desde T5.5); con el campo agregado, `SalesController.crear()` no necesitó ningún cambio (ya hacía `payments: dto.payments` tal cual). `ReturnsService.consultarCredito(numero)`: método nuevo, lectura pura sin lock — suma en vivo `payments.monto` con `metodo = CREDITO_DEVOLUCION` que referencian la devolución, a través de TODAS las ventas (sin columna de saldo cacheada, mismo criterio que `reconciliar()`), `creditoDisponible = totalDevuelto - creditoConsumido`. Expuesto vía `GET /returns/:numero/credito` (`ReturnsController`), cualquier rol autenticado, 404 "Devolución no encontrada" si no existe. Los 3 casos que ya daban verde antes de implementar pasaron a estarlo por la razón CORRECTA (el campo real, no la coincidencia de whitelist) — reconfirmado corriendo el archivo después del cambio. 12/12 del ticket, 401/401 unitarios, 352/352 integración (+12, dos corridas seguidas sin flakes), `tsc --noEmit`/lint/`nest build` limpios. `git status --short` confirma que solo se tocaron `returns.controller.ts`/`returns.service.ts`/`create-sale.dto.ts` — `.env.example` nunca se tocó. **Falta la mitad frontend de T5.8 (quinta opción de medio de pago en `CobroPage.tsx`) para que el ticket completo del roadmap pase a VERDE** — mismo patrón que T4.11/T5.7. | agente |
| 2026-08-28 | T5.8 — Dos hallazgos reales de money-integrity, encontrados en verificación manual (`returns`/`sales`) | VERDE | commit `f078e32` en rama `feat/T5.8-credito-diferido`. Con el backend de T5.8 "cerrado" (commit anterior), la verificación manual en navegador real (backend + frontend corriendo, Postgres real) expuso dos gaps reales de T5.5, ninguno de ellos nuevo en sí — T5.8 fue lo primero en volverlos alcanzables por HTTP. **Hallazgo 1 (double-spend real, confirmado con repro en vivo)**: tanto `SalesService.crearVenta` (paso 8c) como `consultarCredito` usaban `return.total_devuelto` como techo del crédito disponible — pero un `CAMBIO` a una prenda más barata reintegra el excedente por OTRO medio (RN-9), y ese excedente NUNCA fue crédito. Repro: cambio de $1500 a un ítem de $50 → $50 crédito + $1450 reintegrados en efectivo → `creditoDisponible` mostraba **$1450 fantasma**, gastable otra vez en una venta futura sin relación. **Hallazgo 2 (la funcionalidad central de T5.8 era inalcanzable)**: trazando el mecanismo con precisión, el crédito de un `CAMBIO` se gasta SIEMPRE entero, en el momento, contra su propia venta nueva (invariante 3 de esa venta exige `SUM(payments)==total` — no hay forma de que sobre nada); una `DEVOLUCION` simple, por su parte, rechazaba cualquier reintegro `CREDITO_DEVOLUCION`. Resultado: `creditoDisponible` era **SIEMPRE $0** apenas creada la devolución — la nota de crédito "para una venta futura y separada" (AMB-16 RESUELTA) nunca era alcanzable por el flujo real, solo insertando filas a mano (como ya hacían, sin darse cuenta del problema, los propios tests de T5.5). **Ambos hallazgos confirmados con el PO antes de tocar código** (dos rondas de pregunta explícita, ambas resueltas con "arreglalo ahora, dentro de T5.8"). **Fix 1**: el techo real es la SUMA de `return_payments` marcados `CREDITO_DEVOLUCION` (la única parte que efectivamente se convirtió en nota de crédito), no `total_devuelto` — corregido en los dos lugares. **Fix 2**: el paso 0b de `crearDevolucion` se amplió para que una `DEVOLUCION` simple (sin `ventaNueva`, nada que gastar en el acto) también admita UN reintegro `CREDITO_DEVOLUCION`, bancado de verdad. **3 aserciones preexistentes corregidas** (no debilitadas — corregidas porque encapsulaban el comportamiento viejo, incorrecto, documentado caso por caso en cada archivo): caso 25 y 27 de `returns-controller.integration.spec.ts` (esperaban `creditoDisponible` fantasma), un test de `returns.service.spec.ts` (esperaba 400 donde ahora corresponde 201). El fixture `crearDevolucionConCredito` de `sales-controller.integration.spec.ts` reintegraba en EFECTIVO, no en crédito — nunca generó crédito real, solo "funcionaba" por el mismo bug del hallazgo 1; corregido a `CREDITO_DEVOLUCION` real. 402/402 unitarios (+1, el test dividido en dos), 353/353 integración (+1, el nuevo caso del hallazgo 1), dos corridas seguidas sin flakes, `tsc --noEmit`/lint/`nest build` limpios. `git status --short` confirma que solo se tocaron `returns.service.ts`/`sales.service.ts`, los archivos de test ya señalados, y `.claude/launch.json` (entrada nueva `backend` para poder correr el servidor real en la verificación manual) — `.env.example` nunca se tocó. | agente |
| 2026-08-28 | T5.8 (frontend) — Quinta opción de pago con crédito de devolución (`sales`/`returns`) | VERDE | commit `0543298` en rama `feat/T5.8-credito-diferido`. **Fase 04a NO aplica** (frontend, mismo criterio que T4.10/T5.7 frontend). `CobroPage.tsx` gana la quinta opción de medio de pago ("Crédito devolución", atajo `5`) — a diferencia de los otros 4, no precarga el importe hasta buscar la devolución (`GET /returns/:numero/credito`): muestra el saldo disponible y precarga `min(saldo pendiente de esta venta, crédito disponible)`, mismo criterio de UX que los atajos 1-4 de T4.11. El pago confirmado incluye `returnId` y una `referencia` ("Devolución #N") para el registro. `DevolucionPage.tsx`: la sección "Reintegro" de una devolución simple gana "Crédito de devolución" como medio válido (`PaymentLinesBuilder`, prop nueva `allowCredito`, activa SOLO ahí — nunca en la diferencia a cobrar ni el excedente de un cambio, donde no tiene sentido pagarlo con crédito) — es la mitad frontend del hallazgo 2 del commit anterior: sin esto, el backend podía bancar un crédito real pero ninguna pantalla dejaba elegirlo. **Verificación manual end-to-end completa en navegador real** (backend + frontend corriendo, Postgres real, sesión OWNER real, datos propios limpiados al terminar): devolución simple de $200 con reintegro 100% crédito → `GET /returns/:numero/credito` confirma $200 disponibles → venta nueva y completamente separada (otro producto, mismo turno de caja) por $80 paga con "Crédito devolución" → importe precargado correcto ($80) → venta confirmada (`Venta #8652`) → saldo del crédito reconfirmado en $120 restantes (`creditoConsumido: 80`) — el ciclo completo de AMB-16 ("nota de crédito para una venta futura, separada") confirmado funcionando de punta a punta por primera vez. 84/84 Vitest, `tsc -b`, lint y build limpios (bundle 660.36 kB/199.92 kB gzip, creció ~1.8 kB desde T5.7). **Etapa 5 (T5.1–T5.8) completa — módulo `returns` listo para su cierre (Fases 07→12), mismo camino que `sales` recorrió antes.** | agente |
| 2026-08-28 | Módulo `returns` — Fase 07 (cierre de implementación) | VERDE | rama `fase07-cierre-returns` (ramificada de T5.8 VERDE). Contrastado línea por línea contra `state/reports/modulo-returns-spec.md` (secciones 1-11) y `BLUEPRINT.md` §12.4/§12.6: **sin funcionalidad faltante** de las 8 RN y los 7 invariantes que el módulo toca (7/8/10/11/13/14/15) — todos garantizados por construcción y ya probados. Mensajes de error de `returns.service.ts` y `sales.service.ts` (rutas de crédito) contrastados uno por uno contra la tabla de la sección 7: coinciden literalmente los 11. Wiring completo confirmado: `ReturnsModule` (`StockModule`/`CashRegistersModule`/`SalesModule`/`SettingsModule`) registrado en `AppModule`, rutas `/returns/sales/:numero`, `/returns/:numero/credito` y `/returns` sin ambigüedad de matching (confirmado en el log de arranque y en vivo). Frontend: `/devoluciones` registrada en `router.tsx`/`AppLayout`, botón "Confirmar devolución"/"Confirmar cambio" con `loading`/`disabled` (BLUEPRINT §12.6 regla 3), errores mostrados en `err.message` humano (regla 2), `costoUnitario` nunca visible para `SELLER` (regla 4, RN-10 de `sales`). **Sin `TODO`/código muerto/comentado** (grep de palabra completa, sin falsos positivos de "TODO" dentro de "método"). Sin duplicación entre tickets que convenga unificar — cada uno tocó su propia porción sin superposición. **Único ajuste de esta fase**: `state/reports/modulo-returns-spec.md` ganó una sección 12 (addendum) documentando las dos correcciones de T5.8 (techo del crédito, `DEVOLUCION` simple admite crédito) como constancia — el texto original de las secciones 2/5/6/7 queda intacto, sin reescribirse, como registro histórico de lo especificado en la Fase 06. 402/402 unitarios, 353/353 integración backend, 84/84 Vitest frontend, `tsc --noEmit`/`tsc -b`/lint (los dos)/`nest build`/`vite build` limpios. **Explícitamente fuera de alcance, documentado y sin ticket que lo reserve** (mismo criterio que los gaps ya aceptados de `sales`): `GET /returns`, `GET /returns/:id` (listado/detalle históricos, sección 4 de la spec). No se declara el módulo terminado — sigue la Fase 08 (QA adversarial + mutación). | agente |
| 2026-08-28 | Módulo `returns` — Fase 08 (QA adversarial) | VERDE | `state/reports/modulo-returns-qa-2026-08-28.md` — rama `fase08-qa-returns` (ramificada de Fase 07 VERDE). **Hallazgo real (severidad HIGH), corregido y probado**: `crearDevolucion` nunca validaba que los `saleItemId` mandados pertenecieran a la `saleId` declarada — leía `sale_items` filtrando SOLO por su propio `id`, sin `saleId`. Reproducido en vivo con Postgres real: `saleId` de una venta A + `saleItemId` de una línea de una venta B completamente distinta → 201, la devolución quedaba creada con `sale_id` de A pero `total_devuelto`/`costo_unitario` de la línea de B — integridad referencial rota (no explotable para robar plata directamente, el importe sigue siendo el de la línea real, pero corrompe la trazabilidad venta↔devolución y podría distorsionar el CMV de `resultados` que lee `return_items.costo_unitario`). `GET /returns/sales/:numero` ya filtraba correctamente por `saleId` — la pantalla real nunca puede ofrecer un `saleItemId` ajeno, el bug requería manipular la request directamente. **Corregido**: `saleId: input.saleId` agregado al `where` de `tx.saleItem.findMany` (paso 5) — con el filtro, una línea ajena no aparece en el resultado, y el chequeo `if (!saleItem) throw ...` que YA EXISTÍA (con el mensaje exacto "no existe en esta venta", nunca alcanzado hasta ahora) la rechaza sin necesitar código nuevo. Probado con un test de integración (Postgres real, dos ventas genuinas) y aserciones unitarias sobre el `where` exacto. Resto de las categorías (lógica de negocio/rollback, API, UI, security — autenticación/autorización/IDOR/inputs maliciosos/SQL injection/XSS/exposición de información) revisadas sin hallazgos nuevos, detalle completo en el reporte. **Testing de mutación (Stryker, obligatorio — BLUEPRINT §9.8): antes 59.72%** (la gran mayoría del hueco en `buscarVentaParaDevolucion`/`consultarCredito`, sin ningún test UNITARIO hasta ahora — solo integración, y Stryker corre exclusivamente la suite unitaria) **→ después 99.05%** (209 killed, 2 survived documentados como aceptados —borde exacto de `Date.now()` en el chequeo de plazo, imposible de cronometrar de forma determinista sin inyectar el reloj—, 0 sin cobertura). 17 tests unitarios nuevos. **Resultado de validación**: 418/418 unitarios (+16), 354/354 integración (+1, dos corridas seguidas sin flakes), `tsc --noEmit`/lint/`nest build` limpios. No se declara el módulo aprobado — sigue la Fase 09 (security audit, sin modificar código). | agente |
| 2026-08-28 | Módulo `returns` — Fase 09 (security audit) | VERDE | `state/reports/modulo-returns-secaudit-2026-08-28.md` — rama `fase09-secaudit-returns`, sin código modificado (regla de la fase). Sin CRITICAL ni HIGH pendientes. **El hallazgo HIGH de la Fase 08 (manipulación de IDs) reconfirmado corregido EN VIVO**: dos ventas genuinas creadas por HTTP contra el servidor real ($100 y $250), `POST /returns` con `saleId` de una y `saleItemId` de la otra → `400 "La línea 8440 no existe en esta venta"`, mismo mensaje esperado. **CSRF reconfirmado EN VIVO**: `POST /returns` con `Origin: https://evil.example.com` + cookie real → `403 "Origen no autorizado"`, antes de tocar la sesión; Origin legítimo sigue funcionando (llega a la validación de negocio). Los dos `GET` del módulo no son bloqueados por Origin cross-site — comportamiento heredado y ya aceptado del middleware (solo protege métodos mutadores), no un hallazgo nuevo. **Autenticación reconfirmada EN VIVO**: las 3 rutas sin cookie → 401. **`esOwner` forjado en el body reconfirmado EN VIVO**: `400 "property esOwner should not exist"` (`forbidNonWhitelisted`). `costoUnitario` ausente (no `null`) para `SELLER`, ya probado en Fase 08. SQL injection: lock manual parametrizado con `Prisma.join()`, sin concatenación cruda. XSS: sin `dangerouslySetInnerHTML` en los 3 archivos frontend tocados. `npm audit`: 12 advisories totales, 3 high de producción — exactamente los mismos ya documentados en TD-9, sin cambios. **1 hallazgo LOW nuevo**: `POST /returns` sin `@nestjs/throttler` — mismo patrón exacto que TD-12 (`cash-registers`)/TD-14 (`sales`), registrado como **TD-15** en `state/TECH_DEBT.md`, no bloquea (decisión transversal ya tomada, sesión autenticada exigida, abuso acotado por validaciones de negocio). Sin hallazgos en las 20 categorías del protocolo (`state/reports/modulo-returns-secaudit-2026-08-28.md` tiene el detalle completo, sección por sección). No se declara el módulo seguro de forma permanente — corresponde a la Fase 11 confirmarlo de forma independiente. | agente |
| 2026-08-28 | Módulo `returns` — Fase 10 (security remediation) | VERDE | rama `fase10-secremediation-returns`. **Sin ningún cambio de código** — hallazgo real, no forzado: la Fase 09 no dejó ninguna vulnerabilidad CRITICAL ni HIGH pendiente de corregir (el único HIGH real del módulo, manipulación de IDs, ya se había corregido en la Fase 08 y se reconfirmó corregido en vivo en la Fase 09). El único hallazgo nuevo de la Fase 09 fue LOW (TD-15, sin rate limiting en `POST /returns`) — según `QUALITY_GATE.md` ("LOW: pueden permanecer como deuda técnica documentada"), no exige remediación de código, solo la fila de `TECH_DEBT.md` que la Fase 09 ya agregó. Mismo criterio que un ticket cuyo objetivo ya estaba cubierto por trabajo previo (T5.4/T4.9): se documenta como VERDE con la constancia explícita de por qué, no se fuerza ningún cambio innecesario. 418/418 unitarios reconfirmados (sin regresión, nada que romper al no haber tocado código). No se declara el módulo seguro todavía — sigue la Fase 11 (re-auditoría, confirma de forma independiente que sigue sin CRITICAL/HIGH). | agente |
| 2026-08-28 | Módulo `returns` — Fase 11 (security re-audit) | VERDE | `state/reports/modulo-returns-secreaudit-2026-08-28.md` — sin ningún archivo modificado (regla de la fase). Confirmado que entre la Fase 09 y la Fase 10 hubo exactamente un commit, de documentación pura (`git diff --stat` no muestra código tocado). **Reconfirmado EN VIVO contra el servidor real, mismo resultado exacto que la Fase 09**: sin cookie → 401; `Origin` cross-site + cookie real → 403 "Origen no autorizado"; `esOwner` forjado en el body → 400 "property esOwner should not exist"; devolución inexistente → 404 "Devolución no encontrada". El hallazgo HIGH de manipulación de IDs no se reprodujo con un ataque nuevo en esta fase (ya reproducido y confirmado corregido en la Fase 09, sin ningún cambio de código desde entonces que pudiera haberlo revertido). **Sin hallazgos nuevos.** 418/418 unitarios, 354/354 integración (Postgres real, sin flakes). **Sin CRITICAL ni HIGH.** El módulo queda en condiciones de avanzar a la Fase 12 (production readiness) cuando corresponda — no se declara "listo para producción" acá. | agente |
| 2026-08-28 | Módulo `returns` — Fase 12 (production readiness) | VERDE | `state/reports/modulo-returns-prodready-2026-08-28.md` — sin código modificado (regla de la fase); única escritura fue el reporte y el bookkeeping ya hecho en fases previas. **PRODUCTION READY: YES.** Sin CRITICAL ni HIGH (el único HIGH de todo el ciclo, manipulación de IDs, corregido en la Fase 08 y reconfirmado en vivo tres veces — Fases 09/11/12). Reliability: atomicidad por transacción única, rollback completo verificado en cada camino de rechazo, concurrencia real probada dos veces (T5.6 invariante 8, T5.5/T5.8 invariante 14) — siempre una pasa, la otra rechaza. Performance: sin N+1 accidental (todas las lecturas agrupadas con `IN`/`findMany`); el único patrón "una query por línea" (`reingresarPorDevolucion`) es requisito literal del blueprint, mismo criterio ya aceptado en `sales`; una observación menor no bloqueante (lectura/lock de una fila de más solo en el camino de ataque ya bloqueado por el fix de la Fase 08). Code quality: sin duplicación/TODO/dead-code, mutación 99.05% mantenida desde la Fase 08 (sin recorrer Stryker de nuevo — sin código tocado desde entonces). Observability y deployment revisados de punta a punta — sin variables de entorno ni migraciones nuevas en todo el ciclo T5.1–T5.8. **Tests**: 418/418 unitarios, 354/354 integración (Postgres real, reconfirmado sin flakes), 84/84 Vitest — todo corrido de nuevo en esta fase, no reciclado. Build backend y frontend limpios (bundle 660.36 kB/199.92 kB gzip, TD-10 sin agravarse). **2 LOW registradas** (TD-9 transversal sin cambios, TD-15 nueva — `POST /returns` sin rate limiting, mismo patrón que TD-12/TD-14). **Riesgos restantes documentados, ninguno bloqueante**: `GET /returns`/`GET /returns/:id` sin ruta HTTP (gap de alcance ya señalado en la Fase 07). **Módulo `returns` aprobado — pasa a la Fase 13 (Integration Audit) cuando corresponda, junto con el resto de los módulos del MVP. Etapa 5 (`returns`) completamente cerrada: T5.1–T5.8 VERDE + Fases 07→12 VERDE, mismo camino que `sales` recorrió antes.** | agente |
| 2026-08-28 | Módulo `expenses`/`resultados` — Fase 06 (Spec del módulo) | VERDE | `state/reports/modulo-expenses-resultados-spec.md` — spec completa (11 secciones) sobre BLUEPRINT AD-7/AD-8/AD-13, §3.7/§3.8/§3.9, §5.6, invariantes 7/9/10, §9.3/§9.4/§9.7/§9.8, §12.4/§12.6; `MVP_SCOPE.md` §3.6/§5/§7; `schema.prisma` (modelos `Expense`/`ExpenseCategory`, ya en la base desde la Fase 01 — sin migración nueva); `seed.ts` (6 categorías ya seedeadas, `bloqueada: true`); dependencias `auth`/`cash-registers`/`sales`/`returns`/`settings`/idempotencia confirmadas VERDE. **Bloqueo parcial ya conocido y aceptado con el usuario**: T6.5 (agrupación en hora argentina) depende de T0.7 (helper de zona horaria, AD-13), hoy `PENDIENTE` — T6.1–T6.4/T6.6/T6.8/T6.9 avanzan igual, T0.7 se resuelve recién cuando el ticket que lo necesita esté al frente. **Decisión de esta sesión, no ambigüedad de negocio** (documentada en la sección 11 de la spec, con su razonamiento completo): registrar un gasto y ver el listado de gastos quedan **`OWNER`-only** (RN-2/RN-11) — `ROADMAP.md` ya anota T6.8 como "(solo OWNER)" para las pantallas de gastos Y resultados juntas, y dado que un gasto revela montos contra categorías sensibles ("Sueldos"), la lectura más consistente con BLUEPRINT §5.1 (verificación de rol siempre en el servidor) es que el módulo entero de gastos es tan restringido como resultados — el ABM de categorías (solo nombres, sin montos) queda abierto, mismo criterio que marcas/categorías/talles/colores de `products`. **Alcance de rigor definido para el cierre (sección 11)**: `MVP_SCOPE.md` clasifica `resultados` como riesgo MEDIO (Fases 09/11 aligeradas, autorizado explícitamente por el propio documento) — pero `expenses` sí mueve plata real (genera `cash_movements`), así que la Fase 08 del cierre se hace completa para las dos mitades, y las Fases 09/11 solo se aligeran para los endpoints de solo lectura de `resultados`, nunca para `POST /expenses`/`POST /expense-categories`. Fase04a (tests primero) no es obligatoria por el protocolo base para este módulo (`04a-tests-first.md` no lo lista) — se aplica igual, por decisión ya confirmada con el usuario de mantener el rigor de Etapa 5, a T6.2/T6.3 (plata real) y T6.4–T6.7 (`resultados`, el número que la clienta usa para decidir sobre su negocio); T6.1 (ABM sin plata) y T6.8/T6.9 (frontend) no la necesitan, mismo criterio ya establecido. **Sin ambigüedades PENDIENTES.** T6.1–T6.4, T6.6, T6.8 y T6.9 pueden arrancar ya; T6.5 y T6.7 esperan T0.7. | agente |
| 2026-08-28 | T6.1 — ABM de categorías de gasto | VERDE | `backend/src/modules/expenses/{expense-categories.service.ts,expense-categories.controller.ts,expenses.module.ts,dto/create-expense-category.dto.ts,dto/update-expense-category.dto.ts}`, registrado en `app.module.ts`. Sin Fase04a (decisión ya documentada en la Fase 06: ABM sin plata, mismo criterio que `brands`/`categories`). `findAll` (orden por nombre), `create` y `update` — `create`/`update` rechazan con `BadRequestException` cualquier nombre que aluda a compra de mercadería (AD-7: "mercadería"/"compra de ropa"/"proveedores", insensible a mayúsculas y acentos vía filtro de código de punto Unicode, sin regex con caracteres combinantes literales) y `update` además rechaza con `ConflictException` CUALQUIER cambio de `nombre` o `activo` sobre una categoría `bloqueada` (las 6 seedeadas). Violación de nombre único (`P2002`) → `ConflictException` en ambos, igual que `brands`. Sin `@Roles`: gestionar categorías no está en la lista de exclusiones de `SELLER` de BLUEPRINT §5.1 — la restricción real de esta entidad es `bloqueada`, no el rol (la separación OWNER-only es para `expenses`/`resultados` en sí, T6.2+, ya decidida en la Fase 06). **Tests**: 17 unitarios nuevos (`expense-categories.service.spec.ts`, Prisma mockeado, cubre `findAll`/`create`/`update`, `it.each` con 6 variantes de nombre-mercadería con/sin tilde/mayúsculas/substring, ambos campos de `bloqueada` probados por separado) + 15 de integración nuevos (`expense-categories.integration.spec.ts`, Postgres real, patrón de `catalog.integration.spec.ts`: 401 sin sesión, SELLER puede crear, 409 duplicado, `it.each` de 5 nombres-mercadería vía HTTP, PATCH rename/baja lógica, PATCH 404, PATCH mercadería sobre categoría no bloqueada, mass-assignment 400, y el caso real contra la categoría seedeada "Otros" — rechaza rename y desactivación, confirmado que sigue activa leyendo la fila directo de la base después del intento). Suite completa corrida de nuevo tras el cambio: 435/435 unitarios, 369/369 integración (Postgres real, contenedor `manitasv2-postgres-1` healthy). `tsc --noEmit` limpio, `npm run build` limpio, `eslint "{src,apps,libs,test}/**/*.ts"` limpio (9 hallazgos iniciales, los 9 formato/prettier en archivos propios de este ticket, corregidos con `--fix` y reverificados con suite completa de nuevo). Sin migración nueva (`ExpenseCategory` ya vivía en el schema desde la Fase 01). | agente |
| 2026-08-28 | T6.2 — Fase 04a (tests primero), registro de gastos | ROJO (esperado) | commit `848da6f` en rama `feat/T6.2-registro-gastos`, sesión aislada (subagente separado de la sesión de implementación) — no se leyó ninguna implementación de `expenses` (que no existía), solo el ticket T6.2 de `ROADMAP.md`, BLUEPRINT §9.3/§9.4/§9.7 e invariante 10, y la tabla de errores de la spec del módulo sección 7, más la ESTRUCTURA mecánica ya cerrada (`expense-categories.integration.spec.ts`, `cash-registers.controller.ts`) solo como patrón de wiring, nunca como fuente de lógica. Alcance acotado explícitamente a T6.2 (registrar el gasto: categoría existe/activa, monto positivo y con ≤2 decimales, `medioPago` único del enum, idempotencia, `OWNER`-only) — **sin** chequeo de sesión de caja ni creación de `cash_movement` para ningún `medioPago` (eso es T6.3, precedente ya establecido por `returns` T5.1/T5.3). **13 tests unitarios** (`expenses.service.spec.ts`, Prisma mockeado, patrón `MockTx`: camino feliz ×3 `medioPago`, categoría inexistente/inactiva, monto ≤0 ×3, monto con más de 2 decimales ×3, `idempotencyKey` intacto hasta el `create`, `userId` del parámetro de auth) + **18 de integración** (`expenses.integration.spec.ts`, Postgres real: 401/403 en `POST`/`GET`, camino feliz ×3 `medioPago`, sin header `Idempotency-Key`, 404/400 de categoría, monto inválido ×5, doble POST mismo `Idempotency-Key` sin duplicar fila, mass-assignment, `GET` 200 `OWNER`). Incluye el stub mínimo de `ExpensesService` (`registrarGasto`/`findAll` tiran `Error('T6.2 todavía no implementado')`, mismo patrón que el stub de `StockService` en T2.4) y el wiring real de `ExpensesController` (rutas, `@Roles(OWNER)`, `IdempotencyInterceptor`) para que la integración le pegue a un endpoint real. **Verificado de forma independiente por la sesión de implementación** (no solo tomado del reporte del subagente): `tsc --noEmit` limpio; 13/13 unitarios en rojo, todos por la excepción del stub; 10/18 de integración en rojo por el stub (500 en vez del status esperado), **8/18 ya en verde de antemano** porque ejercitan infraestructura ya VERDE ajena a T6.2 (`AuthGuard`/`RolesGuard`/`ValidationPipe` global con `whitelist`+`forbidNonWhitelisted`+formato de `@IsDecimal`) — ningún caso pasó por lógica de negocio propia de T6.2, que sigue sin existir. `backend/.env.example` no tocado. | agente |
| 2026-08-28 | T6.2 — Registro de gastos con medio de pago, idempotente | VERDE | `backend/src/modules/expenses/{expenses.service.ts,expenses.controller.ts,dto/find-expenses-query.dto.ts}`. `registrarGasto`: valida monto positivo y ≤2 decimales (`assertPositive` + chequeo de `decimalPlaces()`, mismo criterio que `sales`/`cash-registers`/`products`), lee la categoría (404 si no existe, 400 si `activo:false`), crea el `Expense` con `fecha` autogenerada, `idempotencyKey` intacta hasta el `create` (la detección de clave repetida es responsabilidad de `withIdempotency` en el controller, no del servicio — mismo patrón que `CashRegisterService.registrarMovimiento`). **Explícitamente sin** chequeo de sesión de caja ni creación de `cash_movement` para ningún `medioPago`, ni siquiera EFECTIVO — precedente ya usado por `returns` (T5.1 vs T5.3): eso es T6.3, el ticket siguiente. `findAll`: paginado servidor (`page`/`pageSize`, mismo patrón que `VariantsService.search`), orden por `fecha` descendente, filtro opcional `desde`/`hasta` sobre `fecha` (sin conversión a hora argentina — esa conversión es AD-13/T0.7, para `resultados`, no para este listado crudo) — completa el contrato de `GET /expenses` de la spec del módulo sección 4; no estaba en el alcance mínimo de la Fase04a, se agregó en la implementación con sus propios tests (regla 8 de CLAUDE.md). `POST`/`GET /expenses` siguen `OWNER`-only (`@Roles`, ya cableado en la Fase04a). **Corrección a un test heredado de la Fase04a**: el test de idempotencia esperaba `.expect(200)` estricto en el segundo `POST` con la misma clave — se corrigió a `[200, 201]` porque ningún controller del proyecto distingue el status HTTP según si `withIdempotency` creó la fila o devolvió la existente (NestJS responde 201 por default en todo `@Post()`); mismo criterio ya auditado en `sales-controller.integration.spec.ts` ("caso 5 — doble click... responde 200/201") — la garantía real de BLUEPRINT §9.7 se verifica al nivel de datos (mismo `id`, una sola fila), no al nivel de status HTTP exacto. No es un debilitamiento: la aserción de datos sigue intacta. **Tests**: Fase04a dejó 13 unitarios + 18 de integración (31 verificados en rojo, luego en verde tras la implementación, más la corrección de idempotencia arriba) — esta fase sumó 4 unitarios (`findAll`: paginación skip/take, where sin filtro, where con `gte`/`lte`, forma del resultado) y 4 de integración (`findAll`: forma paginada, orden descendente, filtro por rango, sin filtro trae todo). Total del ticket: **17 unitarios + 22 de integración nuevos**. Suite completa: 452/452 unitarios, 391/391 integración (Postgres real). `tsc --noEmit`, `npm run build` y `eslint "{src,apps,libs,test}/**/*.ts"` limpios. `backend/.env.example` no tocado. Sin migración nueva. | agente |
| 2026-08-28 | T6.3 — Fase 04a (tests primero), gasto en efectivo vinculado a caja | ROJO (esperado) | commit `61d3114` en rama `feat/T6.3-gasto-efectivo-caja`, sesión aislada — a diferencia de una Fase04a "normal", este ticket EXTIENDE `ExpensesService.registrarGasto` (ya cerrado y VERDE en T6.2): se permitió leer `expenses.service.ts`/`.controller.ts`/`.module.ts` tal como quedaron en T6.2 (el contrato público sobre el que se construye, no la implementación del ticket bajo prueba), y `cash-register.service.ts`/`returns.service.spec.ts` (T5.3, ya cerrado, otro módulo) solo como patrón MECÁNICO de `getSesionAbiertaOrThrow`/`registrarMovimiento` y de cómo mockear `cashRegisterService` — nunca como fuente de la lógica nueva, que salió únicamente del ticket T6.3 (`ROADMAP.md`) y BLUEPRINT invariantes 7/10. Cambio estructural mínimo (el equivalente al "stub" de una Fase04a normal): `ExpensesService` gana `cashRegisterService: CashRegisterService` como segundo parámetro del constructor y `expenses.module.ts` importa `CashRegistersModule` — el CUERPO de `registrarGasto` quedó sin tocar, exactamente el de T6.2. **3 tests unitarios nuevos** (EFECTIVO con sesión → llama `getSesionAbiertaOrThrow`+`registrarMovimiento` una vez cada uno; EFECTIVO sin sesión → propaga el 409, `expense.create` y `registrarMovimiento` nunca se llaman; TRANSFERENCIA/OTRO → ninguno de los dos colaboradores se llama, sin cambios). **3 casos de integración nuevos + 1 reorganizado**: el `it.each([EFECTIVO,TRANSFERENCIA,OTRO])` del camino feliz de T6.2 se separó (TRANSFERENCIA/OTRO intacto; EFECTIVO pasa a su propio test que abre sesión primero y verifica el `cash_movement`) — documentado como corrección justificada, no debilitamiento: el propio comentario de T6.2 ya avisaba "eso lo restringe T6.3, no éste". Más: EFECTIVO sin ninguna sesión en el sistema → 409, cero filas en `expenses`; TRANSFERENCIA/OTRO sin ninguna sesión en absoluto (ni abierta ni cerrada) → sigue en 201, cero `cash_movements`; doble POST idempotente de un gasto EFECTIVO → una sola fila en `expenses` Y una sola en `cash_movements`. Agregó helpers (`abrirSesion` vía HTTP, `closeAnyOpenSessionDirect`, `limpiarTodasLasSesiones`) y reforzó la limpieza de `afterAll` (borrado de gastos huérfanos por `expenseCategoryId`, no solo por `createdExpenseIds`) para que el test todavía-en-rojo que crea un gasto sin capturar su id no rompa el `afterAll` por la FK. **Verificado de forma independiente por la sesión de implementación**: `tsc --noEmit` limpio; 2/2 nuevos unitarios en rojo (uno queda verde de entrada porque TRANSFERENCIA/OTRO no cambian) por ausencia de la funcionalidad — `toHaveBeenCalledTimes` en 0 en vez de 1, o una promesa resuelta en vez de rechazada —, nunca por compilación; 3/3 nuevos de integración en rojo por el mismo motivo (500→201 sin movimiento, o 201 en vez de 409), las 22 preexistentes de T6.2 sin tocar siguen en verde. `backend/.env.example` no tocado. | agente |
| 2026-08-28 | T6.3 — Gasto en efectivo → movimiento de caja vinculado | VERDE | `backend/src/modules/expenses/expenses.service.ts` — `registrarGasto` gana, solo para `medioPago = EFECTIVO`: `getSesionAbiertaOrThrow(tx)` fail-fast ANTES de crear el gasto (409 "No hay una sesión de caja abierta" si no hay ninguna, invariante 10), y `cashRegisterService.registrarMovimiento(tx, {sessionId, tipo: GASTO, monto, referenciaTipo: EXPENSE, referenciaId: <id del gasto recién creado>, descripcion, userId})` DESPUÉS de crearlo (invariante 7 — el signo negativo lo aplica `registrarMovimiento` internamente, quien llama siempre manda el monto positivo). La sesión se lee UNA sola vez (se reutiliza `sesion.id` para el movimiento) — el segundo lock que `registrarMovimiento` hace internamente sobre la misma fila es redundante pero inofensivo (misma transacción/conexión). `TRANSFERENCIA`/`OTRO` siguen exactamente como en T6.2, sin tocar ninguno de los dos pasos de caja. Orden de validaciones preservado tal cual la spec del módulo, sección 5: monto → categoría → (EFECTIVO) sesión → crear → (EFECTIVO) movimiento. **Tests**: los 5 casos que la Fase04a dejó en rojo (2 unitarios + 3 integración) pasan a verde sin modificar ninguna aserción — se verificó archivo por archivo que el cuerpo de los tests no cambió, solo el código de producción. Suite completa: 456/456 unitarios, 394/394 integración (Postgres real, contenedor `manitasv2-postgres-1` healthy) — sin ninguna regresión sobre las 452/391 de T6.2. `tsc --noEmit`, `npm run build` y `eslint "{src,apps,libs,test}/**/*.ts"` limpios (1 hallazgo real corregido: `no-unsafe-assignment` por un `expect.anything()` anidado dentro de un objeto — se resolvió sacando `monto` de ese `objectContaining` y verificándolo aparte con su valor exacto, mismo criterio que `returns.service.spec.ts` ya usa para no fijar cada campo de un `toHaveBeenCalledWith`; más 2 de formato vía `--fix`). `backend/.env.example` no tocado. Sin migración nueva (`CashMovementTipo.GASTO`/`CashMovementReferenciaTipo.EXPENSE` ya existían en el schema desde la Fase 01). Etapa 6: T6.1–T6.3 VERDE — el bloqueo de T0.7 sigue acotado a T6.5/T6.7. | agente |
| 2026-08-28 | T6.4 — Fase 04a (tests primero), consulta de resultados | ROJO (esperado) | commit `1b93f5c` en rama `feat/T6.4-consulta-resultados`, sesión aislada — código enteramente nuevo (a diferencia de T6.3), así que valió el "stub" clásico de una Fase04a normal. Fuente única: el ticket T6.4 de `ROADMAP.md`, BLUEPRINT §5.6 (fórmula textual completa) y la tabla de errores/contratos de la spec del módulo — ningún archivo `resultados*` existía para leer, y no se copió lógica de `expenses.service.ts`; sí se referenció como patrón MECÁNICO `SalesService.reconciliar()` (lectura pura en su propia transacción `RepeatableRead`) y `ReturnsService.consultarCredito()` (`tx.<modelo>.aggregate({ where, _sum })`). **Alcance acotado explícitamente**: T6.4 NO incluye la conversión a hora argentina (AD-13) — eso es T6.5, ticket separado que depende de T0.7 (todavía inexistente); `desde`/`hasta` se interpretan como límites UTC ingenuos (`desde 00:00:00.000Z` a `hasta 23:59:59.999Z`), mismo criterio ya usado en `ExpensesService.findAll` (T6.2). Lo que SÍ es de T6.4 (estructural, no de zona horaria): filtrar siempre por la fecha de la CABECERA (`sales.fecha`/`returns.fecha`/`expenses.fecha`), nunca por `created_at` de las tablas de ítems — y los dos filtros no relacionados con fecha (RN-8: `sales.estado = COMPLETADA` idéntico para ingresos y CMV; `return_items.reingresaStock = true` para la reversión del CMV). Tampoco incluye rankings (T6.6) ni los casos armados a mano de un escenario completo (T6.7, depende de T6.5). El archivo de test unitario deja fijado como CONTRATO el shape exacto de las 5 queries (`tx.sale.aggregate`, `tx.return.aggregate`, `tx.saleItem.findMany`, `tx.returnItem.findMany`, `tx.expense.aggregate`) que la Fase 04 tiene que reproducir literal. **9 tests unitarios** (`resultados.service.spec.ts`, Prisma mockeado sin `tx` recibido — el propio método abre su transacción): camino feliz con números calculados a mano fuera del código (ingresos $300, cmv $200, margenBruto $100, margenBrutoPct 33.33%, gastos $40, resultadoNeto $60); filtro 1 (`where.estado: COMPLETADA` exacto en `sale.aggregate` y `saleItem.findMany`); filtro 2 (`where.reingresaStock: true` exacto); filtro 3 estructural (límites UTC ingenuos propagados a las 5 queries); `ingresos = 0 → margenBrutoPct = "0.00"` (nunca NaN/excepción); `desde > hasta → BadRequestException` antes de abrir la transacción (ningún mock de Prisma llamado); período sin datos → todo en `"0.00"`; `calculadoEn` es una fecha válida; `isolationLevel: RepeatableRead` verificado. **8 de integración** (`resultados.integration.spec.ts`, Postgres real): 401/403 (2 ya verdes de antemano, infraestructura ya cerrada), `desde > hasta` → 400, camino feliz con venta real (costo congelado real de la línea), filtro 1 con `salesService.anularVenta` (sin ruta HTTP, invocado directo vía `prisma.$transaction`), filtro 2 con una devolución real `reingresaStock: false` vía `POST /returns`, filtro 3 con un gasto insertado directo con `fecha` fuera de un rango angosto pero dentro de uno amplio, período sin datos → todo `"0.00"`. **Verificado de forma independiente por la sesión de implementación**: `tsc --noEmit` limpio; 9/9 unitarios en rojo por la excepción del stub (nunca compilación); 6/8 de integración en rojo por 500 en vez del status esperado (los 2 restantes, 401/403, ya en verde por infraestructura ajena a este ticket). `backend/.env.example` no tocado. | agente |
| 2026-08-28 | T6.4 — Consulta de resultados: ingresos, CMV, margen, gastos, resultado neto | VERDE | `backend/src/modules/expenses/resultados.service.ts` — implementa literal el contrato de queries que la Fase04a dejó fijado: `tx.sale.aggregate`/`tx.return.aggregate`/`tx.expense.aggregate` (`_sum` de una sola columna, patrón `ReturnsService.consultarCredito`) y `tx.saleItem.findMany`/`tx.returnItem.findMany` reducidos en JS con `Prisma.Decimal` (`cantidad × costoUnitario`, mismo criterio que `lineSubtotal`/`prorate` de `money.util.ts` — nunca `number`), las 5 dentro de un único `$transaction(..., { isolationLevel: RepeatableRead })` (patrón `SalesService.reconciliar`). `margenBrutoPct` redondeado a 2 decimales (`ROUND_HALF_UP`, mismo criterio que `roundCurrency` aunque no sea moneda en sentido estricto) y forzado a `0` cuando `ingresos` es cero, sin dividir por cero. Todos los importes de la respuesta salen como `string` de 2 decimales (`.toFixed(2)` sobre el `Decimal` ya redondeado). `desde`/`hasta` siguen interpretándose como límites UTC ingenuos — el alcance acotado de la Fase04a se mantuvo sin ampliarlo: no se tocó nada de AD-13/hora argentina, eso sigue siendo T6.5. **Tests**: los 9 unitarios + 6 de integración que la Fase04a dejó en rojo pasan a verde sin modificar ninguna aserción — se verificó con `git status`/`git diff` que solo cambió `resultados.service.ts`, ningún archivo de test. Suite completa: 465/465 unitarios, 402/402 integración (Postgres real, contenedor `manitasv2-postgres-1` healthy) — sin regresiones sobre T6.3 (456/394). `tsc --noEmit`, `npm run build` y `eslint "{src,apps,libs,test}/**/*.ts"` limpios, sin ningún hallazgo nuevo. `backend/.env.example` no tocado. Sin migración nueva (lectura pura, ninguna tabla nueva). Etapa 6: T6.1–T6.4 VERDE — T6.5/T6.7 siguen esperando T0.7; T6.6 (rankings) y T6.8/T6.9 (frontend) quedan disponibles para arrancar. | agente |
| 2026-08-28 | T0.7 — Zona horaria: helper de agrupación en hora argentina (AD-13) | VERDE | `backend/src/common/timezone/argentina-timezone.util.ts` — sin librería de fechas nueva (`date-fns-tz`/`luxon`): Node trae ICU completo (`Intl`), suficiente para una única zona horaria. **Decisión de esta sesión sobre Fase04a**: no se consideró obligatoria ni se aplicó aislada — es un helper puro sin plata/stock/caja de por medio (mismo criterio que `common/money/money.util.ts`, que tampoco la llevó), a diferencia de los tickets de Etapa 5/6 donde sí se aplicó voluntariamente por tocar dinero real. Igual se escribieron tests exhaustivos en el mismo ticket (regla 8 de CLAUDE.md). Expone `argentinaDayRangeToUtc(fechaYYYYMMDD)` (límites UTC exactos de un día calendario en hora argentina — medianoche a medianoche) y `argentinaCalendarDate(date)` (a qué día calendario de Argentina pertenece un instante UTC), más `argentinaWallClock(date)` (hora de pared legible, HH:mm:ss, solo para debug/logs). Implementación: dado que `Intl.DateTimeFormat` no puede formatear milisegundos, el offset de la zona se calcula siempre con `ms=0` y los milisegundos pedidos se suman aparte en aritmética entera — **se encontraron y corrigieron 2 bugs reales durante el propio desarrollo** (no en una fase de QA posterior): (1) sin ese cuidado, el límite `hasta` de un día (`23:59:59.999`) calculaba mal el offset por los 999ms perdidos en el formateo, empujando el resultado casi 1 segundo al día siguiente — detectado por el test de round-trip y el de fin de mes/fin de año; (2) `argentinaWallClock` devolvía la fecha completa en vez de solo la hora, por incluir campos `year`/`month`/`day` en el formateador — detectado por su propio test. **9 tests unitarios** (`argentina-timezone.util.spec.ts`, función pura, sin mocks, valores UTC/Argentina calculados a mano): el caso que BLUEPRINT exige literal (§5.6, "un test debe verificar que una venta de las 23:30 pertenece al día correcto" — una venta a las 23:30 hora argentina cae en el día UTC siguiente, y el helper la ubica en el día argentino correcto, con una aserción explícita de que el día UTC "ingenuo" da el resultado equivocado); caso de control del otro lado de la medianoche; mediodía (nunca cruza); límites exactos de un día (round-trip); un ms antes/después de cada límite cae en el día adyacente; fin de mes y fin de año; formato inválido rechazado; formato de hora de pared. Suite completa: 474/474 unitarios (sin integración — función pura, nada que probar contra Postgres), sin regresiones sobre T6.4 (465). Un flake transitorio de infraestructura (`users.integration.spec.ts`, timeout de `beforeAll` por contención de recursos tras varias corridas seguidas) se reprodujo como falso positivo y se descartó confirmando 18/18 en aislamiento y 402/402 en una corrida limpia posterior — no relacionado con este cambio (archivo puro, sin tocar ningún módulo existente). `tsc --noEmit`, `npm run build` y `eslint "{src,apps,libs,test}/**/*.ts"` limpios. `backend/.env.example` no tocado. Sin migración nueva. **No se tocó `ExpensesService.findAll` ni `ResultadosService.consultar`** (siguen con límites UTC ingenuos) — conectar este helper ahí es responsabilidad de T6.5, no de este ticket. T6.5 y T6.7 quedan desbloqueados. | agente |
| 2026-08-28 | T6.5 — Fase 04a (tests primero), hora argentina en resultados | ROJO (esperado) | commit `1942f9e` en rama `feat/T6.5-hora-argentina-resultados`, sesión aislada. Ticket muy acotado: corrige una sola parte de `ResultadosService.consultar` (ya cerrado en T6.4) — reemplazar el cálculo de límites UTC ingenuos por `argentinaDayRangeToUtc` (T0.7, ya VERDE, usada real sin mockear como fuente de verdad en los tests). Se permitió leer `resultados.service.ts`/`.spec.ts` y el archivo de integración de T6.4 completos (a diferencia de una Fase04a normal) porque el trabajo incluye actualizar 3 tests existentes cuyos propios nombres/comentarios ya avisaban que su expectativa (límites UTC ingenuos) era provisional — no se leyó ni infirió cómo se iba a modificar la implementación. **3 tests unitarios actualizados** (filtro 1, filtro 2, filtro 3 — renombrado de "mitad estructural" a "completo") con los límites esperados recalculados vía `argentinaDayRangeToUtc`, cada uno con comentario explicando por qué cambia (mismo criterio que el `it.each` de T6.2→T6.3: no es debilitar, es corregir una expectativa ya avisada como temporal). **+1 test unitario nuevo** (rango de más de un día: `gte` del inicio del primer día, `lte` del fin del último, nunca el mismo día aplicado a ambos extremos). **+2 tests de integración nuevos** (Postgres real): el obligatorio literal de BLUEPRINT §5.6 — venta a las 23:30 hora argentina de un día D (UTC `D+1 02:30`) aparece en `/resultados` de D y NO en el de D+1 (el día UTC ingenuo que un cálculo sin AD-13 usaría por error) — y su caso de control simétrico (venta a las 00:30 ART, que no cruza la medianoche hacia atrás, pertenece a D y no a D−1). Ningún otro test de los dos archivos se tocó. **No se modificó `resultados.service.ts`** — esta vez la Fase04a fue enteramente tests, sin ni siquiera un cambio estructural (la firma de `consultar` no cambia, no hay un colaborador nuevo que inyectar). **Verificado de forma independiente por la sesión de implementación**: `tsc --noEmit` limpio; 4/4 unitarios en rojo (los 3 actualizados + el nuevo de rango multi-día) por el límite ingenuo recibido en vez del esperado de `argentinaDayRangeToUtc` (nunca por compilación), 6 sin tocar en verde; 1/10 de integración en rojo (exactamente el test de las 23:30 — `ingresos` esperado `'150.00'`, recibido `'0.00'`, la venta cayendo en el día UTC en vez del argentino), los 9 restantes en verde (incluido el caso de control de las 00:30, que no cruza en ninguna interpretación). `backend/.env.example` no tocado. | agente |
| 2026-08-28 | T6.5 — Agrupación temporal en hora argentina (AD-13) + test de venta a las 23:30 | VERDE | `backend/src/modules/expenses/resultados.service.ts` — reemplaza el cálculo `new Date(`${query.desde}T00:00:00.000Z`)`/`...T23:59:59.999Z` (UTC ingenuo, T6.4) por `argentinaDayRangeToUtc(query.desde).desde`/`argentinaDayRangeToUtc(query.hasta).hasta` (T0.7) — dos cálculos independientes, uno por extremo del rango, para que un período de varios días tome el inicio del primer día y el fin del último en hora argentina, no el mismo día aplicado a ambos. Nada más cambió: la fórmula, los filtros 1/2 (estado COMPLETADA, reingresaStock), la transacción `RepeatableRead` y el shape de la respuesta siguen exactamente iguales a T6.4. **Tests**: los 4 unitarios + 1 de integración que la Fase04a dejó en rojo pasan a verde sin modificar ninguna aserción — confirmado con `git status`/`git diff` que solo cambió `resultados.service.ts`. Suite completa: 475/475 unitarios, 404/404 integración (Postgres real, contenedor `manitasv2-postgres-1` healthy) — sin regresiones sobre T0.7 (474/402). `tsc --noEmit`, `npm run build` y `eslint "{src,apps,libs,test}/**/*.ts"` limpios. `backend/.env.example` no tocado. Sin migración nueva. Etapa 6: T6.1–T6.5 VERDE — **T6.7 (tests de cálculo con casos armados a mano) queda desbloqueado**, ya no depende de nada pendiente. T6.6 (rankings) y T6.8/T6.9 (frontend) siguen disponibles. | agente |
| 2026-08-28 | T6.6 — Fase 04a (tests primero), rankings de productos y gastos por categoría | ROJO (esperado) | commit `6a18b66` en rama `feat/T6.6-rankings`, sesión aislada — código nuevo dentro de un archivo ya cerrado (`resultados.service.ts`/`.controller.ts`, T6.4/T6.5): se permitió leer ambos tal como existían (contrato ya cerrado, no la lógica del ticket bajo prueba) y usar `argentinaDayRangeToUtc` (T0.7) real sin mockear, como ya hace `consultar`. Dos endpoints nuevos en el mismo `ResultadosController`. **`GET /resultados/ranking-productos`**: `orden=unidades|margen` (opcional, default `'unidades'` — decisión de esta fase, sin marcarlo obligatorio). Fórmula por producto con la MISMA asimetría que `/resultados` a nivel de período completo (RN-10, "mismos filtros de RN-8"): toda devolución resta unidades/ingresos (el cliente recuperó la plata, pase lo que pase con la prenda — `sale_items.neto_linea`/`return_items.neto_linea`, invariantes 12/11), pero el costo (y por lo tanto el margen) solo se revierte si `reingresaStock: true`. Desempate por `variantId` ascendente. **`GET /resultados/gastos-por-categoria`**: `total` por categoría, descendente, desempate por `nombre` ascendente. Ambos `OWNER`-only, `desde > hasta` → 400, período sin datos → `200 []`. **13 tests unitarios** (`resultados.service.spec.ts`): `rankingProductos` (8 — camino feliz por `unidades` y por `margen` con los mismos datos para confirmar que el orden se invierte, default sin `orden`, devolución `reingresaStock: false` que no toca el costo, desempate por `variantId`, `where`/límites exactos incluyendo que `return_items` NO filtra por `reingresaStock` en el `where` — se filtra en JS porque afecta unidades/ingresos distinto que costo —, `desde > hasta`, período vacío) y `gastosPorCategoria` (5 — camino feliz con 2 categorías, desempate por `nombre`, `where`/límites exactos, `desde > hasta`, período vacío). **13 de integración** (`resultados.integration.spec.ts`, Postgres real): mismos casos de punta a punta, más `orden` inválido → 400 (`@IsIn` del `ValidationPipe`, ya verde de antemano). **Implementación mínima**: `RankingProductosQueryDto`/`GastosPorCategoriaQueryDto` (extienden `ResultadosQueryDto`), stubs `rankingProductos`/`gastosPorCategoria` en `ResultadosService` (mismo patrón `Error` que T6.2/T6.4), dos rutas nuevas en el controller. **Verificado de forma independiente por la sesión de implementación**: `tsc --noEmit` limpio; 13/13 unitarios en rojo por el stub, los 10 de `consultar` (T6.4/T6.5) sin tocar en verde; 8/13 de integración en rojo por 500 (los 5 restantes ya verdes de antemano: 401/403/orden-inválido, infraestructura ajena a este ticket), los 10 de `consultar` sin tocar en verde. Sin bloqueos ni ambigüedades — la spec alcanzó para derivar todos los casos. `backend/.env.example` no tocado. | agente |
| 2026-08-28 | T6.6 — Rankings: productos por unidades vendidas y por margen; gastos por categoría | VERDE | `backend/src/modules/expenses/resultados.service.ts` — `rankingProductos` implementa literal el contrato de queries de la Fase04a: `saleItem.findMany`/`returnItem.findMany` (nunca `groupBy`, porque `cantidad × costoUnitario` no es una columna) reducidos en un `Map<variantId, {unidades, ingresos, cmv}>` con `Prisma.Decimal`, el JOIN de `return_items` hacia `variantId` pasa por `saleItem.variantId` (la tabla no lo tiene propio). `gastosPorCategoria` reduce `expense.findMany` (con `expenseCategory: {select: {nombre}}` incluido, evitando un segundo viaje a la base) en un `Map<expenseCategoryId, {nombre, total}>`. Ambos dentro de su propia transacción `RepeatableRead`, mismo patrón que `consultar`. **Corrección a un test heredado de la Fase04a**: el test de `gastos-por-categoria` con rango amplio asumía ser la única categoría del período — como el describe hermano "camino feliz" usa fechas del mismo día, un rango amplio real legítimamente trae ambos fixtures; se corrigió la aserción para buscar la categoría propia dentro del resultado en vez de exigir un array de un solo elemento (no es un debilitamiento: la aserción de datos exactos sobre esa categoría sigue intacta, solo dejó de asumir exclusividad de un dataset compartido). También se actualizó un comentario de `resultados-query.dto.ts` que había quedado desactualizado desde T6.5 (seguía describiendo el límite como "UTC ingenuo"). **Tests**: los 13 unitarios + 8 de integración que la Fase04a dejó en rojo pasan a verde (más la corrección de arriba) sin debilitar ninguna aserción de datos. Suite completa: 488/488 unitarios, 417/417 integración (Postgres real, contenedor `manitasv2-postgres-1` healthy) — sin regresiones sobre T6.5 (475/404). `tsc --noEmit`, `npm run build` y `eslint "{src,apps,libs,test}/**/*.ts"` limpios (2 hallazgos reales corregidos: una variable sin usar y dos `expect.any(String)` disparando `no-unsafe-assignment` dentro de un objeto tipado, resueltos con `as string` — mismo patrón ya usado en `prices-bulk-update.integration.spec.ts`). `backend/.env.example` no tocado. Sin migración nueva. Etapa 6: T6.1–T6.6 VERDE. Quedan T6.8/T6.9 (frontend, sin Fase04a) y T6.7 (tests de cálculo a mano, ya desbloqueado desde T6.5). | agente |
| 2026-08-28 | T6.7 — Tests de cálculo con casos armados a mano (incluyendo devoluciones) | VERDE | `backend/test/integration/resultados-escenario-completo.integration.spec.ts` (nuevo, Postgres real). A diferencia de todos los tickets anteriores de la Etapa 6, este no implementa nada — el módulo `resultados` (T6.4–T6.6) ya estaba completo y cerrado; es una **verificación independiente** (spec del módulo, sección 9, textual: "no alcanza con probar que la fórmula del código coincide con la fórmula del código — eso no detecta un error conceptual compartido"). Ejecutado por un subagente con la restricción inversa a T6.5/T6.6: **nunca leyó** `resultados.service.ts` ni `resultados.controller.ts`, ni antes ni después de calcular — única fuente de la fórmula, BLUEPRINT §5.6 textual (pegado en el prompt) más invariantes 11/12, AD-7, AD-13. Solo se permitió leer otros archivos de integración ya cerrados (`sales`/`sales-anulacion`/`returns`/`returns-controller`/`cash-registers`) como referencia MECÁNICA (cómo crear una venta real, anularla, devolver con `reingresaStock`, abrir sesión) — nunca como fuente de valores esperados. **Escenario armado** (año exclusivo 2036, confirmado sin colisión con otros fixtures): 2 ventas `COMPLETADA` con variantes/precios/costos distintos (venta 1: 3 remeras $100/costo $40 + 1 pantalón $200/costo $90 = $500; venta 2: 4 buzos $150/costo $70 = $600); 1 venta creada y ANULADA con `salesService.anularVenta` (2 camperas, fechada DENTRO del período a propósito, para probar que la excluye el filtro de ESTADO, no el de fecha); 1 devolución `reingresaStock: true` (1 remera, $100, resta ingreso y costo); 1 devolución `reingresaStock: false` (2 buzos, $300, resta ingreso, el costo queda); 1 gasto en efectivo (categoría "Insumos de limpieza", $150, con sesión abierta) y 1 por transferencia (categoría "Alquiler", $800); 1 venta y 1 gasto de control fechados fuera del rango (2036-05-10, otro día), que no tienen que aparecer en ninguna respuesta. Verificado contra las tres consultas: `GET /resultados` (ingresos $700.00 = $1100−$400, cmv $450.00 = $490−$40, margenBruto $250.00, margenBrutoPct 35.71%, gastos $950.00 = $150+$800, resultadoNeto −$700.00), `GET /resultados/ranking-productos` (por unidades, empate Remera/Buzo en 2 desempatado por `variantId`; por margen, Remera $120 > Pantalón $110 > Buzo $20) y `GET /resultados/gastos-por-categoria` (Alquiler $800 > Insumos de limpieza $150). **El sistema real coincidió con el cálculo a mano en el primer intento, sin ajustar ningún `expect`** — sin hallazgos, ninguna corrección necesaria en `resultados.service.ts`. **Verificado de forma independiente por la sesión de implementación** (no solo tomado del reporte del subagente): corrida propia del archivo (1/1 verde), suite completa 488/488 unitarios y 418/418 integración (417+1, contenedor `manitasv2-postgres-1` healthy, sin regresiones sobre T6.6), `tsc --noEmit`/`eslint "{src,apps,libs,test}/**/*.ts"` limpios, revisión del archivo completo confirmando que el cálculo se muestra íntegro en comentarios antes de cada `expect` y que el header documenta explícitamente qué se leyó (mecánico) y qué nunca se leyó (la fórmula del código). `backend/.env.example` no tocado. Sin migración nueva, sin cambios de producción. Etapa 6 completa en su bloque de cálculo: **T6.1–T6.7 VERDE**. Quedan T6.8/T6.9 (pantallas frontend, OWNER-only, sin Fase04a). | agente |
| 2026-08-28 | T6.8 — Pantallas de gastos y de resultados (solo OWNER) | VERDE | `frontend/src/features/expenses/` (nuevo): `types.ts`/`api.ts` (contratos de `expense-categories`/`expenses`/`resultados`, T6.1–T6.6), `GastosPage.tsx` (listado paginado en el servidor — §12.4, "lo último siempre arriba" ya lo da `GET /expenses` — + botón de alta), `ResultadosPage.tsx` (resumen ingresos/CMV/margen/gastos/resultado neto + `calculadoEn`, RN-9, + ranking de productos con toggle unidades/margen + gastos por categoría, todo sobre el mismo rango de fechas), `components/RegistrarGastoModal.tsx` (categoría/descripción/monto/medio de pago, idempotente vía `useIdempotencyKey` — mismo patrón que `ManualMovementModal` de `cash-registers`) y `components/NuevaCategoriaModal.tsx` (alta rápida de categoría sin salir del formulario de gasto, AD-7 lo valida el backend, acá solo se muestra el mensaje tal cual). Rutas nuevas `/gastos` y `/resultados` agregadas al bloque `RequireOwner` ya existente en `router.tsx` (RN-2/RN-11: tan restringido como el propio módulo de resultados, BLUEPRINT §5.1 — un gasto revela montos contra categorías sensibles); nav links de `AppLayout.tsx` condicionados a `isOwner`, mismo criterio que el resto de la UI (`RequireOwner` ya venía de T2.12/AMB-11, reusado sin cambios). `lib/format.ts` gana `formatPercent` (regla 9, CLAUDE.md: nunca formatear a mano en un componente) para `margenBrutoPct`. **Verificado en el navegador de punta a punta** (backend+frontend reales, login OWNER real): alta de gasto por transferencia con categoría seedeada, alta rápida de categoría nueva (queda auto-seleccionada en el formulario), rechazo AD-7 en vivo ("Compra de mercadería" → mensaje del backend mostrado tal cual), validación de rango inválido en `/resultados` (cliente, sin llegar a pegarle al backend), resumen/ranking/gastos-por-categoría reflejando el gasto recién cargado — sin errores inesperados en la consola del navegador (los únicos `error` registrados son los 401 previos al login y el 400 esperado del rechazo AD-7 en vivo). No se verificó el bloqueo real para `SELLER` contra un usuario `SELLER` corriendo (no hay ninguno seedeado) — se confía en `RequireOwner`, componente ya reusado sin cambios de otras rutas OWNER-only ya en producción (grilla de variantes, alta de variante suelta). Sin tests de componente nuevos — mismo criterio ya establecido en todo el frontend (`find src -name "*.test.tsx"` da cero resultados en todo el proyecto): la disciplina de testing del frontend es extraer lógica pura a un `.ts` y probar eso (`calc.ts`, `cart.ts`, `payments.ts`, `draft-storage.ts`, `grid.ts`) — esta pantalla no tiene lógica de cálculo propia, todo el cálculo vive en el backend ya probado en T6.4-T6.7. `npx tsc -b --force` y `npx eslint .` limpios (3 hallazgos reales corregidos durante el desarrollo: dos `react-hooks/set-state-in-effect` por llamar `setState` síncrono dentro de un efecto en vez de ajustar durante el render — mismo patrón ya usado en `CatalogPage` — y dos `@typescript-eslint/no-unnecessary-type-assertion` por un cast redundante sobre un valor que Mantine ya infería del tipo de `data`). Suite de tests del frontend sin regresiones: 84/84 (sin tests nuevos, ninguno roto). `npm run build` limpio — bundle 677.82 kB / 204.06 kB gzip (crecimiento de +17.5 kB / +4.1 kB gzip sobre el bundle de T5.8, razonable para dos pantallas + 2 componentes nuevos; TD-10 sin agravarse de forma alarmante). `backend/.env.example` no tocado. Sin cambios de backend en este ticket — todo el contrato ya estaba cerrado desde T6.1–T6.6. Etapa 6: **T6.1–T6.8 VERDE**. Queda solo T6.9 (pantalla de configuración). | agente |
| 2026-08-28 | T6.9 — Pantalla de configuración (solo OWNER) | VERDE | Alcance real distinto del asumido inicialmente: T0.13 solo había construido la tabla `settings` + el servicio TIPADO interno (`SettingsService`, consumido por `sales`/`returns`/`cash-registers`) — **nunca existió una ruta HTTP**. T6.9 tuvo que agregar esa capa además de la pantalla. **Backend**: `SettingsService` gana `findAll()` (`GET /settings`) y `setValor(clave, valorRaw, userId)` — dispatcher genérico por el TIPO REAL de la fila (nunca confía en lo que mande el cliente): `valorRaw` viaja siempre como `string` (mismo criterio que `Expense.monto`, BLUEPRINT §9.3) y se valida el formato (`"true"/"false"` para BOOL, regex de enteros para INT, `new Prisma.Decimal` en un `try/catch` para DECIMAL) ANTES de delegar a los setters tipados ya existentes (`setBool`/`setInt`/`setDecimal`, T0.13) — así un valor mal formado da 400 en vez de un 500 sin controlar (era el primer caller HTTP real de esos setters). `SettingsController` nuevo (`GET`/`PATCH /settings/:clave`, ambos `@Roles(OWNER)` — BLUEPRINT §3.8 literal: "Solo OWNER los modifica", la lectura también queda restringida porque esta ruta es específicamente para la pantalla de administración) registrado en el mismo `SettingsModule` ya existente (alcanzable desde `AppModule` de forma transitiva, vía `cash-registers`/`sales`/`returns`, sin necesidad de importarlo directo). **Frontend**: `features/settings/` nuevo — `SettingsPage.tsx` (las 4 filas con metadata de presentación fija, orden de BLUEPRINT §10, no alfabético) + `SettingField.tsx` (BOOL se guarda al tocar el switch, INT/DECIMAL editan en un campo local con "Guardar" habilitado solo si cambió). Ruta `/configuracion` sumada al mismo bloque `RequireOwner` de T6.8; nav link condicionado a `isOwner`. **Corrección de un error real durante esta sesión**: al escribir los tests de integración nuevos se **sobreescribió sin leer primero** un archivo ya existente de T0.13 (`test/integration/settings.integration.spec.ts`, 145 líneas, 4 tests contra `SettingsService` directo con claves de prueba propias) — detectado por un conteo de tests que no cerraba (esperado 430, la corrida daba 426) en vez de por una revisión previa; se restauró el archivo original exacto vía `git checkout <commit> --` y los tests HTTP nuevos se movieron a un archivo aparte, `settings-controller.integration.spec.ts` (mismo criterio ya establecido por `sales.integration.spec.ts` vs `sales-controller.integration.spec.ts`: service-level y HTTP-level separados). **Tests nuevos**: 8 unitarios (`findAll` + `setValor` por los 3 tipos, formato inválido de cada uno, clave inexistente) en `settings.service.spec.ts`; 12 de integración HTTP en `settings-controller.integration.spec.ts` (401/403/200 en ambos métodos, camino feliz por tipo con `updatedByUserId` verificado, formato inválido → 400, clave inexistente → 404, valor vacío → 400, mass-assignment → 400) — cada test que modifica una de las 4 filas GLOBALES la restaura a su valor original en un `finally`, para no dejarle a `sales`/`returns`/`cash-registers` un default corrompido. **Verificado en el navegador de punta a punta** (backend+frontend reales, login OWNER real, sin poder usar `computer` por coordenadas porque el panel del navegador estaba oculto — se usó `read_page`/`javascript_tool` para completar y disparar los guardados): los 4 valores sembrados se cargan correctos, guardado de INT (`dias_plazo_devolucion` 30→45, confirmado en la base, restaurado a 30) y de BOOL (`permitir_venta_sin_stock` false→true, restaurado a false) reflejan `Última modificación` con fecha/hora en formato es-AR; suite completa de integración reconfirmada 33/33 suites, 430/430 tests después de la prueba manual, sin ningún default corrompido. Suite completa: 496/496 unitarios backend (sin cambios frontend en Vitest, 84/84 sin regresiones — sin tests de componente nuevos, mismo criterio ya establecido: la validación de formato vive en el backend, ya probada). `tsc`/`eslint`/build limpios en ambos proyectos (2 hallazgos reales de frontend corregidos: `react-hooks/set-state-in-effect` en la sincronización del borrador de `NumberField` con el valor del padre). Bundle frontend 684.48 kB / 205.89 kB gzip. `backend/.env.example` no tocado. Sin migración nueva (`Setting` ya existía desde T0.13). **Etapa 6 completa: T6.1–T6.9 VERDE — el último módulo del MVP.** | agente |
| 2026-08-29 | Módulo `expenses`/`resultados` — Fase 07 (Cierre de implementación) | VERDE | `state/reports/modulo-expenses-resultados-fase07.md`. Contraste completo contra la spec del módulo: los 8 contratos de API de la sección 4 implementados, nada de la spec sin cubrir, el gap explícito ("sin DELETE físico") respetado. Invariantes 7/9/10 verificados con evidencia (código + tests). `@Roles` y mensajes de error verbatim contra las tablas de permisos/errores de la spec, sin discrepancias. **1 hallazgo real de duplicación, corregido**: `ResultadosService` repetía idéntica la resolución de límites + validación de rango de fechas en sus tres métodos (`consultar`/`rankingProductos`/`gastosPorCategoria`) — unificado en un `resolverRango()` privado, sin cambio de comportamiento (82 tests unitarios de los 4 servicios del módulo en verde antes y después, sin tocar ninguna aserción). Sin `TODO`/código muerto (`grep` sobre los 4 directorios del módulo, backend y frontend). Cobertura de tests confirmada exhaustiva sobre reglas de negocio, no solo caminos felices. Suite completa: 496/496 unitarios, 430/430 integración (Postgres real), `tsc`/`eslint`/`build` limpios. **No se declara el módulo terminado** — sigue Fase 08 (QA adversarial + Stryker, completa para las dos mitades por decisión ya tomada en la Fase 06) y luego Fases 09→12. `backend/.env.example` no tocado. | agente |
| 2026-08-29 | Módulo `expenses`/`resultados` — Fase 08 (QA adversarial) | VERDE | `state/reports/modulo-expenses-resultados-qa-2026-08-29.md` — rama `fase08-qa-expenses-resultados` (ramificada de Fase 07 VERDE). **Testing de mutación (Stryker, obligatorio): antes 84.62% global, con `expense-categories.service.ts` en 75.31% BLOQUEADO** (20 sobrevivientes) **→ después 98.85% global, los 3 archivos por encima del umbral de 80%** (`expense-categories.service.ts` 98.77%, `expenses.service.ts` 100.00%, `resultados.service.ts` 98.40%) — remediado con tests dirigidos a cada sobreviviente real: mensajes de error exactos, propagación de errores no-P2002, límites Unicode exactos del filtro de diacríticos (U+0300/U+036F), boundary exacto de "≤ 2 decimales", filtro `desde`/`hasta` independiente entre sí, `count()` con el mismo `where` que la página, aislamiento `RepeatableRead` en los dos métodos de ranking, devolución huérfana, `reingresaStock: true` en el ranking, y varios desempates reconstruidos con datos que sí distinguen la implementación real de la mutada (los desempates viejos "sobrevivían" por coincidencia de los datos de prueba, no porque el código estuviera mal). 3 sobrevivientes finales documentados como equivalentes/inalcanzables (detalle y razonamiento completo en el reporte), no forzados. **Hallazgo real (severidad Alta/Media), corregido y probado**: `@IsDateString()` (class-validator) y el regex de formato de `argentinaDayRangeToUtc` (T0.7) solo validaban FORMATO, nunca que el día existiera en el mes/año — `2026-02-30` pasaba ambos chequeos y `Date.UTC` lo "rodaba" en silencio al 2 de marzo, devolviendo 200 con el resultado de OTRO rango de fechas en vez de rechazar el pedido (`/resultados`, severidad Alta — cálculo financiero corrido sin aviso) y lo mismo en el listado crudo `/expenses` (severidad Media). **Corregido en dos puntos**: `argentinaDayRangeToUtc` ahora valida el round-trip calendario y tira `BadRequestException` (de paso, también se corrigió que el chequeo de formato tiraba un `Error` común sin capturar, que habría dado 500 en vez de 400); `find-expenses-query.dto.ts` gana un `ValidatorConstraint` propio (no reusa `argentinaDayRangeToUtc` a propósito — ese listado deliberadamente no convierte a hora argentina). Confirmado sin impacto en otros módulos (`argentinaDayRangeToUtc` solo lo consume este módulo). Categorías API (paginación, `orden` inválido, body con campos extra, idempotencia)/UI (reusa patrones ya auditados de `CatalogPage`/`ManualMovementModal`, sin hallazgos nuevos)/Security (autenticación, RBAC — incluida la decisión ya revisada de `expense-categories` sin `@Roles`—, IDOR vía `ParseIntPipe`, inputs maliciosos, exposición de información) revisadas sin hallazgos nuevos, detalle completo en el reporte. **Resultado de validación**: 514/514 unitarios (+18), 436/436 integración (+6, Postgres real), `tsc --noEmit`/`eslint`/`nest build` limpios. No se declara el módulo aprobado — sigue la Fase 09 (security audit, sin modificar código; Fases 09/11 aligeradas solo para los endpoints de solo lectura de `resultados`, nunca para `expenses`, por decisión ya tomada en la Fase 06). `backend/.env.example` no tocado. | agente |
| 2026-08-29 | Módulo `expenses`/`resultados` — Fase 09 (security audit) | VERDE | `state/reports/modulo-expenses-resultados-secaudit-2026-08-29.md` — rama `fase09-secaudit-expenses-resultados`, sin código modificado (regla de la fase). **Sin CRITICAL, HIGH ni MEDIUM.** Las 20 categorías del protocolo revisadas, la mayoría reconfirmadas EN VIVO contra el servidor real (no solo lectura de código): las 8 rutas del módulo sin sesión → 401; `SELLER` real (usuario creado y desactivado al terminar) → 403 en las 7 rutas `OWNER`-only, 200 en las 3 de `expense-categories` (decisión ya tomada y documentada, sin `@Roles` a propósito); `POST /expense-categories` con `Origin` cross-site + cookie real → 403 "Origen no autorizado", Origin legítimo sigue funcionando; mass-assignment (`bloqueada`/`id`/`userId` forzados) → 400 en los 3 endpoints mutadores; IDOR (`id` no numérico, `id`/`clave` inexistente) → 400/404, nunca 500; el fix de fecha de calendario inválida de la Fase 08 reconfirmado EN VIVO en `/resultados` y `/expenses` → 400 en ambos, sin rollover silencioso. Sin SQL injection (sin `$queryRaw` en el módulo, solo query builder tipado), sin XSS (sin `dangerouslySetInnerHTML` en el frontend), sin secretos ni datos sensibles sin cifrar más allá del criterio ya aceptado en el resto del sistema. `npm audit`: mismos 12 advisories/3 high de siempre (TD-9, sin cambios). **1 hallazgo LOW ya esperado**: `POST /expenses`/`POST`/`PATCH /expense-categories`/`PATCH /settings/:clave` sin `@nestjs/throttler` — mismo patrón transversal que TD-12/TD-14/TD-15, registrado como **TD-16** en `state/TECH_DEBT.md`, no bloquea. Sin hallazgos nuevos en las 19 categorías restantes (detalle sección por sección en el reporte). No se declara el módulo seguro de forma permanente — corresponde a la Fase 11 confirmarlo de forma independiente. `backend/.env.example` no tocado. | agente |
| 2026-08-29 | Módulo `expenses`/`resultados` — Fase 10 (security remediation) | VERDE | `state/reports/modulo-expenses-resultados-secremediation-2026-08-29.md` — rama `fase10-secremediation-expenses-resultados`. **Sin ningún cambio de código** — la Fase 09 no dejó CRITICAL/HIGH/MEDIUM pendiente, solo 1 LOW (TD-16, rate limiting) que, según `05-quality-gate.md` ("LOW: pueden permanecer como deuda técnica documentada"), no exige remediación de código — mismo criterio ya aplicado en la Fase 10 de `returns`. 514/514 unitarios, 436/436 integración, `tsc`/`eslint`/`build` reconfirmados limpios (no reciclados: se corrieron de nuevo en esta fase). No se declara el módulo seguro todavía — sigue la Fase 11 (re-auditoría, confirma de forma independiente que sigue sin CRITICAL/HIGH). `backend/.env.example` no tocado. | agente |
| 2026-08-29 | Módulo `expenses`/`resultados` — Fase 11 (security re-audit) | VERDE | `state/reports/modulo-expenses-resultados-secreaudit-2026-08-29.md` — sin ningún archivo modificado (regla de la fase). No aplica la omisión por proporcionalidad (`resultados` es MEDIO, no BAJO, en `MVP_SCOPE.md` §5). `git diff` confirmado: cero commits tocaron `backend/src`/`backend/test` entre la Fase 09 y esta fase (la Fase 10 no tuvo nada que remediar). **Reconfirmado EN VIVO contra el servidor real, mismo resultado exacto que la Fase 09**: sin sesión → 401 en las 4 rutas probadas; `Origin` cross-site + cookie real en `POST /expense-categories` → 403 "Origen no autorizado"; `bloqueada` forzada en el body → 400 "property bloqueada should not exist"; id no numérico → 400, id inexistente → 404 (nunca 500); el fix de fecha de calendario inválida de la Fase 08 reconfirmado en `/resultados` y `/expenses` → 400 en ambos, sin rollover silencioso. Sin cambios en `package.json`/`package-lock.json` desde la Fase 09 — no hizo falta repetir `npm audit`. **Sin hallazgos nuevos, sin CRITICAL/HIGH/MEDIUM.** 514/514 unitarios, 436/436 integración (sin cambios). El único hallazgo (TD-16, LOW) sigue como deuda técnica documentada, no bloquea. El módulo queda en condiciones de avanzar a la Fase 12 (production readiness) — no se declara "listo para producción" acá. `backend/.env.example` no tocado. | agente |
| 2026-08-29 | Módulo `expenses`/`resultados` — Fase 12 (production readiness) | VERDE — **PRODUCTION READY: YES** | `state/reports/modulo-expenses-resultados-prodready-2026-08-29.md` — sin código modificado (regla de la fase); única escritura fue el reporte y el bookkeeping (TD-17 nueva en `state/TECH_DEBT.md`). **Sin CRITICAL ni HIGH.** Security/Reliability/Performance/Code quality/Observability/Deployment revisados contra el checklist de la fase, todo corrido de nuevo (no reciclado): 514/514 unitarios, 436/436 integración (Postgres real), 84/84 Vitest, mutación 98.85% reconfirmada (sin cambios desde la Fase 08). Reliability: atomicidad por transacción única en `registrarGasto` (mismo patrón estructural ya confiado en `sales`/`returns`/`cash-registers`), sin timeouts/dependencias externas (todo contra Postgres), idempotencia en `POST /expenses`. Performance: sin N+1 en las 3 services, sin duplicación de consultas. **1 hallazgo LOW nuevo**: ninguna de las tablas `expenses`/`sales`/`sale_items`/`returns`/`return_items` tiene índice explícito sobre `fecha` (confirmado leyendo la migración SQL directamente) — registrado como **TD-17**, pre-existente y transversal a `sales`/`returns` (ya pasaron su propia Fase 12 con el mismo patrón sin bloquear), no bloqueante a la escala del MVP. Code quality: sin duplicación/TODO/dead-code. Observability y deployment revisados de punta a punta — sin variables de entorno ni migraciones nuevas en toda la Etapa 6. Build backend y frontend limpios, bundle frontend 684.48 kB/205.89 kB gzip, idéntico al de T6.9 (sin crecimiento). **2 LOW registradas** (TD-16 rate limiting, TD-17 índices de fecha — ambas transversales, no específicas de este módulo). **Módulo `expenses`/`resultados` aprobado — pasa a la Fase 13 (Integration Audit) cuando corresponda, junto con el resto de los módulos del MVP. Etapa 6 (`expenses`/`resultados`) completamente cerrada: T6.1–T6.9 VERDE + Fases 07→12 VERDE, mismo camino que `sales`/`returns` recorrieron antes. Con este cierre, los 6 módulos del MVP (`auth`, `products`/`variants`+`stock`, `cash-registers`, `sales`, `returns`, `expenses`/`resultados`) tienen su ciclo de construcción y cierre completo.** `backend/.env.example` no tocado. | agente |
| 2026-08-29 | **Sistema completo — Fase 13 (Integration Audit)** | VERDE | `state/reports/integration-audit-2026-08-29.md` — rama `fase13-integration-audit`. Precondición verificada: los 6 módulos del MVP tienen Fase 12 = VERDE. Sin código modificado (regla de la fase). Mapa de dependencias reales (`imports` de cada `*.module.ts`) contrastado contra `MVP_SCOPE.md` §3: **sin diferencias**. Contratos cruzados verificados en el código real (no solo confiados por fe): `stock.service.ts` único escritor de `stock_movements` confirmado a nivel de todo el repo; `sales.service.ts` toma el lock de variantes ordenado por id ANTES de descontar, tal como `stock.service.ts` exige; `returns.service.ts` toma el lock de `sale_items` ordenado por id ANTES de validar el remanente disponible (previene doble-devolución concurrente de la misma línea); "nunca más de una sesión de caja ABIERTA" enforced por un ÍNDICE ÚNICO PARCIAL de Postgres (`WHERE estado = 'ABIERTA'`), no por lógica de aplicación — inmune a race conditions; el patrón de "double-checked locking" de `registrarMovimiento` (lock de sesión → re-lee estado → recién ahí escribe) es UNA sola implementación compartida por `sales`/`returns`/`expenses`, no 3 copias que pudieran desincronizarse; idempotencia confirmada sin doble efecto en un reintento (el `create` de la fila principal, con la idempotency key, ocurre ANTES que cualquier descuento/reingreso de stock dependiente de su id); matriz RBAC de los 15 controllers consistente con BLUEPRINT §5.1 en las 10 filas relevantes, ocultamiento de costo a nivel de campo (no de ruta) confirmado con el mismo mecanismo en los 3 módulos que exponen costo. **Verificación en vivo de punta a punta**, un escenario que cruza `cash-registers`+`sales`+`returns`+`expenses`+`resultados`+`stock` en la misma corrida (abrir sesión → venta 2 unidades EFECTIVO → devolución 1 unidad con reingreso y reintegro EFECTIVO → gasto EFECTIVO → cerrar con arqueo): **cero discrepancias en los 6 números de cruce** (montoSistema, ingresos, cmv, margenBruto, diferencia de arqueo, stockActual final — cada uno calculado a mano antes de pedirlo y confirmado exacto contra el servidor real). Escaneo de dependencias sistema completo: backend 12 advisories/3 high (sin cambios, TD-9), frontend 0. Escaneo de secretos manual (sin `gitleaks` disponible en el entorno): sin resultados sobre todo el repo versionado, `backend/.env.example` releído y confirmado plantilla vacía. **Sin CRITICAL, HIGH ni MEDIUM. Sin hallazgos nuevos** — los únicos LOW son los ya conocidos y transversales (TD-16, TD-17). **Los 6 módulos del MVP quedan integrados sin hallazgos de integración pendientes.** | agente |
| 2026-08-29 | **Sistema completo — Fase 14 (E2E realista)** | VERDE | `state/reports/e2e-2026-08-29.md` — rama `fase14-e2e`. Precondición verificada: Fase 13 VERDE. Los 11 flujos pedidos ejercitados **por la UI real** (navegador, backend+frontend corriendo, Postgres real), no por API directa: login, apertura de caja, alta de producto, entrada de stock, venta (búsqueda tipo scanner por SKU), actualización de stock, registro de pago (importe precargado exacto), devolución, reingreso de stock, cierre de caja (diferencia de arqueo "0" exacta), consulta de reportes — los 6 números de cruce verificados contra el estado persistido real, no solo contra el mensaje en pantalla. **8 escenarios adversos**, todos sin discrepancia: operación cancelada (modal de confirmación, sin `POST` de por medio); producto inexistente (mensaje claro, sin romper el carrito); stock insuficiente (409 con mensaje humano, carrito y pago NO se pierden, sin venta parcial creada); usuario sin permisos (`SELLER` real — nav oculta las 3 rutas OWNER-only, navegación DIRECTA por URL también bloqueada, `costoActual` confirmado AUSENTE de la respuesta de red, no solo escondido en la UI); **doble envío/operación duplicada — el hallazgo más relevante de la fase, sin bloquear**: un triple-click sintético (peor caso que cualquier click humano, sin dar tiempo a React de deshabilitar el botón) generó 3 `POST /sales` reales, contenidos por "stock insuficiente" en los 2 de más — para aislar la protección real de idempotencia (sin la ayuda accidental de falta de stock), se repitió con 3 requests HTTP **genuinamente concurrentes** (`curl ... & wait`), misma `Idempotency-Key`, stock de sobra: **1 sola venta real creada** (3 respuestas 201 byte-a-byte idénticas, mismo id), stock descontado una sola vez — la doble defensa (UI + servidor) confirmada funcionando incluso cuando la capa de UI se esquiva a propósito; refresh durante operación (el carrito sobrevive por diseño, `sessionStorage`, sin ningún `POST` disparado por el refresh en sí); pérdida de conexión (sin simulación literal posible en el entorno — TD-3 reconfirmado vigente, sin cambios); operaciones simultáneas (cubierto por los locks ya verificados en la Fase 13 + el test de concurrencia real de esta fase). **Sin discrepancias documentadas.** Datos de prueba (usuario `SELLER`, producto/variante de prueba) sin residuo funcional relevante al terminar. | agente |
| 2026-08-29 | **Sistema completo — Fase 15 (Concurrencia y carga)** | VERDE | `state/reports/concurrency-2026-08-29.md` — rama `fase15-concurrency-load-test`. Precondición verificada: Fase 14 VERDE. Sin código modificado. Los 4 escenarios pedidos por el protocolo, probados con requests HTTP **genuinamente concurrentes** (`curl ... & wait`, nunca secuenciales) contra el servidor real, estado verificado releyendo la base después de cada carrera, nunca solo el código HTTP: **A) ventas concurrentes con stock limitado** (8 `POST /sales` paralelas, 8 keys distintas, stock=5) → 5×201/3×409 exacto, stock final 0, 5 ids distintos sin duplicados; **B) cierre de caja mientras hay una venta en curso** → el cierre ganó, la venta perdió con 409 "No hay una sesión de caja abierta", rollback COMPLETO de la venta (sin descuento de stock parcial, sin fila huérfana); **C) doble submit** (3 `POST /cash-registers/movements/ingreso` paralelos, misma key) → 1 sola fila real (3 respuestas byte-a-byte idénticas), `montoSistema` sin triplicar — reconfirma en otro endpoint el mismo mecanismo ya probado para `sales` en la Fase 14; **D) apertura de caja concurrente** (5 `POST /cash-registers/sessions` paralelos, 5 keys distintas) → 1×201/4×409 "Ya hay una sesión de caja abierta" exacto, el índice único parcial de Postgres identificado en la Fase 13 confirmado sosteniendo la garantía bajo concurrencia HTTP real; **E) devolución concurrente de la misma línea** (2 `POST /returns` paralelos pidiendo 2+2 unidades de una venta de 3) → 1×201/1×400 "supera lo disponible", `cantidadDisponible` final exacta (1), sin sobre-devolución. **Ninguna race condition encontrada en los 5 escenarios — sin CRITICAL/HIGH/MEDIUM/LOW nuevos.** Datos de prueba sin residuo funcional relevante (sesión de caja cerrada con arqueo exacto al terminar). | agente |
| 2026-08-30 | **Sistema completo — Fase 16 (Release Candidate)** | **NOT READY** | `state/reports/release-candidate-2026-08-29.md` — rama `fase16-release-candidate`. Precondición verificada: Fases 13/14/15 VERDE. **Incidente al arrancar, resuelto (no es un bug de la app)**: la suite de integración falló en 6 archivos porque mis propias pruebas manuales en vivo de las Fases 08-15 dejaron datos reales (ventas/devoluciones/gastos/sesiones de caja/usuarios/categorías de prueba) en la base de dev compartida, y las rutinas de limpieza de los tests asumen poder borrar cualquier fila de esas tablas — resuelto con una limpieza quirúrgica (no un `prisma migrate reset`, bloqueado por el entorno; confirmado con el usuario antes de proceder) respetando FKs y el trigger de inmutabilidad de `cash_movements`, recalculando `stock_actual` después. **436/436 integración tras la limpieza, dos corridas sin flakes.** Repetido todo de nuevo (no reciclado): 514/514 unitarios, 84/84 Vitest, `tsc`/`eslint`/build limpios en ambos proyectos, `npm audit` (12/3 high backend sin cambios — TD-9, 0 frontend), escaneo manual de secretos sin resultados, `npx prisma migrate status` sin pendientes. Seguridad/permisos/integridad de datos/concurrencia: sin cambios desde las Fases 09/11/13/14/15, todo sigue sosteniéndose. **Hallazgo nuevo, HIGH, BLOQUEANTE**: revisando `ROADMAP.md` (Etapa 0) se encontró que **T0.6 ("Backup diario: GitHub Action con `pg_dump` cifrado a almacenamiento externo") sigue `PENDIENTE`, nunca se implementó** — confirmado que `.github/workflows/` solo tiene `ci.yml` (lint/build/tests), ningún workflow de backup. Bloquea según `QUALITY_GATE.md` ("HIGH: bloquea si afecta... pérdida de información") — sin ninguna copia de seguridad, una falla de la base pierde TODO el historial de la tienda sin forma de recuperarlo. **3 hallazgos nuevos no bloqueantes, registrados en `state/TECH_DEBT.md`**: TD-18 (MEDIUM, T0.10 — Stryker corrido manualmente en cada Fase 08 pero nunca integrado a `ci.yml`, riesgo de regresión silenciosa futura, no del código actual); TD-19 (LOW, script `test:e2e`/Playwright colgado sin ningún test real); TD-20 (LOW, la tabla-resumen de `ROADMAP.md` desincronizada de `STATUS.md` desde la Fase 13). Riesgo de negocio informativo (no de código, ya documentado en `DECISIONES_PENDIENTES.md`): B1 (¿necesita facturar legalmente, AFIP?) sin confirmación en el repo de que se resolvió con la clienta. **No se declara el sistema listo para producción — el único blocker es de infraestructura (backup), no de código: la aplicación en sí misma está lista.** Sigue resolver T0.6 antes de la Fase 18 (Deploy checklist). | agente |
| 2026-08-30 | T0.6 — Backup diario (GitHub Action, `pg_dump` cifrado a almacenamiento externo) | VERDE | Resuelve el blocker HIGH de la Fase 16. `.github/workflows/backup.yml` (nuevo): corre a diario (06:00 UTC = 03:00 ART, horario de tráfico mínimo) y también a mano (`workflow_dispatch`); falla explícito de entrada (`::error::`) si falta cualquiera de los 6 secrets necesarios, en vez de correr a medias; `pg_dump --no-owner --no-privileges` → cifrado GPG simétrico AES256 (passphrase desde secret) → sube a un bucket S3-compatible (R2/B2, BLUEPRINT §9.10) → borra del bucket los backups con más de 30 días (retención mínima exigida por el blueprint), todo dentro del mismo workflow, sin depender de una lifecycle rule configurada aparte en el bucket. `docs/ops/BACKUP.md` (nuevo): qué secrets crear y por qué (con recomendación de un usuario de solo lectura para `BACKUP_DATABASE_URL`, nunca el mismo que usa la app), cómo verificar que corrió bien, y el comando manual de restauración (la prueba real de restauración de punta a punta es explícitamente responsabilidad de la Fase 17, no de este ticket). **Ensayado de punta a punta contra la base de dev real** (Docker local, no solo revisado el YAML): `pg_dump` real → cifrar → descifrar (`diff` vacío contra el dump original) → restaurar contra una base nueva → conteo de filas idéntico tabla por tabla (`users`/`settings`/`expense_categories`/`variants`/`products`). El ensayo encontró un problema real menor: el cliente `pg_dump` local (v18) generaba un dump con una directiva que un Postgres 16 no reconoce al restaurar (`ERROR: unrecognized configuration parameter "transaction_timeout"`) — inofensivo en los datos (mismo conteo de filas), pero corregido fijando `postgresql-client-16` en el workflow (la misma versión mayor que `docker-compose.yml`) para que no vuelva a aparecer. YAML validado con `js-yaml`, los dos bloques de shell más complejos (verificación de secrets, borrado de vencidos) syntax-checkeados con `bash -n` y la lógica de borrado ensayada con datos falsos. De paso, sincronizada la tabla-resumen de fases de `state/ROADMAP.md` con el estado real de `STATUS.md` (TD-20, quedaba desde la Fase 13). **No se puede probar el workflow REAL (contra producción, con los 6 secrets configurados) porque todavía no hay ningún hosting provisionado** — eso queda para cuando se configure Neon/R2 de verdad, antes de la Fase 18. `backend/.env.example` no tocado (los secrets del backup nunca van ahí). | agente |
| 2026-08-30 | **Sistema completo — Fase 16 (Release Candidate), re-corrida** | **READY** | `state/reports/release-candidate-2026-08-30.md` — rama `fase16-release-candidate-v2`. Re-corrida tras resolver T0.6 (único blocker de la corrida anterior, `state/reports/release-candidate-2026-08-29.md`, NOT READY). Todo repetido de nuevo, no reciclado: 514/514 unitarios, 436/436 integración (la limpieza de datos de la corrida anterior se mantuvo, sin nueva contaminación), 84/84 Vitest, `tsc`/`eslint`/build limpios en ambos proyectos (bundle sin cambios), `npm audit` (12/3 high backend sin cambios — TD-9, 0 frontend), escaneo manual de secretos sin resultados (incluidos los 2 archivos nuevos de T0.6), `npx prisma migrate status` sin pendientes. Seguridad/permisos/integridad de datos/integración/E2E/concurrencia/variables de entorno/logs/manejo de errores/performance: sin cambios de código desde la corrida anterior que pudiera haberlos afectado, se dan por vigentes. **Sin CRITICAL, sin HIGH, sin blockers.** Único riesgo remanente: TD-18 (MEDIUM, Stryker no integrado a CI — riesgo de mantenimiento futuro, no del código actual); TD-20 ya resuelto de paso en el propio commit de T0.6. Riesgo de negocio informativo, no de código: B1 de `DECISIONES_PENDIENTES.md` (AFIP) sin confirmación documentada de que se resolvió con la clienta. **RELEASE STATUS: READY — el sistema completo queda declarado Release Candidate.** Sigue, cuando corresponda (requieren hosting real provisionado, fuera del alcance de esta sesión): Fase 17 (backup restore drill con los secrets configurados), Fase 18 (deploy checklist) y Fase 19 (production smoke test). `backend/.env.example` no tocado. | agente |
| 2026-08-30 | Ticket nuevo — Listado de ventas por fecha (`GET /sales`) + búsqueda en Devoluciones | VERDE | Commit `de5fa1b` en rama `feat/listado-ventas-busqueda`. Hallazgo real navegando el sistema post-RC: para procesar una devolución hace falta el número de venta, que solo aparecía un instante en el toast del cobro (sin ticket impreso, AMB-9 diferida) y no quedaba anotado en ningún lado — sin forma de listar ventas pasadas, una devolución de hace unos días era efectivamente imposible. `GET /sales` (paginado, `desde`/`hasta` opcional, sin `@Roles()` — mismo criterio que `POST /sales`, cualquiera autenticado; decidido con el usuario vía `AskUserQuestion`) + sección colapsable "Buscar por fecha" en `DevolucionPage.tsx` (un click en una fila carga la venta en el flujo de devolución ya existente). **Bug real encontrado en vivo, no en el papel**: probando el flujo con una venta real, `desde`/`hasta` con `Date.UTC` ingenuo (copiado del patrón de `ExpensesService.findAll`) excluía casi todo el día de hoy en hora argentina — corregido usando `argentinaDayRangeToUtc` (AD-13/T0.7, mismo mecanismo que `ResultadosService`). El mismo defecto en `GET /expenses` quedó documentado aparte (ver fila siguiente) en vez de mezclarlo en este commit. 531 unit + 445 integration, todo verde. | agente |
| 2026-08-30 | Ticket nuevo — Fix retroactivo: `GET /expenses` filtraba `desde`/`hasta` en UTC ingenuo, no en hora argentina | VERDE | Rama `fix/expenses-filtro-fecha-hora-argentina`. Mismo bug encontrado en el ticket de `sales` (fila anterior), reconfirmado acá: `ExpensesService.findAll` usaba `new Date(query.desde/hasta)` tal cual, violando CLAUDE.md §6 ("toda agrupación por día o período se calcula en hora argentina, nunca en UTC") — un gasto cargado a última hora del día quedaba fuera de un filtro "hasta hoy". Corregido con `argentinaDayRangeToUtc` (AD-13/T0.7), mismo fix exacto que `SalesService.findAll`. Tests existentes de `desde`/`hasta` actualizados (fechas de fixture movidas a mediodía UTC para no cruzar el límite de día) + 2 tests de regresión nuevos (unit e integración) reproduciendo el caso real de un gasto a las 23:30 hora argentina. 532 unit + 446 integration, todo verde. `backend/.env.example` no tocado. | agente |
| 2026-08-30 | Ticket nuevo — SKU automático en el alta manual de una variante | VERDE | Rama `feat/sku-automatico-alta-manual`. Pregunta directa del usuario, verificada contra el código antes de responder: `generateSku` (`P{productId}-{TALLE}-{COLOR}`) ya existía y se usaba en la grilla (T2.11) e importación CSV (T2.13) — la única de las tres vías de alta que exigía `sku` a mano era el formulario de "variante suelta" (`CreateVariantDto.sku`, `@IsNotEmpty()`), tanto en el backend como en la validación del formulario del frontend (`NewVariantPage.tsx`, "El SKU es obligatorio"). `sku` ahora opcional en ambos DTOs (backend y frontend) — sin él (o vacío), `VariantsService.create` busca `size`/`color` solo cuando hace falta y genera uno con la misma función que ya usan los otros dos caminos. Tests nuevos: 4 unitarios (sin size/color, con ambos, sku vacío tratado como ausente, con sku explícito no consulta la base de más) + 3 de integración. Verificado además en vivo contra el backend real: variante creada sin tocar el campo SKU en el formulario → `P{productId}-M` persistido correctamente. 536 unit + 449 integration, todo verde. `backend/.env.example` no tocado. | agente |
| 2026-08-30 | **Sistema completo — Fase 16 (Release Candidate), re-corrida tras 3 tickets nuevos** | **READY** | `state/reports/release-candidate-2026-08-30-v3.md` — rama `fase16-release-candidate-v3`. Re-corrida de `release-candidate-2026-08-30.md` (READY) tras los 3 tickets de las filas anteriores (listado de ventas, fix de timezone en `expenses`, sku automático), todos post-declaración de Release Candidate. Todo repetido de nuevo, no reciclado: 536/536 unitarios backend, 449/449 integración (Postgres real), 84/84 Vitest frontend, `tsc`/`eslint`/build limpios en ambos proyectos (bundle 690.67 kB/207.35 kB gzip, crecimiento esperado — TD-10), `npm audit` (12/3 high backend sin cambios — TD-9, 0 frontend), escaneo manual de secretos sin resultados, `npx prisma migrate status` sin pendientes. Permisos/mass-assignment/integridad de `sku` reconfirmados específicamente para los 3 tickets nuevos (sin cambios de rol, `SaleListItem` sin costo/margen, unicidad de sku por constraint de base, sin gaps nuevos). Verificación manual en vivo de los 3 tickets (no solo tests), datos de prueba limpiados sin residuo. **Sin CRITICAL, sin HIGH, sin blockers.** Único riesgo remanente: TD-18 (MEDIUM, Stryker no integrado a CI, sin cambios). Riesgo de negocio informativo, no de código: B1 de `DECISIONES_PENDIENTES.md` (AFIP), sin cambios. **RELEASE STATUS: READY — el sistema completo, con los 3 tickets nuevos ya incluidos, queda re-confirmado como Release Candidate.** Sigue, cuando corresponda (requieren hosting real provisionado): Fase 17, 18 y 19. `backend/.env.example` no tocado. | agente |
| 2026-08-30 | Ticket nuevo — Dashboard de inicio (accesos rápidos + estado de caja + resumen de hoy para OWNER) | VERDE | Rama `feat/dashboard-inicio`, solo frontend (sin cambios de backend: reusa `GET /resultados` y `GET /cash-registers/sessions/open`, ya existentes). Dos decisiones resueltas con el usuario vía `AskUserQuestion`: (1) contenido para OWNER — accesos rápidos + resumen financiero del día (ingresos/margen bruto/resultado neto, en hora argentina vía el cálculo ya existente de `resultados`); (2) ubicación — reemplaza el redirect de `/` a `/catalogo` (antes `<Navigate>`), ahora `/` es el dashboard, con "Inicio" agregado como primer link de la barra de navegación. `DashboardPage.tsx` (nuevo): estado de caja (abierta/sin sesión/error, mismo patrón que `CashRegisterPage.tsx`) visible para cualquier rol — sin caja abierta no se puede vender; resumen de hoy solo para OWNER (RN-2/RN-11: el `useEffect` que llama a `getResultados` ni se dispara para SELLER, no es solo un `if` de render); grilla de accesos rápidos (Vender destacado visualmente, Devoluciones, Caja, Catálogo para cualquiera; Gastos/Resultados/Configuración solo OWNER, mismo gate que `RequireOwner` en el router). Verificado en vivo con los dos roles reales: OWNER ve las 7 tiles + resumen; SELLER ve solo las 4 comunes, sin resumen y sin llamar a `/resultados`. 536 unit + 449 integration backend (sin cambios, ticket frontend-only) + 84 Vitest frontend, todo verde. `backend/.env.example` no tocado. | agente |
| 2026-08-31 | Fix — Devoluciones: listado de "prenda nueva" en Cambio quedaba recortado | VERDE | Rama `fix/devolucion-cambio-dropdown-recortado`, solo frontend. Bug real reportado por la usuaria navegando el sistema. Causa encontrada inspeccionando el DOM en vivo (no adivinada): la tarjeta `Card` que envuelve la sección "Prenda nueva" en `DevolucionPage.tsx` tiene `overflow: hidden` por defecto (Mantine, para recortar las esquinas redondeadas) — eso recortaba el listado de resultados de la búsqueda, que cuelga por debajo del input con `position: absolute` y crece más que el alto de esa tarjeta. Fix de una línea: `style={{ overflow: 'visible' }}` en esa `Card`. Verificado en vivo contra una venta real: antes del fix, los resultados se recortaban a la mitad; después, se ven completos. Sin tests automatizados (no hay convención de tests de UI/componentes en el frontend de este proyecto — solo Vitest para lógica pura, confirmado antes de omitirlos). `backend/.env.example` no tocado. | agente |
| 2026-08-31 | Ticket nuevo — Alta rápida de marca/categoría/talle/color desde los combos del catálogo | VERDE | Rama `feat/alta-marca-categoria-talle-color`, solo frontend. Hallazgo reportado por la usuaria navegando el sistema: los combos de Marca/Categoría (`ProductForm.tsx`) y Talle/Color (`NewVariantPage.tsx`, `VariantGridPage.tsx`) solo dejaban ELEGIR (`GET`), nunca crear — verificado que los 4 `POST` correspondientes ya existían en el backend sin `@Roles()` (cualquiera autenticado), la ausencia era pura falta de UI. Mismo patrón ya probado en el proyecto (`expenses/components/NuevaCategoriaModal.tsx`, T6.1/T6.8): un botón "+ Nueva/o" al lado de cada combo abre un modal chico, y al crear recarga la lista y deja el valor nuevo ya seleccionado. `NuevoValorModal.tsx` (nuevo, genérico) cubre marca/categoría/color — mismo shape exacto (`{nombre}` → `{id, nombre}`). `NuevoTalleModal.tsx` (nuevo, aparte) cubre talle, que necesita un campo más (`orden`, BLUEPRINT §3.2 — "S, M, L, XL en ese orden, no alfabético"): sugiere el siguiente número libre (máximo actual + 1), editable por si hay que insertar uno en el medio. Verificado en vivo contra el backend real, los 4 casos: marca, talle (con su `orden` sugerido correcto) y color creados y auto-seleccionados en `NewVariantPage`/`ProductForm`, sin dejar residuo (datos de prueba limpiados). 536 unit + 87 integration (`sales-controller`/`returns-controller`/`returns`, aislados — la corrida completa chocó con la sesión de caja real y ventas reales de la usuaria, activa en simultáneo; sin cambios de backend en este ticket, no ameritaba forzar la corrida completa contra datos en uso) + 84 Vitest + build limpio, todo verde. `backend/.env.example` no tocado. | agente |
| 2026-08-31 | Fix — Venta: cantidad editable directo en el carrito | VERDE | Rama `feat/cantidad-editable-carrito-venta`, solo frontend. Hallazgo real reportado por la usuaria: para cargar una cantidad grande (ej. "10 remeras") no había forma directa — el atajo `Ctrl`+`+` documentado en pantalla no funciona de forma confiable en la mayoría de los navegadores (es el atajo reservado de zoom del navegador, `preventDefault()` no lo frena) y, aunque funcionara, sumar de a uno diez veces no es lo que se pedía. `setLineQuantity` (nuevo, `cart.ts`) escribe la cantidad exacta de una — la celda "Cantidad" del carrito (`SalePage.tsx`) pasa de texto estático a un `NumberInput` editable. 4 tests nuevos en `cart.test.ts` (cantidad exacta, nunca baja de 1, trunca decimales, no toca otras líneas). Verificado en vivo: escribir "10" en el campo actualiza cantidad/subtotal/total correctamente al instante. 88 Vitest + build limpio. Sin cambios de backend. `backend/.env.example` no tocado. | agente |
| 2026-08-31 | Fix — Cambio: el número de la venta nueva no se mostraba en ningún lado | VERDE | Rama `feat/numero-venta-nueva-en-cambio`. Hallazgo real reportado por la usuaria: al confirmar un Cambio, el toast solo mostraba "Devolución #X — venta nueva vinculada" — X es el número DE LA DEVOLUCIÓN (`Return.numero`), no el de la venta nueva; buscarlo como "número de venta" no encontraba nada, confundiendo los dos. Causa: `POST /returns` devolvía el `Return` crudo, que solo tiene `saleNuevaId` (id interno) — el `numero` de esa venta nunca viajaba. `CrearDevolucionResult` (nuevo, `returns.service.ts`) agrega `saleNuevaNumero: number | null` (`null` en una DEVOLUCION simple); resuelto en los tres caminos posibles (creación fresca — ya tenía `ventaNueva.numero` en memoria, sin consulta extra; cortocircuito de idempotencia dentro de la transacción; y el fallback de idempotencia a nivel HTTP ante un choque concurrente genuino — los dos últimos comparten `resolveSaleNuevaNumero`, nuevo método del servicio). Frontend: `ReturnResult.saleNuevaNumero` (nuevo) y el toast de Cambio ahora dice explícito "venta nueva #Y"; ambos toasts (devolución y cambio) pasan a `autoClose: false` — el número hace falta después, no tiene sentido que desaparezca solo. Tests nuevos: 2 unitarios (`resolveSaleNuevaNumero` vía los dos caminos de idempotencia, con mock diferenciado por `where.id` ya que comparte el mock de `tx.sale.findUnique` con la consulta de la venta original) + 2 aserciones nuevas en tests de integración existentes (CAMBIO real → `saleNuevaNumero` coincide con la venta real; DEVOLUCION simple → `null`). Verificado en vivo de punta a punta contra el backend real: cambio real, toast mostró "venta nueva #17490", buscarlo en "Número de venta" lo encontró — antes de este fix no se podía. Datos de prueba limpiados sin residuo (venta original, venta nueva, devolución, movimientos de stock y caja, sesión). 537 unit + 449 integration (33/33 archivos, sin sesión de caja real interfiriendo esta vez) + build limpio, todo verde. `backend/.env.example` no tocado. | agente |
| 2026-08-31 | Ticket nuevo — Historial de ventas (pantalla de solo lectura) | VERDE | Rama `feat/historial-ventas`, solo frontend. Pedido directo del usuario, tras la charla sobre el ticket impreso: no había ninguna pantalla para simplemente MIRAR ventas pasadas — la única búsqueda existente vivía adentro de Devoluciones, pensada para procesar un cambio, no para consultar. `HistorialVentasPage.tsx` (nuevo, `/ventas`, link "Historial" en el nav — sin `@Roles()`, mismo criterio que `GET /sales`, cualquiera autenticado): filtro `desde`/`hasta` opcional, tabla paginada (número/fecha/total/estado con badge de color), mismo patrón exacto que `GastosPage.tsx`. Reusa `GET /sales` tal cual — cero cambios de backend. `listarVentas`/`SaleListItem`/`PaginatedResult` agregados a `features/sales/api.ts`/`types.ts` (duplicado del de `features/returns`, mismo criterio ya establecido en el proyecto de aceptar esta duplicación entre features en vez de un import cruzado). Verificado en vivo: venta real creada → aparece en el historial con número/fecha/total/estado correctos; datos de prueba limpiados sin residuo. 537 unit + 449 integration backend (sin cambios) + 88 Vitest + build limpio, todo verde. `backend/.env.example` no tocado. | agente |
| 2026-08-31 | Fix — Acceso rápido a Historial faltaba en el dashboard | VERDE | Rama `fix/dashboard-tile-historial`, solo frontend. Hallazgo de la auto-revisión del sistema pedida por el usuario ("podés seguir navegando vos... y detectando cosas que no estén del todo bien"): el link de nav a `/ventas` (Historial) se agregó en el ticket anterior, pero la grilla de accesos rápidos de `DashboardPage.tsx` nunca se actualizó para reflejarlo — inconsistencia menor entre dos puntos de entrada a la misma pantalla. Se agrega la entrada "Historial" a `ACCIONES_COMUNES`, entre "Vender" y "Devoluciones". Sin cambios de backend, sin tests nuevos (el módulo no tiene tests dedicados, consistente con el resto de `dashboard/`). tsc + eslint + 88 Vitest + build limpio, todo verde. `backend/.env.example` no tocado. | agente |
| 2026-08-31 | Fix — La ficha de producto nunca mostraba talle/color de sus variantes | VERDE | Rama `fix/talle-color-en-ficha-producto`, solo frontend. Hallazgo de la misma auto-revisión: `ProductDetailPage.tsx` nunca mostraba talle/color de cada variante, ni en la tabla ni en los títulos de los 4 modales de edición (Editar/Precio/Ingreso/Ajuste) — la única forma de distinguir filas era el SKU, que no siempre lo dice (ej. `jdjijdodjoi`). El listado del catálogo (`CatalogPage.tsx`) sí los resolvía; esta pantalla no. Se agregan columnas Talle/Color a la tabla y un `varianteLabel(variant)` que resuelve `sizeId`/`colorId` contra `getSizes()`/`getColors()` (mismo patrón ya usado en `NewVariantPage.tsx`/`VariantGridPage.tsx`: tablas de referencia globales pedidas una sola vez, no por producto, `Map<id, entidad>` para lookup O(1); cae al SKU si la variante no tiene ninguno de los dos, ej. un cinturón sin variación). El label resuelto se pasa como prop nueva `label: string` a los 4 modales, que ahora lo usan en su título (antes siempre decían lo mismo sin importar qué variante se estaba editando). Sin cambios de backend — `Variant` ya traía `sizeId`/`colorId` crudos, no hizo falta tocar el `include` del backend por un dato que solo hace falta para mostrar. Verificado en vivo de punta a punta contra el backend real (login OWNER real, producto multivariante real "Campera de Jean"): tabla mostrando Talle/Color correctos por fila, modal "Editar variante — M / Negro" y "Editar precio — M / Negro" confirmados (antes decían solo "Editar variante"/"Editar precio — <SKU>"), sin someter ningún formulario (verificación de solo lectura, cero residuo). tsc + eslint + 88 Vitest + build limpio, todo verde. `backend/.env.example` no tocado. | agente |
| 2026-08-31 | Fix — No había forma visible de sacar un producto del carrito de venta | VERDE | Rama `fix/quitar-producto-del-carrito`, solo frontend. Hallazgo de una segunda ronda de auto-revisión, pedida explícitamente por el usuario ("¿creés que hay más cosas de este estilo?") tras los 4 findings anteriores. La única forma de sacar un producto ya cargado del carrito (`SalePage.tsx`) era el atajo `Ctrl+Supr` — sin ningún botón visible que lo revele; el texto que lo menciona (`Ctrl+Supr la quita`) recién aparece, chico y gris, después de seleccionar la fila. Más grave que el bug de `Ctrl++` ya arreglado: ahí la cantidad al menos tiene un `NumberInput` visible; acá no había ningún control visible en absoluto — la única alternativa para quien no conoce el atajo era cancelar toda la venta. Lo confirma el contraste con Devoluciones, que ya tiene "Quitar" visible en sus listas equivalentes (`quitarNuevaItem`, `PaymentLinesBuilder.quitarLinea`) — Venta era la única pantalla atrasada en esto. Se agrega un botón "Quitar" por fila (mismo texto/estilo que Devoluciones), extraído a `handleRemoveLine` y reusado tanto por el botón nuevo como por `Ctrl+Supr`, que sigue andando igual para quien ya lo usa. De paso, se investigó a fondo un candidato a Finding 2 de la ronda anterior (accesibilidad de los botones +/- de `NumberInput`) leyendo el código fuente de Mantine: **no era un bug real** — esos botones ya vienen con `aria-hidden="true"` y `tabIndex={-1}` puestos por la propia librería (excluidos a propósito del árbol de accesibilidad y del tab order), y la vía accesible ya existe (tipear directo, o flechas ↑/↓ con `withKeyboardEvents`, activado por default) — no se tocó nada ahí. Verificado en vivo: variante agregada por SKU exacto, click en "Quitar" la saca del carrito, totales vuelven a $0 — sin residuo, nunca llegó a tocar el backend (solo el borrador de `sessionStorage`). tsc + eslint + 88 Vitest, todo verde. `backend/.env.example` no tocado. | agente |
| 2026-08-31 | Ticket nuevo — Módulo de clientes mínimo (saldo a favor, BLUEPRINT §8.4) | VERDE | Rama `feat/clientes-saldo-a-favor`. Pedido directo del usuario tras la charla sobre el ticket impreso: que el saldo a favor de una devolución quede registrado atado a una persona, sin depender de que la clienta guarde a mano el número de comprobante. `BLUEPRINT.md` §8.4 ya lo preveía como extensión aditiva ("Tabla customers y un FK opcional en sales. Es aditivo, sin migración de datos.") — alcance acotado a lo pedido: solo el FK en `Return` (de ahí sale el crédito, AMB-16/T5.8), no en `Sale` (sin pantalla que lo usara todavía, hubiera sido columna muerta). `AD-17` (sin cuenta corriente ni fiado) no se toca — esto es "a quién pertenece el crédito que la tienda le debe", no una deuda del cliente hacia la tienda. Migración nueva: `Customer` (`nombre`, `dni` único y obligatorio — pedido explícito del usuario, "puede haber dos Carlos Martínez" — normalizado sin puntos/espacios antes de validar formato de 6-8 dígitos y unicidad; `telefono` opcional) + `Return.customerId` opcional, indexado. `CustomersModule` nuevo: `POST /customers` (alta rápida, P2002 → 409 con mensaje claro, mismo patrón que Marca/Talle/Color), `GET /customers?q=` (busca por nombre o DNI), `GET /customers/:id/credito` (mismo cálculo que `ReturnsService.consultarCredito` pero agregado sobre TODAS las devoluciones del cliente, filtrado a las que todavía tienen saldo). `returns.service.ts` gana un Paso 0c: si viene `customerId` tiene que existir (chequeo barato y puramente informativo, antes de tocar la base) — no cambia ninguna regla de crédito existente. `DevolucionPage.tsx` suma un buscador de cliente opcional (nombre/DNI) + alta rápida antes de confirmar; `CobroPage.tsx` deja buscar el crédito por cliente además de por número de comprobante — con un solo crédito disponible se aplica solo, con más de uno deja elegir cuál. Hallazgo real de paso: `prisma migrate dev` generó un `DROP INDEX` sobre el único `NULLS NOT DISTINCT` de `variants` (evita cargar dos veces un producto sin talle/color) — no está declarado en `schema.prisma` (se agregó a mano en su momento), así que el diff automático lo propone borrar cada vez; se sacó el `DROP` de esta migración y se restauró el índice en la base de dev, el problema de fondo (recurrente en el próximo `migrate dev` de cualquiera) quedó marcado aparte, fuera de alcance de este ticket. Tests nuevos: 7 unitarios (`CustomersService`) + 3 unitarios (`returns.service.spec.ts`, `customerId`) + 11 de integración (alta, búsqueda, crédito por cliente end-to-end incluyendo consumo real vía `POST /sales`, `customerId` inexistente). Verificado en vivo de punta a punta contra el backend real: cliente "Carlos Martínez" creado desde Devolución, devolución simple con crédito atado a él, Cobro lo encontró buscando "Carlos" (crédito único auto-aplicado), pago confirmado referenciando la devolución correcta, y el crédito desapareció de su lista después de consumido. Datos de prueba limpiados sin residuo. 547 unit + 460 integration + 88 Vitest + build, todo verde. `backend/.env.example` no tocado. | agente |

<!--
Agregá una fila nueva por cada fase/ticket/módulo completado. No edites
filas viejas salvo para corregir un error de tipeo — el historial completo
es el punto.
-->
